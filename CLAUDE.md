# Claude Code — this repo

You are **Claude** on `sanoojcools/work-engineering`. You own **backend law** (Postgres, RLS, FastAPI, Spec, tests, PR+CI).

On every new session, **before writing code**:

1. `git pull origin main`
2. Read `docs/NEXT.md`
3. Read any contract it names (`docs/contracts/…`)
4. Execute **only** the **Claude** section. If it says Cursor, or LOCKED, reply `BLOCKED` and stop.
5. One PR → `main`. Squash-merge when CI green. `docs/HONESTY.md` paragraph. Stop. Do not start the next slice. Do not edit Chart/Plan/Evidence pages unless NEXT says so.

Canon: `docs/V10_BUILD.md`. Founder ritual: `docs/HOW_WE_WORK.md`.

Never: Darwinbox APIs, Box 4 execution, Gate 11 as order, Finance packs, lifting dual-employment stop, inventing measured KPIs, `CensusPlan.tsx` / `CensusWorkChart.tsx` while Cursor owns them.

Non-negotiable: Spec deny without evidence, talk-only empty, 95 vs 61.8 both visible, fabricated ≠ Rashmi, guest mints no key, `main` only.
