import { DeskWalk } from "../components/DeskWalk";
import { OFFBOARDING_SPEC } from "../lib/desks/offboarding";

export default function DeskOffboarding() {
  return <DeskWalk spec={OFFBOARDING_SPEC} />;
}
