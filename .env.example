# Database
DATABASE_URL=postgresql+psycopg2://wep:wep@localhost:5432/wep

# LLM-assisted Discovery (optional; service degrades to import-only without a key)
# provider: anthropic | openai | none
LLM_PROVIDER=none
LLM_API_KEY=
LLM_MODEL=claude-sonnet-4-5
# Only needed for OpenAI-compatible endpoints
LLM_BASE_URL=https://api.openai.com/v1

# Enforcement Gateway shared secret used by execution systems to call the Spec API
SPEC_API_KEY=dev-spec-key-change-me
