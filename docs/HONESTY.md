# Scout Import Path — Honesty Matrix

Enforced today vs specified-but-not-enforced, for `POST /api/genome/import`'s `WorkUnitImport` schema. Source: `Scout-Reference.md` (corrected 2026-08-26). Do not add a row claiming a validator that has no matching test.

| Attr | Enforced today | Specified, not enforced |
|---|---|---|
| id | `str`, min 1, max 40 | `^WU-[A-Z]+-[0-9]+[A-Z]?$` |
| name | min 1, max 200 | verb-noun format |
| business_object | min 1; auto-creates `EntityType` | pack ontology allow-list |
| current_condition | min 1; clipped to 80 + ellipsis at write | — |
| desired_condition | same clip | — |
| context | `{decision_branches, variants[]}`, extra=forbid | — |
| trigger | min 1 | verb-content check |
| input | `list[str]` min 1 at API; joined to text col | native array column |
| authority | min 1; PII-scanned | — |
| actor_constraints | min 1; PII-scanned | — |
| acceptance_criteria | `list[str]` min 1 at API; joined | native array column |
| evidence_required | same; PII-scanned | native array column |
| verification_method | free text → enum heuristic, default `human_spot_check` | strict input enum |
| sla_timing | structured, all fields optional, extra=forbid; `time_per_case_min` mapped to `sla_hours`, `volume_per_month` persisted as-is (Slice 2 PR 2c) | `gt=0` on time; `frequency` / `sla_deadline` still not read back |
| dependencies | `list[str]`, default []; unresolved = skip edge | schema/FK reject |
| failure_semantics | min 1 | — |
| regulatory_register_link | `list[str]`; upsert stub on `regulatory_entries.regulation` | `RR-` prefix; must-exist FK |
| provenance | extra=forbid; only `source_type` pattern-validated `^(observed\|declared\|inferred\|designed)$` | `hash_sha256` `^[a-f0-9]{64}$`; server-computed hash; `file_name`; consent existence |

Also true, not a per-attribute row:

- Container `GenomeImportRequest` is `extra="ignore"` — benign top-level metadata (`source_files`, `automation_index`, a stray `org`) does not 422; GQS scores it instead.
- GQS runs on the raw dict, before Pydantic validation.
- `dual_scoring_kappa` is caller-supplied. No mechanism produces two independent scores to compute it from.
- No file writer exists yet for `UploadedFile` — no endpoint uploads a file, so `hash_sha256` in provenance today is whatever the caller supplied, never server-computed.
- Slice 3 PR 3a: `work_units`, `spec`, and `census` are off the legacy global `spec_api_key` (default `dev-spec-key-change-me` is gone, not just changed) and onto per-org `X-Spec-Key` / RLS, same as genome and files. `POST /api/org/keys/rotate` rotates a key; the old one 401s after a 60-minute grace window (`dependencies.ROTATION_GRACE_MINUTES`). `ontology`, `discovery`, `projections`, `verdict`, `work-graph`, `economics`, `regulatory`, `verification`, `clients`, and `admin` are still unauthenticated — that's out of PR 3a's scope (playbook G.1 names work_units/spec/census only), not a gap this PR closed. **They are still open, but no longer non-deterministic.** They run as `wep_app`, which RLS binds, and nothing set `app.current_client_id` for them, so they answered from whatever the pooled connection happened to be carrying from a previous request — in practice nothing, which is why Projections rendered `0 inventory / 0 work graph edges` for a tenant holding 14 work units and 20 edges, and occasionally a *previous caller's* tenant. `discovery`, `projections`, `verdict`, `work-graph`, `economics` and `verification` now take `OptionalTenantDbDep`: an `X-Spec-Key`, when one is presented, pins the request to that tenant; a missing or unrecognised key is ignored rather than rejected, so these routes stay open exactly as before. This narrows what a request can see, it does not widen it, and it is not authentication — a caller with no key still gets a 200. `ontology` is untouched (`entity_types` is global, not per-tenant); `clients` and `admin` are cross-tenant by nature. Covered by `tests/test_projections_tenant_binding.py`, including the guard that these routes must never start returning 401.
- Slice 3 PR 3c: `consent_receipts` (P0 foundation table, RLS since alembic `9a07306c5434`) now has its first HTTP surface — `POST/GET /api/consent/receipts`, `POST /api/consent/receipts/{id}/revoke`, per-org `X-Spec-Key`, same RLS isolation as work_units. `purge_expired_consents()` (daily sweep, triggered manually today via `POST /api/admin/consent/purge`) tombstones `status=active` receipts past `expires_at`. **This reports 0 purged in any real environment today** — no caller writes a consent receipt from an actual interview/transcript flow yet (`genome_import.py` still hardcodes `consent_receipt_id=None`); the only way to see `purged: 1` is the test suite's direct-DB backdated fixture. The job and its RLS-bypassing cross-tenant scan are proven correct; the upstream wiring that would make it matter in production (genome import creating a real receipt at interview time) is not built.
- 15 No-Cracks Gates: #15 (RLS) is fully live. ~6 are partially live. ~8 are specified-only. GQS ≥ 90 means the quality gate passed — it does not mean 15/15 gates passed.
- **Scout Elevated V2 PR1** (`scout_interview_sessions` / `scout_captured_units`, `routers/scout.py`): a founder/SME interview session, a live-editable Work Capture Grid, and a Genome Strength completeness meter are real — tenant-scoped via `X-Spec-Key`/RLS, browser-tested end to end (session create → 401 → key banner → retry → add row → live meter update → inline edit → persisted). None of the "adaptive AI follow-up" or "words become structure live" behavior the design doc describes runs on an LLM — `LLM_PROVIDER=none` in this environment. `DiscoveryPartner`'s question bank is a static, hand-written list per track (founder/SME), not AI-generated or AI-adaptive; its voice input is the browser's own `SpeechRecognition` API (dictation only, works with no backend LLM, degrades to typing where unsupported) — it is not an AI interviewer. Completeness is computed from structured fields the interviewer/interviewee types or dictates into the grid directly; nothing extracts structure from free-text notes automatically. The design doc's `org_id` naming was corrected to `client_id` throughout (this repo's actual tenant column — see `SCOUT_OPERATING_PLAYBOOK.md` §A.3, "never introduce an `orgs` table").
- **Scout Elevated V2 PR2-4** (all 5 elevations, `services/scout_timeline.py` / `scout_contradictions.py` / `scout_pain.py` / `scout_story.py` / `scout_future.py` / `scout_genome.py`): all real, all browser-verified (see `docs/API.md`'s elevation-by-elevation table for the mechanism each one actually uses). None call an LLM. Two are deterministic arithmetic/scheduling (Time-Travel Replay, Pain Heatmap's automation-potential share), one is a plain text diff with a fixed confidence (Contradiction Resolver), one is a sentence-splitter with a guardrail, not extraction (Story to Structure), and one (Future Preview's "Generate V8 Work Units") is a best-effort mapping onto the real 18-attribute contract via the **existing** `genome_import.import_genome` — several attributes the capture grid never asks for (`trigger`, `actor_constraints`, `acceptance_criteria`, `evidence_required`, `failure_semantics`) get one honest literal placeholder rather than fabricated content, so a thin session's genome correctly scores low and gets blocked by the same GQS gate every other import goes through (verified: a 1-unit session scores GQS 30, `accepted: false` — not silently waved through for being Scout-sourced). Future Preview's UI intentionally has no confetti animation, unlike the design doc's spec — a deliberate call to match the app's existing minimal-motion style, stated in the UI copy itself rather than silently dropped. `scout_pain_signals` (the design doc's optional table) was not created — pain is derived on read from `scout_captured_units`, no new table needed.

- **Stitching Scout to V8 delivery** (`frontend/src/pages/Genome.tsx`, new route `/genome/:versionId`): the missing other half of the demo loop — a real UI over the existing, already-tested `genome.py` router (GQS header, a business-object list, each object's work-unit table, full 18-attribute detail per unit, ratify whole-version and ratify-one-business-object, Automation Index tab with bottleneck view and work-graph edge counts). Nothing new on the backend; this page calls endpoints that were already live and tested. `FuturePreview.tsx`'s "Generate V8 Work Units" result now links to `/genome/{version_id}` for both outcomes, gate-passed and gate-failed, closing the Scout → Genome loop visually. **Important, browser-verified finding, not a bug:** the GQS formula (`gqs.py`, frozen) weights `Observed%` at 40% of the score, and every Scout-sourced work unit is honestly labeled `provenance.source_type: "declared"` (interview-derived, not document-derived) — so `Observed%` is structurally 0 for any Scout-only genome, capping its GQS well under the 90 gate. Verified live: an 8-unit, 100%-complete Scout session generates genome v307 at GQS 30.0, "gates passed: none" — and because `import_genome` only persists `WorkUnit` rows on gate pass, that version's business-object list and Automation Index are both correctly empty; the new Genome page renders that empty state honestly rather than erroring or fabricating rows. A genome imported from real observed data (the existing `Private-Genome-MVP-HR-Ops-FIXED.json` fixture) passes the same page's full loop end to end — GQS 94.3 at the time, ratify whole version, ratify one business object, drill from the business-object list down to one unit's full 18-attribute detail, Automation Index with real bottleneck/hours data — verified live at version v306. Nobody worked around the gate to make the Scout path "look" done; the demo narrative is: Scout gets you a structured, honestly-labeled draft, and V8's own quality gate is what tells you it still needs document-backed verification before it ratifies.

  **The fixture's score is now 92.86, not 94.3.** The file used to declare four work units with mutually contradictory `dependencies[]` (A before B *and* B before A for the same pair — e.g. `WU-OFF-02B` and `WU-OFF-03` each named the other), which `services/genome_import.py` faithfully turned into two opposing `sequence` WorkEdge rows per pair and the Work Graph page rendered as an unreadable tangled cluster, not the process order those edges are supposed to encode. Fixed by removing the backwards half of each pair — the kept direction is the one each unit's own `current_condition`/`desired_condition`/`trigger` already states, not invented. One casualty: `WU-OFF-02B` is a genuine chain root (its precondition comes from an external trigger, not another work unit in this genome) and lost its only, backwards dependency, leaving `dependencies: []`. `gqs.py` (frozen — not touched by this fix) treats an empty `dependencies[]` as a missing attribute regardless of whether the unit is a legitimate root, which costs 1/14 of the completeness component: 94.29 → 92.86, still clear of the 90 gate. Sequence edges on this fixture: 13 → 9; total edges 20 → 16 (`shared_object` edges are unaffected).

- **Demo-readiness pass** (bugs found by running the demo end to end, not by the test suite — each was reachable in the documented demo path):
  - `POST /api/genome/import` rejected a re-import with a bare 500. `work_units` is UNIQUE on `(client_id, code)` and nothing checked it before the insert loop, so a colliding id blew up mid-loop on a raw `UniqueViolation` — and left the rows written before the collision behind, attached to a version row still claiming the full count, which then collided with every retry. Now a pre-pass violation (`work_unit_id_already_exists`, `duplicate_work_unit_id`) like file provenance already was, and the write phase is one rollback boundary. Covered by `tests/test_genome_import_conflicts.py`, verified to fail against the previous code.
  - `samples/Private-Genome-MVP-HR-Ops-FIXED.json` and the Client A HR seed both define `WU-OFF-03` and `WU-OFF-04`, so the sample could never be imported into Client A. They are two genomes for two employers; `POST /api/demo/bootstrap` now provisions a separate **Sample Genome Co** tenant for the sample rather than reconciling the ids.
  - `POST /api/demo/bootstrap` (new, gated by `settings.demo_bootstrap_enabled`) seeds the demo **and mints first API keys**, returning each plaintext once. `routers/org.py` could only ROTATE a key you already held, so there was no way to obtain a first key through the app — standing up a demo required a hand-written `org_api_keys` INSERT with a manually computed sha256. It hands out a credential over an unauthenticated request, so it must be disabled outside a throwaway local database.
  - `GET /api/genome/versions` (new): every other genome route needs a `version_id` the caller had to already know, so an imported genome was unreachable from the UI. Backs the new `/genome` index page; Genome also had no nav entry at all.
  - `GET /api/org/whoami` (new): nothing exposed which tenant a key belonged to. The company switcher defaulted to Catalog while the key was Client A's, so every tenant-scoped read was correctly filtered to nothing and the UI rendered a screen of zeros with no explanation. The switcher now defaults to the key's own company and says so when they diverge.
  - `GET /api/clients/` counted work units per client on the RLS-bound session, so every count came back 0 — the switcher read "Client A (0)" on a fully seeded tenant. Enumerating tenants is cross-tenant by definition and now uses the system session.
  - `useApi` never sent `X-Spec-Key`, so Overview, Discovery and Economics all 401'd on `/work-units/` (moved to per-org auth in Slice 3 PR 3a) and silently rendered 0.
  - `/demo/prepare` called `SystemSessionLocal()` directly, escaping the FastAPI dependency override — its test was asserting against the developer's real Postgres rather than its own fixture. The cross-tenant session is now an injectable `get_system_db` that `conftest` overrides.
  - `ApiKeyBanner`'s "Save & retry" stored the key and hid the banner **without retrying**, leaving the operator on an unchanged form with nothing to indicate progress. It now awaits a real retry.
  - `routers/org.py::rotate_key` refreshed a row after `commit()` without re-applying the tenant binding — the documented RLS/connection-pool bug this repo already fixes with `_rebind_tenant` in `scout.py`.
  - UI: the stylesheet was reworked around a real type scale, three line weights and a restrained elevation set; nav is grouped rather than eleven equal-weight links; empty and loading states are explicit everywhere they were blank or a bare "Loading…". No behaviour claims changed — a gate-blocked genome now *says* it was gate-blocked and that no work units were written, instead of rendering an unexplained empty page.

- **Story to Structure now really extracts — when a model is configured.** This supersedes the "None call an LLM" claim in the PR2-4 row above, which was true when written. With `LLM_PROVIDER=anthropic` and a key, `services/scout_story.py` calls Claude (`services/llm.py`, official `anthropic` SDK, default `claude-opus-5`) and returns candidate work units with the grid's structured fields filled in. Three things stay true regardless:
  - **The verbatim guarantee is enforced, not trusted.** Every span the model returns is checked against the transcript and *discarded* if it is not a literal substring. A model that paraphrases cannot put words in the interviewee's mouth. Covered by `tests/test_scout_story_llm.py`, including the paraphrase case.
  - **The structured fields are labelled as the model's reading, not quotes**, and a field the transcript does not cover comes back empty and renders as "not stated" — the UI never fills a gap with something plausible.
  - **With no model configured (still the default) nothing changed**: the deterministic sentence-splitter runs and says so, naming `LLM_PROVIDER=none`. A configured-but-failing model falls back to the same splitter and reports the failure rather than looking like the deterministic mode.
  `services/discovery.py`'s candidate suggestion moved onto the same shared client; its hand-rolled Anthropic POST is gone, so the codebase no longer has two ways of calling a model. The other four elevations are unchanged and still call no model. **Tests never call one** — `tests/conftest.py` forces `LLM_PROVIDER=none` for the whole suite, because a developer with a key in their `.env` was otherwise turning `pytest` into live, billed network calls.
- **Demo setup is a button, not a runbook.** `components/DemoSetup.tsx` on Overview calls `POST /api/demo/bootstrap`, stores the returned key itself, and signs the browser in — the terminal step and the copy-pasting of two opaque 32-character keys are gone. It holds both demo tenants' keys locally so switching between Client A and Sample Genome Co is one click. It requests fresh keys automatically when this browser is not already holding a usable one, because a key's plaintext exists only at the moment it is minted and a re-run would otherwise leave you signed out. Everything it does is the same endpoint documented in the README; nothing is faked to make the demo smoother.
- **Work Capture Grid sample rows** (`lib/scoutSamples.ts`): eight HR & People Ops rows — the same function as the Client A census, so a Scout session sits in the same domain as the rest of the screen. They are saved through the same `POST /scout/sessions/{id}/units` a typed row uses, so the completeness meter reflects them exactly as it would hand-typed input. Eight is not arbitrary: the meter expects 8 work units, so the full set takes a session to 100% and unlocks Future Preview.

- **Scout Elevated upgrade thread, Points 1 & 2** (three-layer interview model, function-agnostic selector, CHRO blast-radius grid). This supersedes the "founder/SME" naming in the PR1 row above, which was true when written.
  - `InterviewType` grows from two organizational altitudes (`founder`, `sme`) to three: `function_head` (renamed, same meaning — e.g. a CHRO), plus a new middle tier `sub_function_lead` (e.g. Head of TA, Head of People Ops). Existing rows carry the new label automatically via an Alembic `RENAME VALUE`, not a data migration. Contradiction Resolver stays a deliberate 2-way (`function_head` vs `sme`) comparison — `sub_function_lead` sessions are not compared against anything yet, stated in the UI's own copy, not silently dropped.
  - The CHRO blast-radius grid (`services/scout_blast_radius.py`, `/scout/blast-radius`) is a published 44-sub-function/6-cluster HR catalog — copied from a reviewed build instruction, with one correction: its People Operations cluster listed 10 items under a "(9)" label, the only cluster where the two disagreed and the only place the stated 44 total didn't reconcile with its own list; fixed by merging two clearly-overlapping items (Employee Self Service into HR Shared Services & Helpdesk). The instruction's per-item "typical owner" and "expected work units (8-12)" are **not** built — invented per-item numbers with no real source. Owner and priority are fields the CHRO fills in during the session; nothing is pre-suggested.
  - `ScoutBlastRadiusSelection` is a sparse, tenant-scoped table (one row per sub-function actually touched, RLS-isolated like every other scout table) — the 44-item catalog itself is a code constant, identical for every tenant, not per-tenant data.
  - The function-agnostic selector (`pages/BlastRadius.tsx`) shows HR as active/detailed and Finance/Legal/Sales/Operations as "coming soon." **There is no catalog, no data, and no backend route for the non-HR functions** — clicking one renders a static sentence and makes no API call. This is a genuinely open gap, not a placeholder standing in for hidden work.
  - "Start interview" on an in-scope sub-function card navigates to the existing session-create form with the track and owner name pre-filled via router state. It does **not** create a session automatically — the interviewer still presses Start. No cascade, no auto-generated interview queue.
  - Browser-verified end to end via Playwright against the local dev stack: the 44/6 grid renders with correct per-cluster counts, a checkbox/owner/priority edit persists and recomputes the live meter, the Start-interview hand-off lands the right track and name on the create form, and the Finance placeholder shows no data.

- **Offer Desk worked example** (`pages/OfferDesk.tsx`, `lib/offerDeskData.ts`, `/scout/offer-desk`, linked from the blast-radius grid's Offer Management and Onboarding cards). All 11 steps, the handoff map, the exception catalog, and the automation-readiness summary are transcribed verbatim from a real interview-sourced workflow document (Session 1, Rashmi KN, 12 May 2026) — no field, number, name, or policy is invented.
  - **"Run this on the platform"** (Track 1 of the enterprise-readiness roadmap, "Prove it"): creates a real Scout session and captures all 11 steps as real Work Capture Grid rows through the same `POST /scout/sessions/{id}/units` any interviewer uses — so the completeness breakdown that follows is the platform's own computation, not this page's. **Verified live: the session settles at 84% completeness, not 100%**, and stays there honestly — the transcript states a pain point for only 5 of the 11 steps and step 1 has no output field at all in the source, so `Pain & Exceptions` (5/11) and `Inputs / Outputs` (8/11, since that dimension requires both fields non-empty) both come in under 100% by construction, not by omission. "Generate V8 Work Units" stays correctly locked below 100% completeness, the same rule every Scout session faces — this run does not reach genome generation, and the page's own copy says so rather than promising a genome it doesn't produce. `time_minutes` for steps with a stated range (e.g. "5-10 min") uses that range's midpoint; step 1 has no stated minutes for Rashmi's own effort (it's the recruiter's trigger) and is left `null` rather than guessed. Per-step `pain` and `handoffs` values are condensed from that step's own quoted text in the source (its "heaviest," "no mail merge," "≤18 characters," etc.) — five of eleven have no such quote and are left empty, not filled with a plausible-sounding guess.

- **Track 2 of the enterprise-readiness roadmap ("make it safe to run")**: CI, and the hardcoded PII-encryption-key default.
  - `.github/workflows/ci.yml` (new): a Postgres 16 service container (`POSTGRES_DB: wep`, matching every test file's own hardcoded `localhost:5433/wep` DSN — 17 files use that literal string, not `settings`, so the CI database must be named exactly `wep`), `alembic upgrade head`, `alembic check`, then `pytest -q` for the backend; `npm ci && npm run build` (typecheck + build) for the frontend. Runs on push to `main` and on every PR. No CI existed anywhere in this project's history before this.
  - Verified for real, not just written: migrations applied cleanly to a genuinely empty database (a throwaway `wep_ci_check` db, same cluster), `alembic check` reported no drift, and the full 146-test suite passed against a `wep`-named database matching CI's config exactly. One real mistake surfaced by this verification, not by the CI change itself: pointing the app at a differently-named database while several test files' own hardcoded fixtures kept writing to the literal `wep` database left 7 orphaned `test-*` client rows behind (their import failed against the mismatched target before fixture teardown could run) — found and cleaned up directly, not left for the next person to trip over.
  - `pii_encryption_key` (`app/config.py`) has no default now — every environment, local dev included, must set `PII_ENCRYPTION_KEY` explicitly (`openssl rand -hex 32`, see `.env.example`) or the app refuses to start. A harder-to-guess hardcoded value would not have closed this: the actual risk was a default committed to source at all, discoverable by anyone who reads the repo. Making the field required surfaced a real, separate bug: `uvicorn --reload`'s watcher subprocess does not reliably inherit the parent's working directory, so the old relative `env_file=".env"` path resolved inconsistently and crashed only under `--reload` — fixed by anchoring `env_file` to an absolute path derived from `config.py`'s own location rather than the process's CWD. `tests/conftest.py` sets a fixed, clearly-labeled non-secret test value before importing `app.config`, since no test ever decrypts real data.

- **Track 3 of the enterprise-readiness roadmap ("make the spec trustworthy"), first piece: Work Graph cycle detection.** `services/genome_import.py::_detect_dependency_cycles` is a new pre-pass check — same shape as `_validate_file_provenance`/`_validate_work_unit_codes` in the same module — that rejects any genome whose `dependencies[]` form a cycle (self-reference, mutual pair, or a longer ring), before any row is written. GQS's own completeness check (frozen, untouched) only ever validated that a dependency reference *exists*; it never checked that the graph those references form is acyclic. This was a real, previously-reachable gap: `samples/Private-Genome-MVP-HR-Ops-FIXED.json` shipped with exactly this shape once (two work units naming each other as dependencies), fixed by hand before this check existed — see the `92.86, not 94.29` note earlier in this file. Runs on every import, Scout-originated or not, since the gap was never specific to Scout.
  - Fixing this surfaced a real, separate finding, not introduced by this change: three existing test fixtures (`test_genome_import_conflicts.py`, `test_genome_version_numbering.py`, `test_automation_index.py`) had been quietly relying on self-referencing or ring-shaped dependencies purely to satisfy GQS's non-empty-dependencies completeness rule — a hack the new check correctly now rejects. Fixed the same way the real sample genome's own cycle was fixed: keep one direction, let the other end become a legitimate root with a placeholder dependency (a token that isn't `WU-`-prefixed, so it satisfies completeness without tripping GQS's `orphan_dependency` check or being treated as a graph edge). `test_automation_index.py`'s synthetic fixture went from two 4-edge rings plus a 2-edge mutual pair (10 sequence edges) to three non-cyclic chains (7) — the edge-count assertion changed to match, not to make the test pass superficially.
  - New coverage: `test_genome_import_cycles.py` (6 tests) — a two-node mutual pair, a four-node ring (with the reported cycle path checked to actually name all four units), a one-node self-reference, a valid chain that must *not* false-positive, a dependency naming a code outside the current payload (out of scope for this check, same exclusion `_write_genome`'s own edge-creation loop already applies), and that a rejected-for-cycles import still leaves an auditable `GenomeVersion` row.

- **Track 3 of the enterprise-readiness roadmap ("make the spec trustworthy"), second piece: consent as an enforced precondition.** Closes the join Part K9 named as the open piece: "`genome_import.py` still hardcodes `consent_receipt_id=None`... nothing currently *requires* a session to cite a receipt at all." `services/genome_import.py::_validate_consent` (new pre-pass check) and a new `enforce_consent: bool = False` parameter on `import_genome`. When `True`, any unit whose `provenance.interview_id` is set must also cite a `consent_receipt_id` that resolves to a real, active `ConsentReceipt` for that tenant, or the batch is rejected (`missing_consent` / `invalid_consent`) before any row is written. `services/scout_genome.py::generate_genome` — the one real, live Scout-session-to-genome path — now sets `provenance.interview_id`/`consent_receipt_id` on every unit it builds and calls `import_genome(..., enforce_consent=True)`. `WorkUnitProvenanceDetail.consent_receipt_id` is now written from the validated value (resolved defensively even when `enforce_consent` was off, so a bad value can't crash the insert against a real foreign key) instead of hardcoded `None`.
  - **Deliberately not wired into the generic JSON-body import** (`POST /api/genome/import`, `enforce_consent` stays `False`). Turning it on there broke the shipped sample genome: `samples/Private-Genome-MVP-HR-Ops-FIXED.json` carries decorative UUID-shaped `interview_id`/`consent_receipt_id` values (e.g. `WU-US-01`'s `"consent_receipt_id": "ce8a8faf-cfde-5a6e-b707-9dd28a4b1fee"`) that predate the real consent-receipt feature (Slice 3 PR 3c) and were never meant to resolve against it — its `notes` field explains the shape: `"Observed - USHR CSV Row 3 + Interview <redacted interviewer>..."`, a document-backed unit whose evidence happens to also cite a corroborating interview. Enforcing consent there would have blocked a well-established, heavily-referenced (README, this file, multiple tests) fixture over data that was never real consent-tracking to begin with — found via `test_rls_genome_isolation_via_http` and two other existing tests failing when this was first wired in broadly, not by inspection.
  - **A real, separate gap this surfaced, stated precisely rather than silently left implied**: nothing in the Scout Interview UI prompts a user to create a consent receipt before starting a session. The API requires and checks one at genome-generation time; no screen guides anyone to `POST /api/consent/receipts` first. A session created exactly as the UI walks you through it today has no receipt and will be blocked the moment "Generate V8 Work Units" is pressed.
  - New coverage: `test_scout_consent_gate.py` (9 tests), exercised directly against `import_genome(enforce_consent=True)` with a constructed, GQS-passing payload rather than through the real Scout HTTP flow — a real Scout-generated genome always sets `dependencies: []` (never captured by the Work Capture Grid), which GQS's own completeness check treats as a missing attribute on every unit, so a real Scout session's genome is *always* blocked by GQS first (an 8-unit, 100%-complete session scores GQS 30, documented above) and the consent gate would never be reached to test in isolation that way. Covers: `enforce_consent=False` ignoring a missing receipt, a missing receipt, an unresolvable receipt id, a non-integer (UUID-shaped) receipt id failing cleanly rather than raising, a revoked receipt, another tenant's receipt (the gate's own lookup is client-scoped independent of RLS), a real active receipt letting the outcome fall back to GQS instead of short-circuiting it, a document-sourced unit with no `interview_id` being unaffected even with `enforce_consent=True`, and audit coverage for a consent-blocked import.

- **Track 3 of the enterprise-readiness roadmap ("make the spec trustworthy"), third piece: a re-audit of the 15 No-Cracks Gates against `Scout-Reference.md`'s original 26 August table.** This supersedes the "#15 fully live, ~6 partial, ~8 specified-only" line earlier in this file, which was true when written. Current count: **5 of 15 are live** (1 — founder/function_head why captured; 4 — bulk attachments; 8 — Work Graph orphan+cycle detection, closed this same track; 12 — partial ratification; 15 — RLS, regression-tested), **2 more are substantially built but don't match the original spec's exact letter** (3 — SME transcripts + consent are each real and now joined for the live Scout path, but nothing enforces a minimum *count* of transcripts; 14 — PII lifecycle is real and consent is now gate-enforced for Scout, but not for the generic JSON-body import, deliberately), and **8 remain not built**. Of those eight, three (5, 7, 13) are unchanged, genuinely partial gaps already named precisely elsewhere (schema permissiveness, caller-supplied kappa, FK-vs-auto-create). The other five (2, 6, 9, 10, 11) are not just unbuilt but not *attempted* here, because each needs a real product decision before it's buildable at all: a Function Pack SDK's question-bank format (2); what heuristic legitimately decides a Work Unit names more than one business object or authority, the F1 split rule (6); what a "terminal state" means per business-object type (9); what should count as a conformance gap during import specifically, distinct from the existing `/discovery/gaps/scan` path (10); and what "Playback 1 before Playback 2" maps to given the current system has one `GenomeVersionType`, not two (11). A plausible-looking heuristic invented to make one of these look closed would be worse than leaving it named as open — none were built speculatively to move this count. See `docs/Work-Engineering-V8.md` Part K10 for the full per-gate table.

- **Gate 11 revisited, then explicitly not built as a gate.** The line above (Track 3, third piece) said gate 11 wasn't attempted because "the current system has one `GenomeVersionType`, not two" — that turned out to be wrong: the enum already has `inferred`/`detailed`/`ratified`, and every write path (`services/genome_import.py::import_genome`) simply hardcoded `version_type=GenomeVersionType.detailed`, so nothing had ever created an `inferred` row. Revisiting it surfaced that the gate as originally worded — "a client's first `GenomeVersion` must be `inferred`; a `detailed` version cannot be created unless an `inferred` version already exists for that client" — is a capture-*sequence* rule (Scout's interview-then-docs order), not a V8 quality gate; V8's D1 requires both provenance arms without ordering them, and a customer importing from an SOP pack with no prior Scout sitting is a legitimate, intended path (Sample Genome Co is exactly that, on purpose). Enforcing the literal predecessor check would have blocked Sample Genome Co's and the Offer Desk import's very first import, since neither ever produced an `inferred` row. **Decision: label only, no order enforced.** `import_genome` now takes a `version_type` parameter (default `detailed`, unchanged for every existing caller); `services/scout_genome.py::generate_genome` — the one path whose output is genuinely interview-shaped and incomplete-by-contract (`NOT_CAPTURED` placeholders, declared-only provenance) — passes `version_type=GenomeVersionType.inferred`. No predecessor check exists or is planned under this name; nothing about Client A or Sample Genome Co changed. `version_type=GenomeVersionType.ratified` is still never set by anything — "ratified" as a state is `GenomeVersion.ratified`, a boolean flipped in `routers/genome.py`, a separate mechanism from this enum's third value. Do not read a passing GQS score, a `detailed` label, or an absence of an `inferred` predecessor as evidence of anything about import order — none of it is checked.

- **The hosted pitch instance's "Set up the demo" 500'd on Render — root-caused against the live database, not guessed.** `POST /api/demo/bootstrap?new_keys=true` returned `psycopg2.errors.InsufficientPrivilege: new row violates row-level security policy for table "work_units"` (confirmed via Render's own log API on commit `f219980`, 2026-09-05 17:19 UTC). Root cause, confirmed by querying `pg_roles` on the live Render Postgres directly: the managed database's owning role (`wep`) has `rolsuper=false` and `rolbypassrls=false`. Every managed Postgres provider withholds both from the app-facing owner role — this was never Render-specific, and was never going to survive first contact with any real host. `db.py`'s `get_system_db()` and `routers/admin.py`'s cross-tenant endpoints (`demo/bootstrap`, `demo/prepare`, `admin/consent/purge`) were always designed to bypass RLS by connecting as this "superuser" — a design that only ever worked locally because docker-compose's `wep` happens to *also* be a genuine Postgres superuser (bootstrapped by the stock `postgres:16-alpine` image), which bypasses `FORCE ROW LEVEL SECURITY` unconditionally. **Fix, verified against a real Postgres 16 instance with Render's exact role attributes reproduced locally (`ALTER ROLE wep NOSUPERUSER`), not just read as plausible:** an explicit, portable session flag, `app.system_bypass`, that `get_system_db()` sets on every system session and that alembic `68c3926e1143` ORs into every existing `tenant_isolation` RLS policy (found by introspecting `pg_policies` for every table literally named `tenant_isolation` — 25 tables, five of them added by *later* migrations that the original 9a07306c5434 migration's hand-maintained table list never mentioned, proof a hand-maintained list drifts). Reproduced the exact reported traceback locally by stripping `wep`'s superuser bit to match Render's `pg_roles` row exactly, confirmed `bootstrap_demo()` then succeeds once the fix is applied, and ran the full 165-test suite clean both under those Render-matching conditions and under CI's actual conditions (a real Postgres superuser, same as docker-compose) — RLS/tenant-isolation tests included, so this does not weaken isolation for ordinary per-request sessions, which never set the new GUC.
  - **A second, related bug the same investigation surfaced**: `main.py`'s startup `lifespan` hook calls `bootstrap_tenants()` (clones catalog Work Units across clients — itself a legitimate cross-tenant write) via a bare `SystemSessionLocal()`, not the `get_system_db()` dependency, so it never got the new GUC either. Reproduced and confirmed fixed the same way: under Render's exact non-superuser conditions, `/api/health`'s `db_ready` field flips from what would have been a silent `false` (the failure is caught by a broad `except Exception`, so it never surfaces as an error) to `true` once `main.py` also sets `app.system_bypass` before calling `bootstrap_tenants`.
  - **What is still a known, narrow gap, not silently left implied**: several test files' own fixture helpers (e.g. `tests/test_scout_sessions.py::_cleanup`) open a raw `SetupSession` directly against `SYSTEM_DATABASE_URL` for setup/teardown, bypassing `get_system_db()` and its GUC — this only works today because CI's own Postgres service container, like docker-compose, hands out a genuine superuser. It has no bearing on the deployed app (production code never uses these fixtures), so it wasn't in scope for this fix, but a test run against a database where the connecting role isn't superuser would need the same GUC added to those fixtures too.
  - **A separate, real issue on the *same* public-URL concern, found while reading `DemoSetup.tsx`**: "Set up the demo" auto-detected `new_keys=true` whenever *this browser* held no stored key — correct for one developer's laptop, but on a shared public pitch instance every new visitor's browser starts with empty `localStorage`, so every single "Set up the demo" click silently retired the *previous* visitor's Client A key, signing them out with no warning. Fixed: only the explicit "Issue fresh keys" button may pass `new_keys=true` now; a browser that finds no key for a tenant that already exists is told so in plain language instead of silently rotating the credential out from under whoever was using it. A fresh local dev database is unaffected, since `new_keys=false` still mints a real key on a tenant's true first bootstrap.
  - Also fixed alongside, lower-risk: the sidebar's internal `"V9 on V8"` label (meaningless outside this repo's own commit history) is now just `"Specification layer"`; `ALLOWED_ORIGINS` is now set on the Render API service defensively (requests from the static frontend arrive same-origin through its own `/api/*` rewrite already, so this was not the actual cause of the 500, but a direct browser call against `work-engineering-api.onrender.com` — e.g. `/docs` — is genuinely cross-origin and needs it).

- **Spec deny can now be cleared with real evidence, inline, on the same screen.** `pages/OfferDeskSpecDeny.tsx` previously only ever asked `POST /spec/check` with `evidence_ref: ""` — always denied, by design, with no way to see the other side. It now also uploads a real file (a small CSV, sample checklist content) through the actual `POST /files/upload` endpoint — server-computed sha256, a real `UploadedFile` row, the same endpoint `offer-desk-inputs/` used — and re-runs the identical check with that hash as `evidence_ref`, rendering both outcomes stacked rather than replacing the deny. `services/spec.py::enforce`'s evidence check only ever asked "is `evidence_ref` a non-empty string" — confirmed by reading it, not assumed — so the fix is a real upload feeding a real check, not a shortcut. Verified end to end via `TestClient` before shipping: bootstrap → denied (empty ref) → upload → allowed (real sha256), on the exact contracted unit (`WU-ONB-01`) the walk already used.
  - **Deliberately not built the same way everywhere** — the three deny/stop screens in this walk aren't the same shape, and a uniform "clear with evidence" button would misrepresent two of them: **Save talk-only / the GQS gate** is blocked because most of a genome is `declared` not `observed`, which one file can't fix (that's what `offer-desk-inputs/`'s whole seven-file pack was for) — that screen now names this explicitly instead of offering a button that couldn't deliver, with a real evidence-pack view named as the next build. **Dual employment** (`OfferDeskDocumentCheck.tsx`) is a policy stop, not an evidence gap — V8's own design intent is "appetite does not lift it" — so it gets an explanation of why it has no clear button, not one added anyway. Only the Spec-deny case is a genuine one-file, one-unit gap, which is why it's the only one that got a real "clear" action.

- **"What if the evidence existed?" (`pages/OfferDeskEvidencePack.tsx`, new) — the Save-talk-only gate's "what would clear this" now has a real, live answer, not just a citation.** Uploads all 7 `offer-desk-inputs/` files for real through `POST /files/upload` (server-computed sha256 each time — not the batch's originally recorded hashes, though they match byte-for-byte, confirmed), rewrites the embedded 11-unit genome's `provenance.file_id`/`hash_sha256` to point at *this* tenant's fresh uploads (the original batch's file ids meant nothing outside the throwaway tenant that first proved this), then imports it through the same `POST /genome/import` and the same GQS>=90 gate every genome faces. Verified end to end via `TestClient` before shipping — not assumed to still work because it worked once during `offer-desk-inputs/`'s own construction: reproduces the exact **92.73/90, accepted, 11 Work Units** result live.
  - **Deliberately a separate genome, on a separate screen — never a mutation of Rashmi's real talk-only result.** Save talk-only's own result (0 saved, GQS ~30, declared) is untouched by this page; the two are linked to, not merged. The disclaimer banner and the page copy both say `offer-desk-inputs/` is fabricated test evidence before anything else on the page.
  - The 2 units the original batch couldn't back with any log (`WU-OD-07` welcome mail, `WU-OD-11` candidate drop-out) stay `declared` here too — nothing upgrades a unit that wasn't genuinely re-uploaded.

- **Track 4 (per-user login) got its first, decision-independent brick: a `users` table, schema only.** `models/security.py::AppUser`/`AppUserRole`, alembic `efc855b7d06a`: `client_id` FK, `external_id` (deliberately provider-agnostic, not `workos_user_id` — doesn't lock in a choice `docs/ROADMAP-DECISIONS.md` Track 4 says is still pending), `role` (admin/editor/viewer), RLS from creation with the `app.system_bypass` clause already in its `tenant_isolation` policy (no follow-up migration needed the way the *original* RLS migration needed one, five months and five tables later). **Nothing reads or writes this table.** No router, no session, no WorkOS/Clerk/Auth0 call, no login. Real per-user auth still does not exist — only per-org keys. Built this far, and no further, because the column shapes don't depend on which of the three still-open decisions (provider, 3 roles vs. 4, whether org keys stay live) gets made; building past this point would mean guessing at one of those on the roadmap's behalf.
  - Caught and fixed before it shipped, not after: a copy/paste edit first landed with `Ratification`'s own trailing `client`/`version`/`work_unit` relationships accidentally re-parented onto `AppUser` (an incomplete `Read` before the edit missed them) — surfaced immediately as `NoForeignKeysError: ... relationship AppUser.version` across 87 failing tests, fixed by putting the three lines back on `Ratification` where they belonged, confirmed by rerunning the full suite clean (165/165) on a fresh database before considering this done.

- **The 12-minute Offer Desk walk now works for a stranger with no key, not just a signed-in colleague.** Every V9 route (Home → Enterprise → HR → HR operations → Offer Desk → three seats → Playback → Spreadsheet → Save talk-only → How we cut it → Gap → Document check → Hours → Spec deny → Evidence pack → Sitting record) renders fully in guest mode, carrying `"Looking only — nothing is saved."` The three action screens (Save talk-only, Spec deny, evidence-pack import) don't attempt their live write for a guest at all — no 401 noise, no crash — showing a preview/explanation and a sign-in prompt instead, which is a stronger claim than "denied": nothing is even attempted without a key. `useIsGuest()` (`lib/guestMode.ts`) is `keyClientId === null` from the same `POST /org/whoami` resolution `CompanyProvider` already does, so a stranger and a colleague whose key has quietly expired see the identical honest state, not a crash for one and a banner for the other.
  - Home's own front door flipped: "Look — no key needed" (linking straight to the walk) is now the primary action; "Set up the demo" is a collapsed disclosure underneath, for whoever will actually save something.
  - Two pieces of V8-walk chrome were leaking onto V9 routes and are now suppressed there: `GuidedTour`'s floating "Step X of Y" widget (its own steps target `/`, `/work-units`, `/discovery` — the pre-V9 walk) and `ProgressTracker`'s "1 of 7" pill bar (which used to treat Home, `/`, as its own step 1 "Overview," a doubly wrong reading now that Home is V9's front door). Both still render correctly on the pages they actually describe (`/overview`, `/work-units`, etc.) — this isn't a removal, it's making them route-aware.
  - **Verified against the real running app, not just typechecked**: a Playwright pass with a brand-new browser context (guaranteed empty `localStorage`) walked all 18 screens — every one returned 200, rendered the app shell, showed the guest banner on every Offer Desk route, and **never once had a real key appear in `localStorage`**. A second pass on the SAME running app confirmed the keyed colleague walk is unaffected: "Set up the demo" still mints keys and imports the sample genome, and Spec deny's upload → allow still flips for real.
  - Backend untouched: unauthenticated persist/Spec-check/genome-import already 401'd before this slice (`require_org_api_key`) and still do — the guest experience is entirely about the frontend not attempting those calls in the first place, not a new backend permission. `render.yaml`'s `ALLOWED_ORIGINS` already covered the CORS case this slice's brief asked about.

- **Track 1 slice 1.1 — Gate 10 (declared vs. observed at import) is now built, on the specific shape `docs/ROADMAP-DECISIONS.md` recommended: a Work Unit whose `provenance.source_type` is `declared` and has no corroborating `observed` Work Unit gets a `ConformanceGap` (`kind=undeclared`, new `severity` column, `P2` default) at import time — a warning, never a reject; the import's `accepted`/`work_unit_count`/GQS outcome is unchanged either way, with a new `conformance_gaps_flagged` count on the response.** `services/genome_import.py::_flag_undeclared_gaps`, reusing the pre-existing `conformance_gaps` table (`models/discovery.py`) — no second gap table.
  - **"Sharing its code," read literally, turned out to be impossible to build**: `work_units` is `UNIQUE(client_id, code)`, so a newly-imported Work Unit's code can, by construction, never already belong to another row for that client — that collision is a hard reject (`_validate_work_unit_codes`) long before Gate 10 would run. The corroboration this actually checks is **business object**: does this client have *any* observed-provenance Work Unit — from this import or an earlier one — for the same `business_object_type_id`. This is a real design decision made in this slice, not something the roadmap doc pinned down to the byte; stated here rather than left implicit.
  - **A genome that is 100% `declared` cannot pass GQS at all, so it never reaches Gate 10.** `services/gqs.py` weights Observed% at 40% of the score; a payload with zero observed units caps at 60 regardless of completeness, violations, or kappa — the same ceiling `test_scout_consent_gate.py` already documented for Scout's own capture. Every test proving Gate 10 fires or doesn't therefore carries real observed filler content in the same payload (an 80%-observed / 20%-other ratio clears the gate on its own) — Gate 10 is then read off the declared unit(s) in that batch. This is the correct interaction, not a workaround: it means Gate 10 only ever gets a chance to flag a *minority* of declared content riding in on an otherwise-evidenced import, which is what "declared ≠ observed" is supposed to catch — not a wholesale declared-only genome, which GQS already stops on its own.
  - **Scout's `generate-genome` is unaffected and still blocked by GQS first**, same as before this slice (`test_scout_generate_genome_still_blocked_by_gqs_and_never_persists_observed`) — Scout always sets `provenance.source_type: declared` and empty `dependencies`, so it never clears GQS's own gate to reach Gate 10 at all, and nothing in this slice gives it a way to claim `observed` to dodge it (`test_scout_payload_is_always_declared_never_observed`).
  - Covered by `tests/test_gate10_conformance_gap.py` (8 tests): declared-only-for-its-business-object creates a gap; an observed twin in the same import suppresses it; a twin for a *different* business object does not count; a twin already on record from an earlier `GenomeVersion` still counts; `inferred`/`designed` provenance is out of Gate 10's scope (the frozen contract names `declared` specifically); the check is client-scoped; and the two Scout non-regression tests above.
  - Not built, deliberately: Gate 11 predecessor ordering (out of program), and any matching against real trace/`DiscoveryCandidate` data (`/discovery/gaps/scan`'s job, Track 6, not started) — Gate 10 only ever has the declared side of a genome import to look at, per the roadmap doc's own framing of how it differs from `/discovery/gaps/scan`.

- **Track 1 slice 1.4 — HR Function Pack v0: a real question bank + a real, standalone CLI that fails on missing coverage.** `packs/hr/question_bank.yaml` and `./scout pack validate <pack_dir>` (dispatching to `backend/app/services/pack_validate.py`) exist and do exactly one thing: fail with a non-zero exit unless every one of the 18 Work Unit attributes (`gqs.py::REQUIRED_18`) has at least one interview question mapped to it. Proven both ways — `packs/hr` passes (18/18, exit 0) and a pack with one attribute's mapping stripped fails (exit 1, names the missing attribute) — as pytest cases (`test_pack_validate.py`) and by hand.
  - **Source = the existing hand-written bank, not invented content.** Every question's `id`, `text`, and `why` in `question_bank.yaml` is copied verbatim from `frontend/src/components/scout/DiscoveryPartner.tsx`'s `QUESTION_BANK` — `test_hr_pack_questions_are_not_invented` asserts each one is a literal substring of that file. Only the `attributes` mapping onto the 18 is new work product from this slice. That source bank has 13 questions across three tracks (function_head/sub_function_lead/sme) for 18 attributes, so several questions are honestly mapped to more than one attribute (e.g. "walk me through what happens today, step by step" covers `trigger`, `current_condition`, and `desired_condition` at once) rather than one question being split or invented per attribute.
  - **Deliberately not wired to anything else.** `scout pack validate` is not called from GQS, from Scout `generate-genome`, from genome import, or from any CI step — it is a standalone check run by hand against a pack directory. GQS's formula and weights are untouched; Gate 11 order is still not enforced; no login/WorkOS path is touched.
  - **Only the one file this slice asked for.** `Scout-Reference.md`'s "Function Pack SDK — 5 Files + CLI" target design also names `pack.json`, `business_objects.yaml`, `regulatory_stubs.yaml`, `parser_hints.yaml`, and `verdict_anchors.yaml` — none of those exist. This slice built the question bank and its coverage validator only; the rest of that design remains not built.
  - **No Finance or Legal pack.** `packs/` holds `hr/` only. A second function pack is Track 5, gated on a founder-reviewed catalog (`ROADMAP-DECISIONS.md`), not attempted here.

- **Track 2 slice 2.1 — a consent screen sits between Scout capture and `generate-genome`, and the API now refuses outright (4xx) when one is missing, instead of quietly returning 200 with `accepted: false`.** `ConsentGate.tsx` renders inside the existing Future Preview elevation (`FuturePreview.tsx`) whenever `session.consent_receipt_id` is null: pick a real, active receipt already on file (`GET /api/consent/receipts`) or create one (`POST /api/consent/receipts`) in plain words — "who agreed," "what we told them it's for," "keep this record for (days)," a checkbox instead of a legal-consent wall of text — then attach it via a new `PATCH /api/scout/sessions/{id}/consent-receipt` endpoint. `routers/scout.py::generate_genome` now checks `session.consent_receipt_id` before calling the import pipeline at all and raises `400` if it's missing — a real HTTP failure, not a body flag, proven by `test_generate_genome_without_consent_receipt_4xx` (asserts the 4xx *and* that no `GenomeVersion` row gets written, unlike a GQS-blocked attempt).
  - **The deeper gate is untouched.** `genome_import.py::_validate_consent` (missing/invalid/revoked/cross-tenant receipt, all pre-existing from Track 3 PR 3c) still runs exactly as before once a receipt is attached — this slice only adds a fast, honest door in front of the one case (no receipt at all) a UI screen can prevent outright. A revoked or wrong-tenant receipt id still falls through to that existing check and comes back as `accepted: false` at `200`, same as it always has (`test_scout_consent_gate.py`, unmodified).
  - **GQS still blocks Scout-only genomes correctly, and this doesn't paper over that.** `test_attach_consent_receipt_then_generate_runs_existing_path` attaches a real receipt to a thin session and confirms `generate-genome` reaches the real pipeline (`200`, a `gqs` score present) — whatever GQS decides for a Scout-mapped genome's honest placeholders is exactly what it decided before this slice.
  - **One pre-existing test's setup no longer matched the new contract, so it was fixed, not weakened**: `test_generate_genome_uses_real_import_pipeline_and_gate` created a session with no consent receipt and asserted `200` — true before this slice only because a thin Scout genome fails GQS *before* the (previously unreachable via HTTP) consent check ever ran. It now attaches a real receipt first, so it still tests what its name says (the GQS gate), rather than accidentally depending on a code path this slice intentionally closes off.
  - **No WorkOS, no Gate 11 ordering, no invented traces.** The consent screen calls only the existing `consent_receipts` table and the two Scout endpoints above; nothing here touches login, a predecessor check, or any Zwayam/HRIS-shaped data.
  - Verified against the real running app: `Prepare Client A HR demo` → new session → captured unit → Future Preview shows the consent screen (not the genome preview) → pick-existing and create-new both work and correctly disappear once a receipt is attached, confirmed with a second session against the first session's saved receipt. `curl`'d directly: no receipt → `400` with a plain-language `detail`; with a receipt, the normal locked/unlocked preview returns. Full backend suite (184 tests) and frontend typecheck/build both pass.

- **Track 1 slice 1.2 — Gate 6 (F1 split rule) is now built, on the specific shape `docs/ROADMAP-DECISIONS.md` recommended: advisory, not blocking.** A Work Unit whose `business_object` contains a multi-object delimiter (comma, slash, "and") OR whose `authority` names more than one distinct approver gets a `ConformanceGap` (`kind=split_recommended`, `severity=P2`) at import time — a warning, never a reject, and the unit is never auto-split. `services/genome_import.py::_flag_split_recommended`, reusing the same pre-existing `conformance_gaps` table Gate 10 already writes to (`models/discovery.py`) — no second warnings table; the response gained a `split_recommended_flagged` count alongside Gate 10's existing `conformance_gaps_flagged`.
  - **`GapKind` gained one new value, `split_recommended`.** Postgres enforces this as a native enum type, so a small additive migration (`a1b2c3d4e5f6`) runs `ALTER TYPE gapkind ADD VALUE IF NOT EXISTS` — same shape as the existing `interviewtype` precedent (`8c8ef364ec46`). Verified for real: applied clean against a live Postgres 16 instance, `alembic check` reports no drift, and the full suite passes against that same database afterward.
  - **The delimiter check is literal, not semantic.** `_split_distinct_parts` splits on `,`, `/`, and the word "and" (case-insensitive, word-boundary so it doesn't fire inside a word like "Andaman"), then dedupes case-insensitively. "Offer, Employee" and "Payroll and Benefits" both flag; "Offer" alone does not. The same function checks both `business_object` and `authority` independently, and either one matching is enough — a unit can carry both reasons in one gap's description if both fire.
  - **Scout's own genome payload is unaffected.** `services/scout_genome.py::build_genome_payload` sets `business_object` to the captured unit's name and `authority` to the single interviewee name — neither carries the three delimiters by construction, so nothing about this slice changes Scout's existing GQS-blocked outcome for a thin session (`test_scout_generate_genome_still_blocked_by_gqs_unchanged_by_gate6`).
  - Covered by `tests/test_gate6_split_recommended.py` (5 tests): a comma-delimited `business_object` warns and the import still succeeds; slash and "and" both trigger the same warning; a multi-approver `authority` warns independently of `business_object`; a single clean object/authority produces no gap; and the Scout GQS-blocked path is unchanged.
  - Not built, deliberately: no hard-block path (the roadmap doc's own recommendation — the heuristic is fuzzy enough that blocking risks false-positive rejections of real imports), no auto-split, no WorkOS, no Gate 11 predecessor ordering.

- **Track 1 slice 1.3 — Gate 9 (BO state machines, closed loop with terminal state) is now built, on the specific shape `docs/ROADMAP-DECISIONS.md` recommended: advisory, not blocking, same reasoning as Gate 6.** At genome import, a business object's state graph is inferred from the `current_condition` -> `desired_condition` transitions of every Work Unit sharing that business object, cumulative across the client's Work Units (not just the units in this import) — same corroboration scope as Gate 10. A business object with >=3 Work Units and no terminal state (a `desired_condition` that is never any other unit's `current_condition` for that BO) gets a `ConformanceGap` (`kind=missing_terminal_state`, `severity=P2`) — a warning, never a reject. `services/genome_import.py::_flag_missing_terminal_state`, reusing the same pre-existing `conformance_gaps` table Gate 6/10 already write to — no second warnings table; the response gained a `missing_terminal_state_flagged` count alongside the existing `conformance_gaps_flagged`/`split_recommended_flagged`.
  - **`GapKind` gained one new value, `missing_terminal_state`.** Same additive-migration shape as Gate 6's `split_recommended` (`b2c3d4e5f6a7`, `ALTER TYPE gapkind ADD VALUE IF NOT EXISTS`). Verified for real: applied clean against a live Postgres 16 instance, `alembic check` reports no drift, and the full suite (194 tests) passes against that same database afterward.
  - **2-unit business objects are exempt, per the roadmap doc's own threshold** — too small a sample for "no terminal state" to mean anything. The gate only applies at >=3 Work Units for a BO.
  - **`entity_types.state_machine` is never written by this slice.** The roadmap doc's Gate 9 recommendation is to eventually *populate* `state_machine` from the inferred graph, but this slice is the warning only — no auto-fix, no state machine written, per the explicit scope of Track 1 slice 1.3.
  - **Scout's own genome payload is unaffected**, same non-regression shape as Gate 6/10: a thin Scout session still fails GQS before Gate 9 (or Gate 6/10) ever runs (`test_scout_generate_genome_still_blocked_by_gqs_unchanged_by_gate9`).
  - Covered by `tests/test_gate9_terminal_state.py` (5 tests): a 3-unit BO whose conditions form a closed cycle (no terminal state) warns and the import still succeeds; the same 3-unit shape with a real sink (a `desired_condition` no unit ever has as `current_condition`) produces no gap; a 2-unit BO with the same closed-cycle shape is exempt; the gap never causes `entity_types.state_machine` to be written; and the Scout GQS-blocked path is unchanged.
  - Not built, deliberately: no hard-block path, no auto-fix/auto-population of `state_machine`, no WorkOS, no Gate 11 predecessor ordering.

- **Track 6 slice 6.1 — a 500 now logs one structured, greppable line; `/api/health` stops lying.** Every unhandled exception (anything that isn't a deliberate `HTTPException` a router raises on purpose) now produces exactly one line — `unhandled_exception request_id=<hex> method=<...> path=<...> exc_type=<...>` — via a single request-id-and-error-handling middleware (`main.py::_install_error_handling`), plus an `X-Request-Id` header/body field on every response, error or not. Deliberately never logs or returns the exception's own message/args: this app's error paths run through SQLAlchemy and consent/PII services, and a raw exception message (a SQLAlchemy statement error, in particular) can carry bound parameter values — the safe default is to log nothing that might be an API key or a consent/PII payload, not to trust every exception type to stay clean. `/api/health` used to return HTTP 200 unconditionally regardless of `db_ready` — meaning Render's own `healthCheckPath`-driven monitoring (`render.yaml`) could never have detected a failed boot even in principle, since it only looks at the status code, never the body. It now returns 503 when the last boot's DB check failed.
  - **A real Starlette gotcha, found by testing, not assumed away.** The first version registered the structured logging as a separate `@app.exception_handler(Exception)` alongside the request-id middleware — this silently never attached the `X-Request-Id` header to an error response, because Starlette's `build_middleware_stack` special-cases any handler registered for the bare `Exception` class (or status 500): it hands that one to the *outermost* `ServerErrorMiddleware`, not `ExceptionMiddleware`, which sits outside every `@app.middleware("http")`. Confirmed empirically (a debug trace showing `call_next` itself re-raising the exception, past the point where the header would have been added) before rewriting it as one middleware with its own try/except around `call_next`, which sidesteps the split entirely. Covered by `tests/test_error_logging.py` (4 tests, including one asserting a secret-marker exception message never reaches the response body *or* the log line).
  - **`/api/health`'s 503 needed a real fix to test, not a shortcut.** `conftest.py`'s `client` fixture sets `app.state.db_ready = True` by hand, but the real `lifespan` always runs on `TestClient.__enter__` and unconditionally recomputes `db_ready` from actual DB reachability — so that manual line has always been inert (harmless before this slice, since `/api/health` ignored `db_ready` for its status code either way; newly relevant now that it doesn't). `test_health.py`'s new unhealthy-path test breaks connectivity for real (monkeypatching `main.engine` to a stub whose `.connect()` raises) rather than trusting a value the real lifespan would just overwrite. Full suite (113 tests, up from 108) run clean against both SQLite and a real local Postgres 16 with Render-matching migrations applied.
  - **Render's native notify path is documented, not re-verified live.** Confirmed via the Render API that both hosted services carry a per-service `notifyOnFail` field (currently `"default"`, inheriting the workspace's own Notification Settings) — the free, no-code path to an email on a failed deploy. Could not click through Render's own dashboard or docs this session to confirm the exact current wording/navigation: `render.com` is blocked by this session's egress policy the same way `onrender.com` already was (see this file's own "hosted pitch instance" entry and `STATUS.md`'s Ship-ready row for that precedent). Documented as best-known in `docs/STATUS.md`, with the honest caveat attached rather than presented as re-confirmed.
  - **A real incident, checked against history before being written down as new.** This slice's own log-forensics (Render's log API against `work-engineering-api`) turned up a genuine production 500 — `POST /api/demo/bootstrap` failing with `psycopg2.errors.InsufficientPrivilege` on `work_units`, 2026-09-05T17:19Z. Cross-referencing Render's deploy history caught, before this went into any doc, that this is the *same* incident this file's own "hosted pitch instance's Set up the demo 500'd" entry (above) already root-caused and fixed same-day (commit `30bbab2`, ~2 hours after) — not a new, still-open bug; the instance that hit it was still running the pre-fix commit `f219980`. Kept as a real worked example in `docs/OPS.md` rather than discarded, because it's still useful for exactly one thing: it shows nothing in CI would catch that class of bug reappearing (CI's Postgres role is a genuine superuser, unlike Render's managed database's role — the one condition that actually triggered it), which is a real, separate, still-open gap named for whoever next touches test infrastructure.
  - **What this slice deliberately did not build**: no Sentry/PagerDuty/APM, no synthetic uptime prober, no push notification for a single endpoint 500'ing while the process and `/api/health` both stay green (exactly the shape of the incident above) — closing that needs either a paid plan/third-party DSN (a STOP-GATE this slice never needed to open) or a monitoring product this slice was told not to invent. No WorkOS, no Gate 11, no Track 3/4/5 work.

- **PITCH-FACTORY: Track 1's already-merged gates (1.1–1.4, 2.1) become visible on the walk instead of only in this ledger, plus Track 6.2's backup check.** Slices 1.1–1.4 (Gates 10/6/9, the HR Function Pack), 2.1 (consent UI), and 6.1 (alerting) had all merged to `main`, but a colleague clicking through the actual pitch walk had no way to see any of that — the Offer Desk **Gap** screen still showed four static, hand-written rows regardless of whether a real genome existed, and `docs/STATUS.md` still listed the consent screen under "What is not built." This slice does not touch Gate logic, GQS, or any backend rule — every change is either what's *shown*, what's *checked in CI*, or what's *documented*.
  - **`OfferDeskGap.tsx` now reads real `ConformanceGap` rows instead of always rendering the same illustrative table.** Guest (no key) still gets exactly the old four-row educational table, now explicitly labelled "educational walkthrough," not a live query. A signed-in colleague gets a live `GET /discovery/gaps` for their own tenant (`useApi` + `withClient`, the same idiom `Discovery.tsx` already used), filtered client-side to only the three `GapKind` values a genome import can produce (`undeclared`, `split_recommended`, `missing_terminal_state`) — **this filter turned out to be load-bearing, not defensive boilerplate**: querying the real database this slice found Client A already carries 6 `unimplemented`-kind gaps from the unrelated census/SOP path (`services/census.py::gaps_from_sop`, seeded at `/api/demo/bootstrap`) that have nothing to do with this Offer Desk sitting — without the filter, a colleague's first "Set up the demo" would have put six irrelevant SOP findings on the Gap screen before they'd imported anything Offer-Desk-shaped at all. Each surfaced kind gets an everyday-language label with the formal `GapKind` name in brackets plus an `InfoTooltip` i-button (e.g. "Said, but not backed up yet [undeclared]"). Zero real gaps renders a plainly-labelled empty-state card ("a true empty state, not a clean bill of health"), never the fabricated rows dressed up as live data.
  - **Verified live, twice, against a freshly migrated local Postgres — not asserted from reading the code.** A throwaway Playwright script (this repo has never committed Playwright infra; every prior "Playwright-verified" claim in this file was the same kind of ad hoc run, so this one stays out of the repo too) drove a real Chromium against the real dev stack: guest walk (fresh browser context, zero `localStorage`) confirmed Hours still shows both 95 and 61.8, the guest banner text, and that no key is ever written; keyed walk confirmed the Gap page's pre-import state is the true empty state (`SELECT` against `conformance_gaps` at that point showed only Client A's 6 pre-existing `unimplemented` rows, correctly excluded), then imported `offer-desk-inputs/` via the existing **With evidence (sample)** screen (GQS **92.73**, 11 Work Units, confirmed via direct `SELECT` on `genome_versions` — unchanged from the value this repo has cited since that pack was built) and confirmed the Gap page then shows exactly 2 real rows: `WU-OD-07` ("Welcome Communication") and `WU-OD-11` ("Candidate Drop-out Case"), both `kind=undeclared` — the same two units this file's own evidence-pack entry already named as the ones with no corroborating log in that batch. Re-ran end to end against a database dropped and re-migrated from scratch between runs to rule out state left over from an earlier click-through; both runs agreed. One incidental, real finding from the same query, unrelated to this slice's own change and not investigated further here: Sample Genome Co's bootstrap import of `Private-Genome-MVP-HR-Ops-FIXED.json` trips Gate 6 (`split_recommended`) **13 times** — Gate 6 was previously only shown firing on synthetic test fixtures, not observed on the one real shipped genome sample.
  - **`OfferDeskEvidencePack.tsx`'s fabricated-data banner now says "not a real Zwayam, Zoho, or UAN connector" explicitly**, above the fold, next to the pre-existing "not Rashmi's production month" line — the page's body copy already named Zwayam/Zoho/UAN as things this pack is not, but the banner itself, the one thing guaranteed to be read, did not.
  - **`.github/workflows/ci.yml` now runs `python scout pack validate packs/hr` on every push and PR**, before migrations (it needs no database — confirmed by running it standalone, no `DATABASE_URL`/`PII_ENCRYPTION_KEY` required, since `pack_validate.py`'s only import from the app is `gqs.py::REQUIRED_18`, which itself imports nothing beyond `typing`). Previously this was Track 1 slice 1.4's own CLI, correct but only ever run by hand — a pack losing coverage on a future PR would not have failed anything. Still exactly one pack (`packs/hr`); this does not add Finance/Legal.
  - **Track 6.2 (Postgres backup): checked for real via the Render API, not enabled, STOP-GATE opened.** `get_postgres` on the live `work-engineering-db` instance returns `plan: free` with no backup or point-in-time-recovery field anywhere on the resource — confirmed alongside the `create_postgres` tool's own schema, which has no such parameter either; backup is a function of plan tier, not a flag. Sharper than "no backup": the same record's `expiresAt` is `2026-10-05T09:42:10Z`, roughly 29 days from this entry — Render's free Postgres is a 30-day instance, not a permanent free tier, so without a decision the *primary copy* is gone on that date, not just its backup history. This slice did not upgrade the plan and did not substitute a `pg_dump` cron job for a real backup product. `docs/OPS.md` §4a has the full finding; `docs/STATUS.md` carries the STOP-GATE line verbatim: **paid Render backup: no — founder decides**, before 2026-10-05.
  - **`docs/STATUS.md` corrected, not just extended.** It previously listed "a UI step that prompts a user to create a consent receipt before a Scout session starts... no screen guides anyone to create one first" under "What is not built" — wrong since Track 2 slice 2.1 shipped `ConsentGate.tsx`, and this file's own ledger already said so; `STATUS.md`'s summary had simply never been updated to match. Fixed, and Gates 6/9/10 plus the HR Function Pack — both previously undocumented anywhere in `STATUS.md`'s own "what is real" summary despite being fully documented in this file — now have bullets there too.
  - **Hosted verification: attempted again, still blocked, said so rather than claimed.** Both `WebFetch` and `curl` against `work-engineering.onrender.com` and against `render.com` itself were re-tried this slice; both tools, both hosts, same result as every prior slice — `EGRESS_BLOCKED` / `connect_rejected (organization policy)`. No hosted claim is made anywhere in this slice's changes. The guest-walk Hours screen (95/61.8) is Playwright-verified locally, twice, and explicitly flagged in `STATUS.md` as needing one real founder click-through against the hosted URL — an agent action cannot substitute for that.
  - **Verified before pushing**: full backend suite, **199 passed, 0 failed, 0 skipped**, against a real local Postgres 16 with the exact `wep`/`wep_app` role split CI uses (not SQLite, so the RLS-dependent tests actually ran rather than self-skipping) — up from the 194 last recorded in this file, reflecting tests added since. `alembic check`: no drift. Frontend: `tsc --noEmit && vite build` clean.
  - **Did not do**: no WorkOS, no Gate 11 ordering, no Finance/Legal pack, no backend rewrite, no new repo, no auto-split of a `split_recommended` unit, no auto-written `state_machine`, no purchased Render plan.

- **Slice `TIGHT-WEDGE` (T1–T6): the six architecture joints named in `docs/BUILD_PROGRAM.md`, one PR, no backend change.** Every line below was verified against the real running app on a freshly migrated local Postgres 16 (the exact `wep`/`wep_app` role split, not SQLite), not asserted from reading the code.
  - **T1 — Document check unit card** (`pages/OfferDeskDocumentCheck.tsx`, `lib/offerDeskWorkRecord.ts`). The card now shows the record explicitly shaped like a Work Unit — current condition, desired condition, acceptance criteria, evidence required, dual-employment stop — each with an i-button, still never persisted (Document check is not, and does not become, a saved `WorkUnit` row). `acceptanceCriteria` is step 2's own `decisionBranches` from `lib/offerDeskData.ts` (real, already-transcribed source text), not a new paraphrase. `desiredConditionCheckability()` is the "uncheckable desired → warn, not reject" rule: checkable iff both `acceptance_criteria` and `evidence_required` are non-empty — the same pairing Spec's own evidence/acceptance checks and VERDICT's Hard Gate 4 already rely on (`ARCHITECTURE.md`, "Spec API"). This record passes today (real, verified: the "Checkable today" banner renders, not the warning) — the function stays real and exported rather than hardcoded true, so a future edit that clears either field would surface a warning here instead of silently going uncheckable. It never hides the card or disables the helper list either way.
  - **T2 — Offer Desk Work Graph** (`pages/OfferDeskWorkGraph.tsx`, new route `/scout/offer-desk/work-graph`). Filters this tenant's whole `/projections/work-graph` down to `WU-OD-*` codes (the evidence-pack genome's own prefix, confirmed to appear nowhere else in this codebase) and to `edge_type=sequence` only — verified as necessary, not decorative: `services/automation_index.py`'s own bottleneck detector persists real `shared_resource` edges into this same `work_edges` table the moment anyone views a genome's Automation Index, and all 11 evidence-pack units share one `authority` string (`"Rashmi KN (Offer Desk SME)"`, confirmed by reading the pack's own JSON), which is exactly what that detector groups on. A guest sees the 11 step names as a plain `<ol>` — confirmed zero `<svg>` elements render on that page for a guest — never a drawn arrow that isn't a computed edge. Before an import, a keyed tenant sees a true empty state, not a fake chain. After importing `offer-desk-inputs/` via **With evidence (sample)**, confirmed live and cross-checked directly against `work_edges`: **11 real work units, 10 real `sequence` edges, zero of any other edge type** — the expected shape for an 11-node linear chain (`WU-OD-01` depends on a non-`WU-` placeholder, so it correctly has no inbound edge; each of `WU-OD-02`..`WU-OD-11` depends on exactly the prior step).
  - **T3 — Verification spec card**, same page. Method is stated as `human_spot_check` with an i-button. Independent checker is named honestly — **"None today,"** with the real reason (Exception 9 in `lib/offerDeskData.ts`'s `OFFER_DESK_EXCEPTIONS`: "Single point of failure... no formal KT") rather than a generic disclaimer, grounded in `Work-Engineering-V8.md` G3's own table (human spot check is only "medium" independence, and only if it's a different person). File hash is optional and real: a **"Upload a sample file & show its hash"** button reuses the existing `POST /files/upload` (same endpoint Spec deny and the evidence pack already use) — confirmed live, a real server-computed sha256 renders (`8f00bc19cf20574b…`, this run). Guest gets a sign-in prompt and no attempted call, same idiom as every other write action on this walk. One explicit line states no promotion ladder exists on this card, and why: Document check has no autonomy level to promote or demote because it is never saved as a Work Unit on this walk.
  - **T4 — Gap: "what this sitting cannot see"** (`pages/OfferDeskGap.tsx`). A new card, shown in both guest and keyed states, names two real, sourced limits: no Zwayam/Zoho connector (nothing in this product reads either system, ever — every label here comes from an interview or an uploaded file), and judgment-blind (step 2's own agent notes: "judgment on employment gaps, dual employment decisions, non-standard documents" — the same Exception 9 single-point-of-failure finding T3 cites). No percentage appears anywhere on this card or page — confirmed by an explicit Playwright assertion that no `N% coverage`-shaped text renders. The existing real-row logic (Gates 10/6/9, filtered to `undeclared`/`split_recommended`/`missing_terminal_state`) is untouched. Re-verified fresh, not assumed unchanged: Client A's own Gap query, direct against `conformance_gaps` scoped to `client_id`, shows exactly the same **2 real rows** this file's PITCH-FACTORY entry already found — `WU-OD-07` and `WU-OD-11`, both `undeclared` — confirming no regression from a database dropped and re-migrated from scratch since that entry was written.
  - **T5 — `docs/INTENT_CONTRACT.md`** (new, documentation only). Freezes the one real path Box 2 (`IntentSource`) already has into 3a: `services/discovery.py::scan_gaps`'s `_intent_matches_unit`, which reads `intent.title` concatenated with `intent.body` and writes `declared_ref=intent.title` verbatim on no match — read directly from that function, not paraphrased. States plainly, as a named finding rather than left implicit: this path and the Offer Desk walk's own Gap screen are disjoint today — the three gate kinds `OfferDeskGap.tsx` shows never cite an `IntentSource`, and the CHRO/HR-Ops "stand-in" quotes on this walk are frontend constants, never written through `POST /discovery/intent`. Refuses, by name, a new identifier or table scheme for intent records (the "no INT-007 tables" instruction) — `IntentSource.id`, a plain integer primary key, is the only identity this freeze recognizes.
  - **T6 — Hours unchanged, 72.2 guarded.** `pages/OfferDeskHours.tsx` is untouched; 95 and 61.8 both still render, confirmed live. `72.2` (the evidence-pack genome's own real Automation Index `total_hours_current`, per `offer-desk-inputs/README.md`) does not appear anywhere on the Offer Desk walk today, confirmed by grep before writing anything — but the evidence-pack screen's own result banner now carries a standing footnote, confirmed to render on a real accepted import, naming it a different calculation from Rashmi's declared/defended numbers and saying not to merge it into either, in case a founder later opens that version's Automation Index from the Genome page and sees it.
  - **Walk reordered to match `docs/BUILD_PROGRAM.md`'s own TIGHT-WEDGE spec**: `lib/offerDeskSeats.ts`'s stepper and every page's own "next" link now read How we cut it → Document check → Work Graph → Gap → Hours (previously Gap preceded Document check). `docs/STATUS.md`'s two walk-order mentions were corrected to match, not left to drift the way this file's own prior entries have twice found `STATUS.md` doing.
  - **Verified before pushing, all real, none assumed**: full backend suite, **199 passed, 0 failed, 0 skipped**, against real local Postgres 16 with the `wep`/`wep_app` role split (unchanged count from the last entry — expected, since this slice touches zero backend files). `alembic check`: no drift. Frontend: `tsc --noEmit && vite build` clean. A throwaway Playwright script (not committed — this repo's own precedent, see the PITCH-FACTORY entry above) drove a real Chromium against the real dev stack end to end: **30/30 checks passed**, covering the guest walk (Hours 95/61.8 + banner, Document check card + checkability + honest verification card, Work Graph's zero-SVG list, Gap's illustrative rows + cannot-see card, and confirmed zero real key ever lands in `localStorage`) and the keyed walk (real demo bootstrap, a real uploaded file hash, Work Graph's true empty state before import and its real 11-unit/10-edge chain after, the evidence pack's 72.2 footnote, and Gap's real-or-empty rows).
  - **Did not do**: no WorkOS, no Gate 11 ordering, no Finance/Legal pack, no backend rewrite, no new migration or endpoint, no promotion ladder, no INT-007 (or any other) new intent identifier scheme, no S1/S2/S3 scenario product, no purchased Render plan. Track 6.2's Postgres backup STOP-GATE is unchanged and still open — the free `work-engineering-db` instance still expires **2026-10-05**; this slice did not buy anything.

- **Slice `TIGHT-3D` (T3d-S + T3b-P): allocation scenarios and field-level provenance on Document check, one PR, no backend change.** Both new backend, `/work-units/` and `/verdict/`, were already fully sufficient — `WorkUnitOut` already carries `provenance` and a server-computed `missing_attributes` list, `VerdictOut` already carries the seven raw property scores, `recommended_level`, `applied_gates`, and `allocation`. Everything below reads those two existing endpoints; nothing new was added to reach them.
  - **T3d-S — S1 floor / S2 derived / S3 ceiling** (`frontend/src/lib/offerDeskScenarios.ts::scenarioStrip`, wired into `OfferDeskDocumentCheck.tsx` and, cheaply, as a one-line-per-unit annotation on `OfferDeskWorkGraph.tsx`). `docs/BUILD_PROGRAM.md` names the shape but not an arithmetic, same as `services/verdict.py`'s own docstring says of itself — so this is a stated design decision, not a hidden one: S2 (derived) is not recomputed at all, it is `Verdict.recommended_level` exactly as the backend returned it. S1 (floor) and S3 (ceiling) replay `verdict.py`'s own `base_level()` against the minimum/maximum of the SAME seven real property scores instead of their mean — "if this record's weakest/strongest scored property dominated" — using a hand-mirrored copy of `base_level()`'s bands and L6 special case (Python and TypeScript necessarily duplicate this small pure function; a comment in the TS file points back at the Python original so the two don't drift silently). The hard-gate cap is not re-derived (the frontend has no access to `evidence_path_exists`/`requires_licensed_human`) — it is read back from the real `applied_gates` list the backend already computed and reapplied identically to all three scenarios. This is the actual enforcement of `BUILD_PROGRAM.md`'s hard rule ("S3 cannot make 'release offer' allowed without evidence"), not just UI copy asserting it: **verified against the real backend**, not just reasoned about — `WU-OD-02` scored with `evidence=1` (deliberately tripping `gate4_evidence`) returns `recommended_level=2, allocation=make, applied_gates=["gate4_evidence"], uncapped_level=5` from the real `/verdict/` endpoint (via `TestClient`, see the finding below for why not the full live browser round trip); fed through `offerDeskScenarios.ts`'s own functions (reproduced standalone in Node against that exact JSON), **S1 floor = L1 make, S2 derived = L2 make, S3 ceiling = L2 make** — capped down from what the raw min/mean/max math alone would put at L1/L2/L6, by the one real gate. Dual employment itself is not a VERDICT gate (it is Offer Desk's own policy stop, `offer-desk-inputs/02-policy/uan-dual-employment-stop-policy.md`: "No level of hiring urgency, manager appetite, or business pressure authorizes releasing the offer") — nothing about this strip's math touches it; "Release offer" stays hardcoded `disabled` exactly as T1 (TIGHT-WEDGE) left it, unconditionally, regardless of what S1/S2/S3 read. **"Not scored" is real, not a placeholder for a spinner**: computed only from a real Work Unit (`WU-OD-02` — the evidence pack's own code for this exact record — matched by code, per-tenant) and a real `VerdictScore` row; either missing renders "Not scored" with a real next step (import the pack; go score it) instead of any number. Guest never attempts the read at all, same idiom as the rest of this walk.
  - **T3b-P — field provenance** (`offerDeskScenarios.ts::fieldProvenance`). `WorkUnit.provenance` (`models/workunit.py`) is **one value for the whole row** — there is no per-attribute `source_type` column, confirmed by reading the schema before designing this, not assumed. So "observed N / declared M... across the 18 attributes" is built as: every one of `services/contract.py::missing_attributes`' 16 checked scalar attributes that IS populated on the real fetched unit gets attributed to that unit's own real `provenance` value; the rest render as a named "not yet provided" list (the exact labels `missing_attributes` already returns, e.g. "7 Trigger" — not reworded). Every other source-type bucket (of the four `Provenance` values) renders its real count of zero rather than being hidden — `BUILD_PROGRAM.md`'s own "(and other source_types if real)" qualifier, read literally. `#15 dependencies` (lives on the Work Graph as an edge, not a scalar field — `contract.py`'s own comment) and `#17 regulatory_register_link` (null legitimately means unregulated) are named on the card as tracked separately rather than folded into a fabricated "18," a precision this file's own culture asks for over a rounder-looking number. **No hardcoded 7/11**: verified live — `WU-OD-02`'s real card reads a real populated/not-yet-provided split under its own real `provenance` (`observed`, confirmed against `offerDeskEvidencePack.json` — 9 of the 11 evidence-pack units are `observed`, `WU-OD-07`/`WU-OD-11` are `declared`, the same two Gate 10 already flags on the Offer Desk Gap screen), not the architecture map's illustrative example. Guest sees educational copy and a "not scored" state, never a fake Client A count.
  - **A real, separate finding — investigated, not fixed, and not this slice's to fix.** Scoring `WU-OD-02` for real through the actual `/verdict` page in a live browser (meant as this slice's own end-to-end proof of the gate-capping math above) reproducibly 500s (`InvalidRequestError`, logged only as `exc_type` per Track 6 slice 6.1's own deliberate no-message-logging policy) — but only through the long-running `uvicorn` dev server after the request volume this walk's earlier screens generate by that point. Root-caused enough to place it, not guessed: the identical `PUT /api/verdict/{id}` call succeeds cleanly through FastAPI's `TestClient` (a fresh in-process app) and against a raw tenant-bound DB session called directly; a bare `http.client` PUT straight at `:8000`, bypassing the browser and the Vite dev proxy entirely, reproduces the 500 anyway — ruling out both the browser and the proxy and pointing at connection-pool/session-lifetime state in `dependencies.py`/`db.py`'s existing plumbing (shared by every tenant-scoped router, `verdict.py` included) under sustained load. Nothing in this slice touches any of those files. Named here precisely, per this file's own standard, rather than silently worked around or left implied — a real, pre-existing gap for whoever next touches connection lifecycle, not a TIGHT-3D defect.
  - **Verified before pushing**: full backend suite, **199 passed, 0 failed, 0 skipped**, against a real local Postgres 16 with the `wep`/`wep_app` role split (unchanged count — expected, this slice touches zero backend files). `alembic check`: no drift. Frontend: `tsc --noEmit && vite build` clean. A throwaway Playwright script (not committed — this repo's own established precedent), run against a freshly migrated database each time: **19/19 checks passed** — guest walk (Hours 95/61.8 unchanged, both new strips render in an honest not-fabricated guest state, Release offer disabled, zero real key ever lands in `localStorage`) and the keyed walk (real demo bootstrap, real evidence-pack import, Document check showing both strips with real fixture-scored VERDICT data matching hand-computed values exactly, Field provenance's real non-hardcoded count, Work Graph's cheap per-unit annotation, Release offer still disabled) and Spec deny (still HTTP 200 denied, `evidence_ref required by contract`, unchanged).
  - **Did not do**: no WorkOS, no Gate 11 ordering, no Finance/Legal pack, no buying Render, no promotion ladder, no extra Work Graph edge types, no INT-007 (or any other) new intent identifier scheme, no org-moderation inbox, no `p*`, no actor-class runtime, no Box 4, no backend rewrite, no new migration or endpoint. Track 6.2's Postgres backup STOP-GATE is unchanged and still open, expiring **2026-10-05**.

- **Slice `HR-FAMILY`: VERDICT-CARD on Document check, plus the real HR family alongside Offer Desk — one PR, no backend change.** `VerdictOut` already carried everything the card needed (`uncapped_level`, `recommended_level`, `level_name`, `allocation`, `applied_gates`, the seven raw scores); nothing new was added to reach it.
  - **VERDICT-CARD** (`components/VerdictCard.tsx`, mounted on `OfferDeskDocumentCheck.tsx` above the existing, untouched TIGHT-3D S1/S2/S3 strip — additive, not a replacement). Shows the seven V·E·R·D·I·C·T scores, **readiness** (`Verdict.uncapped_level` — the real, already-computed field, not re-derived), **restraint** (`uncapped_level − recommended_level`; "None" when no gate fired, or the gate count and levels when one did), **recommended level**, and **applied gates translated to plain sentences** (a `GATE_LABELS` map naming what each of `verdict.py::apply_hard_gates`' four real gate ids actually caps and why — not a re-derivation of the gate logic itself). Every field with an i-button. "Not scored" renders whenever no real `VerdictScore` row exists for the matched Work Unit; guest sees an explicit "not fabricated for guest viewing" line, same idiom the S1/S2/S3 strip already uses on this page. The dual-employment stop line is restated unconditionally, regardless of level or gates. No Box 4.
  - **Desk family** (`frontend/src/lib/desks/`). `types.ts`'s `DeskSpec`/`DeskStep` generalize Offer Desk's own step-list columns (id, name, what happens, system, time, volume, automation tag, SPOC) — not a new shape invented alongside it. Five sittings — Onboarding, Offboarding, Vendor Mgmt, US HR, HRBP — transcribed verbatim from `desks/*.xlsx`, the founder's real Trianz Time & Motion Study (May 2026), extracted from the actual source email (Sriharshini Rallabandi → Sanuj, 21 May 2026, forwarded 16 Aug 2026) via its raw MIME attachments, not retyped from a screenshot or invented. `desks/README.md` carries that email's own one-line honesty status per desk: Onboarding and Offboarding **finalized**; Vendor Mgmt, US HR, and HRBP **need follow-up** — Trianz's own qualifier, not this repo's guess, and it is repeated verbatim on every one of the five new desk-walk pages via a status badge + i-button, not smoothed over. Offer Desk itself (`offerDeskData.ts`) is untouched — `desks/offerDesk.ts` adapts its existing exported constants (one new `export` added to an existing internal `STEP_TITLES` map, zero data changed) into the shared `DeskSpec` shape, so Offer Desk was never re-parsed or cloned. None of the five new desks has an ingestion path into the real backend (no genome, no Work Unit, no VerdictScore) — every number on their pages is `declared` (the sitting's own words), never `observed`, same distinction this file already draws for Offer Desk. No second "defended" hours figure was invented for any of the five: Offer Desk's own 61.8 came from four costing disciplines a human applied by hand (`offerDeskWorkRecord.ts`), not a second number its sheet states, and none of the five new sheets states one either — each desk page shows one declared total, labelled as declared.
  - **Map**: `HrOps.tsx` now links all five desks (Offer Desk live; Onboarding/Offboarding labelled finalized; Vendor Mgmt/US HR labelled needs-follow-up). `HrMap.tsx` makes "HR business partner" its own clickable peer card next to "HR operations" — not nested under it, since the sitting shows HRBP's own onboarding/offboarding involvement is real work distinct from either desk. Talent acquisition, employer branding, workforce planning, and total rewards stay grey, unchanged.
  - **HR Function Work Graph** (`pages/HrFunctionGraph.tsx`, `lib/desks/functionGraph.ts`, new). One node per desk. Sequence lines are each desk's own step-id order, read straight off its `DeskSpec.steps`, not redrawn as a picture of a different order. Exactly three cross-desk HANDOFF edges are drawn, each read live off a real `DeskHandoff` row (`functionGraph.ts`'s `find()` throws at import time if the underlying row ever changes shape, so this cannot silently drift from the desk data) rather than re-typed as a fourth copy of the quote: Offer Desk → Onboarding (Offer Desk's own handoff map, "Rashmi → Prerana/Sasikala/Tamil"), Offboarding ↔ HRBP both directions (Offboarding's own handoff map names both "Darwinbox → HRHub (HRBP)" and "HRBP → Separation SPOC"), and Vendor Mgmt → Offer Desk (Vendor Mgmt's own handoff map, "Reshma V → Rashmi KN (Offer Desk)" for contractor conversion). That last one is deliberately **not** drawn bidirectionally: Offer Desk's own sheet separately names an "Asset vendor" for contractor offer letters, but that is not unambiguously the same party as the Vendor Mgmt desk, so a second edge was not guessed into existence — the page says so in its own copy rather than silently drawing three edges where the brief named three pairs. US HR renders as its own visually separate "parallel cluster" (Job Vite, not Zwayam) with no edge into the India cluster. Rashmi KN — the same real person interviewed for both Offer Desk (12 May) and US HR (14 May) — is named on both cards as a shared SPOC, explicitly captioned as *not* an edge, and the two desks are never merged into one node. No edge type beyond sequence and handoff exists anywhere on this page (no shared-object, shared-resource, or reciprocal claims) and no handoff beyond the three named above is drawn. This is a client-side, `lib/desks/*.ts`-only schematic — it does not call `GET /projections/work-graph` or any other backend endpoint, so guest and signed-in viewers see the identical page; none of the five new desks has ever been imported as a genome, so there is no observed edge available to read even for a signed-in tenant.
  - **Desk walks**: one shared `components/DeskWalk.tsx` (overview + step list + SPOC + hours-from-sheet + a link to the function graph) behind five thin page wrappers, not a 12-tab Offer Desk clone repeated five times. Three-seat playback is explicitly deferred, per the brief, not attempted.
  - **A real, separate finding, rediscovered independently and consistent with the one already named above (T3d-S's "investigated, not fixed" entry) — not a new bug and not this slice's to fix.** Scoring `WU-OD-02` for real through the actual `/verdict` page in a live browser reproduced the same `PUT /api/verdict/{id}` 500 that entry already documents; a fresh, unloaded `TestClient` call to the identical endpoint with the identical payload returned a clean 404 (RLS correctly hiding an unpinned tenant's Work Unit), not a 500 — consistent with that entry's own conclusion that this is connection-pool/session-lifetime state under sustained load, not a fresh-request defect, and not something this slice's own changes could have caused (zero backend files touched). One additional, smaller, real observation surfaced while tracing this, named rather than acted on: `Verdict.tsx` calls `api.put()` (`frontend/src/api.ts`), and `api.ts`'s `put()` helper — unlike its `get`/`post`/`postForm`/`patch` siblings, and unlike every method `apiFetch.ts` wraps — never accepts or forwards a `specKey`, so the shipped Verdict page's save action never sends `X-Spec-Key` at all. `verdict` is one of the routes on `OptionalTenantDbDep` (a missing key there is "not an error," per `dependencies.py`'s own docstring), so this does not 401 — it just means a real, tenant-scoped VERDICT save from that page runs unpinned, riding on whatever the pooled connection happens to be carrying. Named here as an observation, not claimed as the 500's root cause (a fresh, keyless `TestClient` call did not reproduce it) and not fixed here — `api.ts`/`apiFetch.ts`/`Verdict.tsx` are untouched by this slice. This slice's own live-scoring proof therefore used the same workaround T3d-S already established: `apply_verdict()` called directly against a real, RLS-bypassed system session (matching exactly what the endpoint does internally), not the broken browser round trip — verified to produce the identical result (`WU-OD-02`, `evidence=1` → `recommended_level=2`, `applied_gates=["gate4_evidence"]`, `allocation=make`) that T3d-S's own `TestClient` proof already found for the same unit and the same deliberately-low evidence score.
  - **Verified before pushing**: full backend suite, **113 passed, 0 failed, 86 skipped** (RLS/tenant-isolation tests self-skip without a real non-superuser role split locally — unchanged shape from prior entries; zero backend files touched by this slice). `alembic upgrade head` clean on a freshly created database. Frontend: `tsc --noEmit && vite build` clean. A throwaway Playwright script (not committed — this repo's own established precedent), run against a freshly migrated local Postgres 16 each time: **24/24 checks passed** — guest walk (Hours 95/61.8 unchanged; HR operations shows all 5 desks as clickable cards; the function graph shows all 3 named handoffs, the US HR parallel-cluster label, and the Rashmi KN shared-SPOC note; HRBP clickable on the CHRO map, peer to HR operations; the VERDICT card's honest guest copy; the S1/S2/S3 strip unchanged) and the keyed walk (real demo bootstrap, real evidence-pack import, the VERDICT card rendering an honest state post-import, then — after a real `VerdictScore` row exists — all seven letters, Readiness, Restraint's "held back" text, Recommended level L2, the evidence gate named in plain words, the unconditional dual-employment line, and the S1/S2/S3 strip still present alongside it).
  - **Did not do**: no WorkOS, no Finance, no Darwinbox API connector, no extra Work Graph edge types beyond sequence + handoff, no observed-pack theatre (no desk's declared numbers were upgraded to observed), no backend rewrite, no new migration or endpoint, no Box 4, no three-seat playback for the new desks.

- **Slice `FAMILY-GENOME`: the VERDICT write fix HR-FAMILY's own entry named as a real, separate, unfixed gap, plus one family genome spanning all six real desk sittings — one PR, one backend service function touched.**
  - **`PUT /api/verdict/{id}` 500 under load — root-caused for real, not guessed, and it was two bugs, not one.** HR-FAMILY's and TIGHT-3D's own entries above named this precisely as "investigated, not fixed... a real, pre-existing gap." Reproduced live against a real local Postgres 16 (the exact `wep`/`wep_app` role split, not SQLite/TestClient — TestClient never showed this, same finding those entries already made) by hammering `PUT /api/verdict/{id}` for a freshly created, never-scored Work Unit with 20 concurrent requests: 1 in 20 500'd, `exc_type=IntegrityError` in the structured error log (Track 6 slice 6.1's own logging). Root cause: `services/work_units.py::apply_verdict` is a check-then-insert upsert (`row = wu.verdict; if row is None: db.add(VerdictScore(...))`) and `verdict_scores.work_unit_id` is `UNIQUE` — two concurrent first-time scores for the same unit both see `wu.verdict` as `None` and both try to insert; the loser's flush hits the winner's already-committed row.
    - **The first fix attempt (`db.rollback()` then re-query the winner's row) reproduced a second, worse failure live, not caught by reasoning about it in advance**: `ObjectDeletedError` on the very next reload of `wu`, roughly 1 in 20 again. Root cause, confirmed by reproducing it directly against the DB (bypassing HTTP for a full traceback, since Track 6 slice 6.1's error handler deliberately never logs exception messages): this endpoint's request opens on `OptionalTenantDbDep`, which sets RLS scoping with a plain `SET app.current_client_id` (`dependencies.py`, deliberately not `SET LOCAL` — see that file's own comment on why) at the start of the request's one transaction. Postgres undoes a plain `SET`, exactly like any other statement, when the transaction that issued it is rolled back — `db.rollback()` silently stripped the request's own tenant scoping, so the next reload of `wu` ran unpinned and RLS correctly returned zero rows for it.
    - **Fix: a `SAVEPOINT` (`db.begin_nested()`), not a full rollback.** The speculative insert is wrapped in a nested transaction; on `IntegrityError` only that nested transaction unwinds, leaving the outer transaction — and the `SET` it carries — untouched. Re-verified the same way: 6 runs × 20 concurrent direct-DB calls and 4 runs × 20 concurrent HTTP calls against the real running `uvicorn` dev server, **0/200 failures**, `grep unhandled_exception` on the server log empty across all of it. `tests/test_verdict_write_race.py` (3 tests, SQLite — the race itself doesn't depend on RLS or real concurrency, only the unique constraint and a session that already cached `wu.verdict` as `None` before a competing row lands, the same "simulate the collision deterministically in one session" idiom `test_genome_import_conflicts.py` already established for the analogous `work_units.code` race) reproduces the original `IntegrityError` against the pre-fix code (confirmed by temporarily reverting the fix and re-running) and passes against the fix.
    - **The second half of the same named gap**: `frontend/src/api.ts`'s `put()` never accepted or forwarded `specKey` — the shipped Verdict page's save action never sent `X-Spec-Key`. Fixed (`put<T>(path, body, specKey?)`, matching `patch`'s existing signature); `Verdict.tsx` now passes `getSpecKey()`. **This turned out to matter more than "an unpinned read": reproduced live that without a key, concurrent requests each land on whatever tenant a recycled pooled connection happens to be carrying** — 20 concurrent keyless `PUT`s against a real Work Unit returned 1 success and 19 `404`s (RLS correctly hiding the unit from a request pinned to nothing, or to a stale tenant), not a consistent result either way. `frontend/src/pages/Economics.tsx` calls `api.put()` the same unkeyed way and was not touched — named here as a real, separate, adjacent instance of the same pattern rather than silently left implied; `economics` is one of the routes `dependencies.py` already documents as still open (`OptionalTenantDbDep`), so it doesn't 401, it just stays unpinned, same as verdict did.
    - Verified end to end, live, through the real browser UI (Playwright, Chromium, against the real dev stack): scoring a freshly created Work Unit through the actual Verdict page's Save button succeeds and renders the real recommended level; a second, separate freshly created unit hit with 15 concurrent `PUT`s through the real Vite dev-server proxy (not a direct backend call) returns 200 fifteen times.
  - **One family genome, all six real desk sittings, same client as Offer Desk.** `frontend/src/lib/desks/familyGenome.ts` (new) builds a `POST /genome/import` payload — the same unmodified pipeline and the same GQS>=90 gate Offer Desk's own evidence pack faces — from all six `DeskSpec`s (`lib/desks/*.ts`, HR-FAMILY's own real, verbatim Trianz data; nothing re-typed for this slice). 95 Work Units (Offer Desk 11, Onboarding 23, Offboarding 26, Vendor Mgmt 14, US HR 10, HRBP 11), every one `provenance.source_type: declared` — this platform has no Darwinbox/Zwayam/Job Vite connector, so nothing here can honestly be `observed`, including Onboarding and Offboarding despite their sittings being "finalized" (`desks/README.md`'s own distinction: finalized describes the workflow documentation, not an observed system-of-record log).
    - **Codes namespaced per desk, 3-digit** (`WU-OD-001`, `WU-ONB-001`, `WU-OFF-001`, `WU-VEN-001`, `WU-US-001`, `WU-HRBP-001`, ascending by sheet order). **A real, checked-for collision, not assumed clear**: `services/demo.py::ensure_catalog_hr` clones a fixed 12-unit generic HR census onto every client at bootstrap using 2-digit `WU-ONB-01..07` / `WU-OFF-01..05` codes — a different, older, coarser SOP sample already occupying this tenant. `work_units` is `UNIQUE(client_id, code)`; verified against a real Client A already carrying that seed that all 95 3-digit codes are disjoint from it (direct `SELECT`, zero overlap) — 2-digit and 3-digit are different strings even where the number would otherwise collide, so this genome imports into the same tenant as that census without touching it.
    - **Edges: sequence within desk, computed per step-id cluster, not one flat chain.** Five desks use plain numeric step ids (one chain each); HRBP's sheet uses its own `OB-`/`OFF-`/`GR-` prefixes for three genuinely independent sub-processes (onboarding overlay, offboarding overlay, grievance) bundled in one sheet — treating all 11 as one chain would have wrongly claimed, e.g., that the (discontinued) onboarding touchpoint step blocks the voluntary-resignation discussion step. Grouping by each step id's leading alphabetic prefix (falling back to one group per desk where there is none) gets both cases right from one rule, with no HRBP-specific branch.
    - **Exactly the three handoffs already on the function graph (`functionGraph.ts::CROSS_DESK_HANDOFFS`), wired as real dependency edges — no new edge type.** `EdgeType` has no `handoff` value (`models/graph.py`); every edge this genome produces, cross-desk included, is `sequence`, the same type Offer Desk's own evidence-pack chain already uses — "no extra edge types" read literally. Each of the three (four directional: Offboarding↔HRBP is genuinely bidirectional) handoffs is anchored at a specific step chosen from each desk's own text, not the nearest convenient step: Offer Desk step 9 ("Place documents & notify onboarding team" — the step whose own title names the handoff, not step 11, which is the drop-out exception path) → Onboarding step 1; Offboarding step 5 ("HRHub (HRBP) discussion") ↔ HRBP step `OFF-1` ("Voluntary resignation — HRBP discussion", the same real event named from each side); HRBP `OFF-1` → Offboarding step 6 ("Separation details confirmed", immediately following step 5, matching that handoff's own "After HRBP discussion" trigger); Vendor Mgmt step 14 ("Consultant onboarding", its last step — the sheet names no dedicated conversion step) → Offer Desk step 1. `familyGenome.ts::crossDeskDependencies` resolves these against `CROSS_DESK_HANDOFFS` itself and throws on any count mismatch, so the genome can never silently draw a fourth edge or drop one of the three. Verified live: the real payload's four cross-desk pairs are exactly `WU-OD-009→WU-ONB-001`, `WU-OFF-005→WU-HRBP-004`, `WU-HRBP-004→WU-OFF-006`, `WU-VEN-014→WU-OD-001`.
    - **GQS: fails honestly, for the right reason, not padded.** Live import against a real Client A: **GQS 30.00 / 90, `accepted: false`, zero violations, zero orphan dependencies, 95 Work Units scored** — confirmed against the real running backend, not computed by hand. Observed% is structurally 0 (the whole point), which alone caps the score at 60 regardless of anything else (`services/gqs.py`'s frozen 40% weight); no `dual_scoring_kappa` is supplied, unlike the bootstrap sample genome's own disclosed-but-invented 0.85 — nothing in this slice measures two independent scorings, so none is claimed, even though the gate fails by a wide enough margin that it would not have mattered either way. **A known, pre-existing GQS quirk, not something this slice worked around**: `completeness_pct` also reads 0%, not the 100% every unit's own 18 filled attributes would suggest, because `regulatory_register_link: []` (none of these steps cite a specific regulation — a legitimate value) is scored identically to a missing attribute by `_wu_is_complete`'s blunt `in (None, "", [], {})` check. TIGHT-3D's own field-provenance entry above already named this exact `#17` distinction ("null legitimately means unregulated"); this slice does not invent a fabricated regulatory citation to work around it, the same restraint that entry already established. A handful of the 18 attributes with no direct DeskStep column (`acceptance_criteria`, `evidence_required`, `failure_semantics`, `context.decision_branches`) get one honest, literal placeholder — the same idiom `services/scout_genome.py`'s `NOT_CAPTURED` already established for the identical reason (a genome should score as thin as the source genuinely is).
    - **The Offer Desk evidence pack stays the only path that can pass GQS for `WU-OD-*` units, and the two never collide or merge.** This genome's Offer Desk units are `WU-OD-001..011` (3-digit, declared); the evidence pack's are `WU-OD-01..11` (2-digit, observed, fabricated test evidence, 92.73/90). Different strings, both importable into the same tenant, never averaged into one "all desks observed" number — the family-genome screen says so above the fold, not just in this file.
  - **Keyed "Import family (declared)" screen** (`pages/HrFamilyGenome.tsx`, new route `/hr/family-genome`, linked from HR operations). Lists units per desk and the four cross-desk edges (real `WU-*` code pairs) **before** any import is attempted, from the same payload builder that would be posted — "still lists units/gaps" is literal: a rejected import writes no `WorkUnit`/`WorkEdge` rows (`import_genome` returns before `_write_genome` runs on a GQS fail, same as every other genome), so what renders is this screen's own payload, not a read of rows that don't exist. Calls the real, unmodified `POST /genome/import`; renders the real GQS breakdown and violations either way (`accepted` or gate-held), same pattern `OfferDeskEvidencePack.tsx` already established. Guest sees the same units/edges list (nothing about desk content is tenant-specific) but no attempt button — sign-in prompt only, same idiom as every other write action on this walk.
  - **`HrFunctionGraph.tsx`: keyed-only section reads from this genome, not only the static sheet.** The existing static schematic (guest and keyed both see it, unchanged) now sits above a new card, shown only when signed in, that reads `buildFamilyGenomePayload()` directly — the identical object the import screen posts, not a second hand-maintained picture — and renders the same four cross-desk `WU-*` pairs. The real GQS score for that exact payload is deliberately not pre-computed or guessed here; the card links to the import screen for that.
  - **Verified live, not asserted from reading the code**: full backend suite, **202 passed, 0 failed, 0 skipped**, against a real local Postgres 16 with the exact `wep`/`wep_app` role split (up from 199 in TIGHT-3D's last recorded count — the 3 new race tests). `alembic check`: no drift (zero migrations touched). Frontend: `tsc --noEmit && vite build` clean. A throwaway Playwright script (this repo's own established precedent — not committed), run against the real dev stack (`vite` dev server → proxy → real `uvicorn` → real Postgres): **18/18 checks passed** — guest walk (Hours 95/61.8, no real key ever in `localStorage`, function graph shows the static OD→Onboarding handoff and correctly has no "read from this genome" section), the keyed VERDICT write and its 15-concurrent-request non-regression check, the family-genome screen's pre-import units/edges list and its live GQS 30.00/90 gate-held result, the keyed function graph's new genome-reading section showing `WU-OD-009→WU-ONB-001` from data, and keyed Hours still 95/61.8 unchanged.
  - **Did not do**: no WorkOS, no Finance, no Darwinbox/Zwayam/Job Vite API connector, no login, no extra Work Graph edge types beyond sequence + handoff, no observed-pack theatre for any desk (Onboarding/Offboarding included), no fabricated `dual_scoring_kappa`, no padding of observed or completeness to sneak past 90, no merge of the evidence pack's 92.73 into this family, no backend rewrite beyond the one `apply_verdict` fix, no new migration or endpoint, no fix to `Economics.tsx`'s own unkeyed `api.put()` call (named above, out of scope for this slice).

- **Slice `OBJECTS-HOURS`: three Box-1-lite object cards (Employee, Offer, Contractor) plus a declared-hours-by-desk panel — one PR, frontend only, FAMILY-GENOME untouched.** `frontend/src/lib/desks/objects.ts` wires desks to objects exactly per the brief (Offer Desk → Offer; Onboarding/Offboarding/HRBP → Employee; Vendor Mgmt + Offer Desk's own contractor-labeled steps → Contractor) and deliberately leaves US HR unwired to any of the three, since the brief names five of six desks and this file does not invent the sixth mapping; each card's "which desks/units reference it" is read live from `familyGenome.ts`'s own `buildFamilyGenomePayload()` (real `WU-*` codes, not a second hand-typed list), and the Contractor card's Offer Desk share is computed by scanning `whatHappens` for the sheet's own `CONTRACTOR:`-labeled branches (steps 2, 4, 5 — `WU-OD-002/004/005` — verified against the real step text, not guessed) rather than a hardcoded step-number list. `current_condition` on each card is a plain-English paraphrase of the contributing desks' own `outcome` fields, not invented ERP status language, and every card states its `as-of` sitting date(s) as declared, never observed. `frontend/src/lib/desks/functionHours.ts` reuses the exact sourcing Offer Desk's own Hours page already established for its declared 95 (`DeskSpec.totalEstimatedSavings`, verbatim — the same string `OFFER_DESK_TOTAL_SAVINGS` that page's `declaredHoursLabel` already reads) across all six desks, live-verified: 95/22/42/140/16/90–150 (HRBP's own line states two figures, "~30-50 hrs/mo per HRBP" and "~90-150 hrs/mo across team" — the team-wide one is summed, to stay comparable with every other desk's own desk-wide number), summing to 405–465 hrs/mo, labelled "stated across sittings, not defended" per the brief, with zero invented defended figure for any desk (`OfferDeskHours.tsx` itself is untouched — still 95 vs 61.8, on its own page only). `HrFunctionGraph.tsx` gains object-card links on desk nodes and on the three handoff cards (an additional link alongside each desk's existing click-to-walk-page behaviour, not a replacement for it, since that behaviour already shipped in HR-FAMILY); guest and keyed see identical object-card and hours-panel content, since neither reads tenant data. **Verified live**: backend suite unaffected (202 passed, zero backend files touched — this slice is frontend-only), `tsc --noEmit && vite build` clean, and a throwaway Playwright script (31/31 checks) against the real dev stack — guest Hours 95/61.8 unchanged, all three object cards render guest-visible and declared-labelled, the hours panel shows all six real figures with every "61.8" mention attributed to Offer Desk specifically (never implied for another desk), the keyed walk clicks function graph → Employee object card → function hours panel end to end, and the family genome import still GQS-fails live (30.00/90, unchanged) confirming FAMILY-GENOME was not reopened. Did not do: no WorkOS, no Finance, no observed-pack theatre, no Darwinbox API, no extra Work Graph edge types, no Box 4, no Render purchase, no workforce simulator (Box 6), no real Ontology backend (Box 1 stays "later or refuse" — these are static reference cards, no `entity_types`/`entities` row, no endpoint), no US HR object mapping invented past what the brief named.

- **Slice `CENSUS-v0`: the six-step Work Census shell, one real Work System (Offer Desk → Onboarding) with a persisted keyed Ratify, and the Work Chart hero screen — one PR.** Does not reopen OBJECTS-HOURS/FAMILY-GENOME/HR-FAMILY/TIGHT-3D/TIGHT-WEDGE: Offer Desk, 95/61.8, Spec deny, family genome, object cards, function hours, and `desks/*.xlsx` are all untouched except the two copy edits named below.
  - **Census shell (Part A).** Home (`/`) is now step 1, Scope — not a separate landing page ahead of a six-step wizard. Five new routes (`/census/capture`, `/census/evidence`, `/census/gap`, `/census/chart`, `/census/plan`) plus a new `CensusStepper` component render "Work Census · n of 6" on every one of the six. All six are thin shells over already-shipped depth, per the brief's own instruction not to rebuild: Capture links to the existing three-seat Offer Desk walk (and only from here — Home's own page no longer links into Offer Desk at all, satisfying "demote it off Home"); Evidence links to Document check / the evidence pack / Spec deny's own upload-and-clear button; Gap reuses the exact same four guest rows and the exact same `GET /discovery/gaps` read `OfferDeskGap.tsx` already used (no second gap detector); Plan is links-only to Document check's VERDICT card, Hours, and function hours — nothing recomputed. `ProgressTracker.tsx`'s and `GuidedTour.tsx`'s existing `V9_ROUTE_PREFIXES` both gained `"/census"` so the old V8 "n of 7" pill bar and the pre-V9 tour stay hidden on every one of these six routes, the same mechanism that already hides them on `/enterprise`, `/hr`, and `/scout/offer-desk`.
  - **Copy: "blast radius" → "Scope", narrowly, not a global rename.** Only the two places that literally used the customer-facing words "blast radius" got touched — `HrMap.tsx`'s `<LabelWithInfo label="Blast radius" />` and two sentences of `BlastRadius.tsx`'s own lede/meter copy. The route (`/scout/blast-radius`), the component name (`BlastRadius`), `services/scout_blast_radius.py`, `ScoutBlastRadiusSelection`, and every internal/technical reference to "blast radius" elsewhere in this codebase are untouched — renaming those would be a much larger, riskier refactor this slice was not asked to do. "GQS → quality gate" and "genome strength → completeness" (the brief's other two customer-copy rules) were applied only inside this slice's own new copy (e.g. Evidence's "the one path in this walk that actually clears the quality gate") — the pre-existing GQS-labelled screens this PR did not touch (`HrFamilyGenome.tsx`, `Genome.tsx`, `Overview.tsx`, `GenomeStrengthMeter.tsx`, …) still say "GQS" and "Genome Strength," on purpose: renaming those is a separate, wider change this slice does not make. "Genome" itself stays everywhere, including here.
  - **One Work System, a real table (Part B).** `work_systems` (new: `backend/app/models/work_system.py`, alembic `549fc2e9287a`, same `client_id` + `tenant_isolation` RLS policy shape as the most recent tenant table, `users`) holds exactly one row per tenant for this slice: the Offer Desk → Onboarding journey, `code="WS-OFFER-ONBOARD"`. `entry` is Offer Desk's own real trigger, `exit`/`outcome` is Onboarding's own real stated outcome (both read verbatim from `OFFER_DESK_META`/`ONBOARDING_SPEC` client-side, `frontend/src/lib/workSystem.ts` — not re-typed in Python), and `owner` reuses the exact stand-in label `OFFER_DESK_SEATS` already uses ("Head of HR operations (stand-in)") because nobody today genuinely owns the cross-desk handoff itself — each desk only owns its own half, which is the honest reading of "missing middle." `GET/POST /api/work-systems` is get-or-create by `(client_id, code)` — a second POST from a later page load returns the existing row unchanged rather than clobbering an already-ratified one. `POST /api/work-systems/{id}/ratify` requires a non-empty `ratified_by`, sets a server `ratified_at`, and 409s on a second ratify of the same row. This is real persistence, not in-session state: verified live (Playwright, below) that a ratified Work System survives a full page reload. `tests/test_work_systems.py` (7 tests, real Postgres — `TenantDbDep`'s `SET app.current_client_id` is Postgres-only syntax, same reason `test_rls_http.py` is) covers get-or-create idempotency, ratify's status/name/timestamp, the 409 on a second ratify, the 422 on an empty name, and cross-tenant isolation (Org B gets 404 ratifying Org A's row, and Org B's own `ensure` call creates its own separate row rather than colliding). No second Work System, no Offboarding journey, this slice.
  - **Work Chart (Part C, hero).** `pages/CensusWorkChart.tsx`: a purpose strip (function = HR operations; this journey's target = the same declared-95/defended-61.8 Hours page, linked not recomputed), the Work System card with its Ratify control, and two lanes — Offer Desk (11 steps) and Onboarding (23 steps). **HRBP is deliberately not a third lane**: `CROSS_DESK_HANDOFFS` (`functionGraph.ts`) names exactly three handoffs — Offer Desk→Onboarding, and Offboarding↔HRBP (bidirectional) — and none of them touch this Offer→Onboarding journey, so the honest answer to "HRBP only if a named handoff touches it" is that it doesn't; the page says this in its own copy rather than silently omitting the lane. Each row is code, name, SPOC, and a level badge; the careful / as-calculated / ambitious toggle is a relabelling of Document check's own S1 floor / S2 derived / S3 ceiling and calls the exact same `scenarioStrip()` (`lib/offerDeskScenarios.ts`) against a real `VerdictScore` — no second scoring engine, and a row with no matched, scored Work Unit always reads "not scored," toggle position notwithstanding. Guest sees only the declared `DeskSpec` schematic (`lib/desks/offerDesk.ts` / `lib/desks/onboarding.ts`): every row's code renders as `(step N — not imported)`, never a fabricated `WU-*` code, and every level reads "not scored" — no backend call is made at all for a guest. Keyed additionally reads real `GET /work-units/` + `GET /verdict/` and matches each sheet step against the two real code shapes this codebase's own import paths actually produce (the family genome's 3-digit `WU-OD-0NN`/`WU-ONB-0NN`, and — Offer Desk only — the evidence pack's 2-digit `WU-OD-NN`, the only path that clears GQS for those units); a step with no match in either shape falls back to the same "not imported" label a guest sees. Clicking a row navigates to Document check (Offer Desk step 2 specifically — the same `WU-OD-02` this app already treats as the document-check unit) or to the desk's own page (`/scout/offer-desk` / `/hr/operations/onboarding`) otherwise; clicking an object name goes to the existing Employee/Offer cards (`DESK_TO_OBJECT`, unchanged). A standing note on the page states the dual-employment stop is never lifted by any scenario, at any toggle position — the toggle only ever replays `VerdictScore`'s own hard-gate caps, it does not compute a fresh score that could sidestep them.
  - **Playwright, real for the first time in this repo (previous slices' Playwright coverage was a documented throwaway script, run but not committed).** `frontend/e2e/census.spec.ts` (new, `@playwright/test`, `playwright.config.ts`) — five tests, all against a real running backend + a real built-and-`vite preview`d frontend, no mocked network: guest walks Scope→Plan with the stepper reading `1 of 6` through `6 of 6` and the Work Chart showing both real lane headings; Hours 95/61.8 reachable and visible from Plan; a keyed Ratify that survives a `page.reload()`; the family-genome import (unchanged, FAMILY-GENOME's own screen) scoring in the 15–45 GQS band around the documented ~30/90; and Spec deny's existing empty-`evidence_ref` path still returning `HTTP 200 · denied`. Each keyed test mints its own fresh demo key via `POST /demo/bootstrap?new_keys=true` (the same call "Issue fresh keys" makes) rather than assuming a pristine tenant — `services/tenants.py::get_or_create_client_a` is a singleton, so a from-scratch CI Postgres and a warm, already-exercised one must both pass, and both were verified live, not just the former: **5/5 passed** against a freshly dropped-and-recreated local Postgres (the true first-ever-bootstrap path, exercising the actual Ratify click), and **5/5 passed** again on two subsequent runs against that same now-warm database (exercising the "already ratified from a prior run" branch instead). Wired into CI as a third job (`.github/workflows/ci.yml`'s `e2e`, `needs: [backend, frontend]`) — its own Postgres service container, real `alembic upgrade head` + `uvicorn`, real `npm run build` + `vite preview`, `playwright install --with-deps chromium` (this repository's own dev sandbox has a pre-installed browser; a stock GitHub Actions runner does not, so CI installs one for real rather than assuming the sandbox's shortcut is universal).
  - **Verified live**: backend suite **209 passed** (202 baseline + 7 new, real Postgres 16, `wep`/`wep_app` role split unchanged), `alembic check` clean (the new model's `Index` declaration matches the migration's own — autogenerate agreed on the first honest attempt only after that was added), `python scout pack validate packs/hr` unaffected (18/18). Frontend `tsc --noEmit && vite build` clean. Playwright 5/5, three times, under both `vite dev` and the production `vite preview` build, and once against a database that had never seen this feature before.
  - **Did not do**: no WorkOS, no Finance, no Darwinbox, no login, no observed-pack theatre, no Box 4, no extra Work Graph edge types, no Render purchase, no INTENT-LITE, no Plan rebuild (Plan is links only, exactly three of them), no second Work System / Offboarding journey, no global GQS/"blast radius" rename, no change to Offer Desk, Hours, Spec deny, family genome, object cards, function hours, or any `desks/*.xlsx`.

- **Slice `INTENT-PLAN`: D — INTENT-LITE (two intents, persisted, draft until a keyed Confirm as owner) and E — PLAN (Plan rebuilt as a real screen, plus a new, real, logged Moderation control) — one PR.** Does not reopen CENSUS-v0/OBJECTS-HOURS/FAMILY-GENOME/HR-FAMILY/TIGHT-3D/TIGHT-WEDGE: Offer Desk, Chart, `work_systems`, 95/61.8, Spec deny, family genome, and object cards are all untouched except the two additive changes named below (the Work Chart purpose strip, and one renamed placeholder).
  - **D — INTENT-LITE, persisted as nine columns on the existing `work_systems` row, not a second table.** Alembic `ac2a876395e4` adds `function_intent_{outcome,owner,measure,confirmed_by,confirmed_at}` and `work_system_intent_{purpose,owner,confirmed_by,confirmed_at}` to `work_systems` — `docs/INTENT_CONTRACT.md`'s "no INT-007, or any other, new identifier or table for intent records" still holds, because this is two named, single-row fields on an already-tenant-scoped table, the same shape that table's own `entry`/`exit`/`owner`/`outcome` already use, not a generic per-tenant intent engine. `status` is never its own stored column — `services/work_system.py::to_out` derives `"draft"` vs `"confirmed"` purely from whether `*_confirmed_at` is null, so the two can never disagree. `POST /api/work-systems` (get-or-create, unchanged shape) now also drafts both intents' text from the payload on first creation only, exactly like `entry`/`exit`/`owner`/`outcome` already refuse to be overwritten by a later call (`test_ensure_is_idempotent_and_never_overwrites_existing_row` extended to cover this). Two new endpoints, `POST /api/work-systems/{id}/confirm-function-intent` and `.../confirm-work-system-intent`, are the entire "Confirm as owner" write: a required `confirmed_by` name (422 on empty, same as `WorkSystemRatifyIn`) plus a server `confirmed_at`, 409 on a second confirm, 404 across tenants — the same shape `ratify` already established, applied twice.
  - **Sourced from sheet/sitting text only, no invented COO strategy — and one new verbatim transcription, named as such.** Function intent's outcome (`"Safe offer, two-hour SLA"`) is not retyped prose; `frontend/src/lib/intent.ts` imports it directly from `offerDeskSeats.ts`'s own `PLAYBACK_ROWS` constant (the "What the work is" / function_head cell), so there is no second copy of that sentence to drift. Its owner reuses `OFFER_DESK_SEATS.sub_function_lead.interviewee_name` ("Head of HR operations (stand-in)"), the same stand-in label `work_systems.owner` already uses. Its measure is **parsed**, not hardcoded: `extractSlaMeasure()` regex-matches the `SLA: ...` clause out of `OFFER_DESK_META.frequency` (the T&M sheet field, `OfferDesk_Agent_Ready.xlsx`) and falls back to the literal string `"not stated"` if that clause ever stops existing — the fallback path is real code, not a comment promising one. Work System intent's purpose is the one genuinely new transcription this slice adds: a direct quote from `offer-desk-inputs/01-interviews/function-head-interview-chro.md`'s Q1 ("If Offer Desk stalls, we don't lose a hire in one day — we lose it over two weeks, quietly, as candidates walk to other offers") — that markdown file is sitting text exactly like the `desks/*.xlsx` sheets `offerDeskData.ts` already transcribes from, not imported at runtime, so it's copied here under the same "verbatim, not paraphrased" discipline, cited by file and question number in `intent.ts`'s own doc comment rather than left unattributed. Its owner is the same Head of HR Ops stand-in, per that interview's own Q3 ("HR Operations reports through the Head of HR Ops, who owns the desk's SLAs and escalation path.").
  - **Guest sees the identical drafted text, labelled draft — never governed-looking.** `useWorkSystem()`'s guest fallback (`lib/workSystem.ts`) builds the same `IntentOut` shape with `status: "draft"`, empty `confirmed_by`, null `confirmed_at`; the badge is the plain (non-`ok`-class) `"draft · unconfirmed"` pill, never the ratified/confirmed green styling, and the card's own copy says "Guest: shown as draft/sitting — sign in to Confirm as owner" rather than offering a control it can't complete. No strategy-level constitution, no Intent Studio product, no third intent — exactly the two the brief named.
  - **E — PLAN: `pages/CensusPlan.tsx` is a real screen now, not three links.** This journey's units (Offer Desk + Onboarding, both lanes) each show VERDICT-or-"not scored" together with an S1/S2/S3 strip — the identical `scenarioStrip()` replay Work Chart and Document check already use, extracted (not duplicated) into a new shared `lib/workSystemUnits.ts` (`buildRows`/`candidateCodes`/`documentCheckRoute`, unchanged logic, one owner instead of two copies once Plan needed it too). The dual-employment stop is restated verbatim from `DOCUMENT_CHECK_RECORD.stopRule`, not reworded, with a link back to Document check where it actually lives. Spec deny gets a restatement card and a link to the existing, untouched `/scout/offer-desk/spec-deny` screen — this PR does not re-run or rebuild that check. Hours shows the real 95/61.8 pair directly on the page (not only behind a click-through, which is what guest coverage below actually tests) plus every other desk's own stated-only hrs/mo line from the existing `deskHoursSummary()` (`functionHours.ts`, untouched) — Offer Desk is excluded from that second table since it already has its own callout with both numbers; no defended figure is invented for any other desk. A quality-gate reminder card states the family genome's own known result (~30/90, not a pass) and links to the live screen rather than re-running the import from Plan.
  - **Moderation: real, persisted, append-only — `moderation_entries` (new table, alembic `ac2a876395e4`, same `client_id` + `tenant_isolation` RLS shape as `work_systems`).** `POST /api/moderation` requires both `reason` (min length 1) and `moderated_by` (min length 1) at the schema layer — 422 without either, not only a greyed-out button — and checks `work_unit_code` against a real `WorkUnit` on this tenant (404 otherwise, `get_by_code_or_404`), so a request can't cite a code that was never imported. It never writes to `verdict_scores` or `work_units`: `from_level`/`to_level` are the caller's own already-computed integers, stored as a logged opinion, exactly like `AuditLog.detail` is caller-described text rather than server-re-derived — this is explicitly **not** the VERDICT promotion ladder (`BUILD_PROGRAM.md`'s "no ladder" refusal), and nothing reads this log back into any scenario, gate, or the dual-employment stop. The UI (`CensusPlan.tsx`'s `ModerationSection`) picks a real matched unit code when this tenant has one (from the same `buildRows` used above), using that unit's real S2/S3 when it's actually scored; when it isn't (the common case — genome import alone never creates a `VerdictScore`, only `PUT /verdict/{id}` does), the "move" defaults to a labelled L2→L3 rather than a fabricated real score, with a link to go score it for real. Guest gets an explanation and no form — this needs a real tenant and a real name, same idiom as every other write on this walk.
  - **Verified live, not asserted from reading the code — including a from-scratch local Postgres 16 stood up inside this sandbox specifically to prove it** (no CI run available from here): `alembic upgrade head` applied `ac2a876395e4` cleanly on top of `549fc2e9287a`; `alembic check` reports no drift. Full backend suite **220 passed** (209 baseline + 11 new: 5 in `test_work_systems.py` for confirm-as-owner's set/409/422/cross-tenant-404, 6 in new `test_moderation.py` for create/list/404/422×2/RLS). Frontend `tsc --noEmit && vite build` clean. `frontend/e2e/census.spec.ts`: **8 passed** (5 baseline + 3 new — Chart's purpose strip showing draft intent, a keyed Confirm-as-owner surviving `page.reload()`, and keyed moderation staying disabled until a reason is filled — plus the baseline guest walk extended to assert 95/61.8 and the "~30/90 is not a pass" copy are visible directly on Plan, not only behind a click). **Two real regressions found and fixed by this same run, not shipped and found later.** First: adding the two intent-confirm inputs gave the page a second and third element matching `getByPlaceholder("Your name")`, breaking the pre-existing Ratify test's strict-mode locator — fixed by renaming the intent inputs' placeholder to `"Confirmed by"` (distinct label, distinct `aria-label` throughout), not by loosening the assertion. Second, found by running the new Confirm-as-owner test three times in a row against the same warming Client A tenant (unconfirmed → one intent confirmed → both confirmed): the test's own first draft assumed `getByRole("button", { name: "Confirm as owner" }).first()` always meant Function intent's button, which is only true before either intent is confirmed — once Function intent is confirmed and its button is gone, "first" silently becomes Work System intent's button instead, and the test tried to fill an input that no longer existed. Fixed in the test itself, not the app: it now loops over however many "Confirm as owner" buttons remain, filling each one's own immediately-preceding input via `preceding-sibling::input[1]` rather than a hardcoded label string — verified stable across all three real states (0, 1, and 2 already confirmed), not just the first. Beyond the automated suite, the full Moderation happy path was checked by hand once through the real browser against the real stack: created a genuine `WU-OD-02` Work Unit via the real API, filled and submitted the actual form, and confirmed the new entry rendered in the "Logged" list with a real server timestamp and the form correctly cleared itself.
  - **Did not do, per the brief's own refusal list**: no WorkOS, no Finance, no Darwinbox, no login, no Box 4, no extra Work Graph edge types, no observed-pack theatre, no Render purchase, no Evidence/Gap rebuild (Gap and Evidence are untouched), no delinquency register, no reopening CENSUS-v0. Also did not: build the VERDICT promotion ladder (moderation is a log, not a score change), add a third intent or a per-tenant intent-creation UI, add a strategy constitution or an "Intent Studio," or touch Offer Desk, Hours, Spec deny, family genome, object cards, function hours, or any `desks/*.xlsx`.

- **Slice `EVIDENCE-GAP`: F1 — Evidence (census step 3, a real screen) and F2 — Gap (census step 4, journey-wide) — one PR.** Does not reopen CENSUS-v0/INTENT-PLAN/OBJECTS-HOURS/FAMILY-GENOME/HR-FAMILY/TIGHT-3D/TIGHT-WEDGE: Chart, Plan, intents, 95/61.8, Spec deny, and family genome are all untouched.
  - **F1 — `pages/CensusEvidence.tsx` is a real screen now, not three link cards.** Two new, honest facts about this tenant replace what used to be a description of where to find them:
    - **Files this tenant actually has: `GET /api/files` (new — `routers/files.py`, `schemas/files.py`).** Reads the same `UploadedFile` table `POST /files/upload` (Slice 1 PR 1a) already writes and the same `WorkUnitProvenanceDetail` table `POST /genome/import` (PR 1c) already writes — no second store, no new upload path. This closes a gap `test_file_upload_sha256.py` named explicitly when it shipped: "No GET /files endpoint in this PR." For each file it reports every Work Unit whose provenance cites it, by code plus a plain-language claim built from the unit's own name/business-object/current-desired condition ("Verify candidate documents — moves 'Candidate Document Pack' from 'Documents unchecked' to 'Documents verified complete, or offer blocked with a named reason'.") — and says "Backs nothing yet" for a file nothing cites, rather than a blank cell. A guest sees "No files in this walk," never a fabricated example standing in for a real upload; if the fabricated evidence pack is linked from this page, it is only ever a link to `OfferDeskEvidencePack.tsx`'s own already-bannered screen, with the same disclaimer echoed inline, never embedded here as if it were this tenant's own data.
    - **Three registers — missing / uncertain / contradictory — `lib/gapRegisters.ts` (new, frontend-only).** Buckets the eight real `GapKind` values (`undeclared`/`missing_acceptance`/`unimplemented` → missing, `split_recommended`/`missing_terminal_state` → uncertain, `policy_violation`/`workaround`/`shadow_process` → contradictory) read from the same `GET /discovery/gaps` the Gap screen already calls — no second detector, no coverage percentage anywhere on this page, counts only, zero is a real answer. A repair line per register actually present (never per file, never a Darwinbox/Zwayam API) states what would close a gap of that shape in plain process language. A guest has no tenant to query, so its three counts replay the same four illustrative rows already shown on Gap (`GAP_ROWS`), bucketed by which of the three shapes each row's own sentence describes — not a live query, not a second illustrative set invented for this page.
  - **F2 — `pages/CensusGap.tsx` is journey-wide now, not a count card linking out.** `pages/OfferDeskGap.tsx` stays reachable and byte-for-byte untouched (its own gate-kind table is duplicated, not imported, into `CensusGap.tsx`, so a future edit to one can never silently change the other). Three additions genuinely wider than one desk's sitting:
    - **Head vs doer**: `components/scout/ContradictionResolver.tsx`'s `sessionId` prop is now optional — the only change to that component, and the only change anywhere near the Contradiction Resolver's own detection/resolution logic (`services/scout_contradictions.py` is untouched). `ScoutInterview.tsx`'s existing call is unaffected (it still always passes a session id); `CensusGap.tsx` calls the same component with no session filter, so `GET /scout/contradictions` returns every Function-Head-vs-SME disagreement on the tenant instead of one session's.
    - **"What this journey cannot see"** is `OfferDeskGap.tsx`'s own card, generalized: the no-connector line now cites both desks' own real systems lists (`OFFER_DESK_META.systems`, `ONBOARDING_SPEC.systems`) rather than Offer Desk's alone, and the judgment-blind line stays attributed to Offer Desk's own sourced Exception 9 rather than inventing an Onboarding-side equivalent that was never interviewed.
    - **No fake KPI**: this screen computes no measured-vs-declared score for the journey. Where a real one already exists it links to where it lives, labelled declared or real, not repeated or blended here — Offer Desk's 95 hrs/mo on Hours, the family genome's real GQS on its own screen.
  - **Verified live, not asserted from reading the code.** This environment has no Docker daemon, so a native `postgresql-16` cluster was stood up on port 5433 with the same `wep`/`wep_app` roles and `wep` database name CI's own service container uses, migrations applied clean, `alembic check` reports no drift (this slice adds no model/migration — `GET /files` only reads existing tables). Backend suite **225 passed** (221 baseline + 4 new in `test_file_upload_sha256.py`: 401 with no key, an empty list for a fresh tenant, RLS isolation between two tenants' file lists via the real endpoint rather than a direct SQL check, and a file's real backing populated after a genome import that cites it — plus the same fixture proving a file nothing cites reports `backs: []`). Frontend `tsc --noEmit && vite build` clean. Playwright **9/9** (8 baseline unchanged + 1 new + the guest walk test extended): the guest walk now asserts step 3's "No files in this walk" and all three register labels render, and step 4's "Head vs doer" and the literal no-coverage-percentage sentence render, checking `localStorage`'s spec-key directly to prove neither step mints one just by being looked at; a new keyed test runs the real evidence-pack import, then confirms `GET /files` on `/census/evidence` shows a real file's real backing (`zwayam-candidate-export.csv` → a `WU-OD-*` code) and the one file that fixture never cites (`uan-service-history-sample.csv`) honestly reporting "Backs nothing yet." **One real bug this surfaced in the test itself, fixed before shipping, not glossed over**: Client A is a reused singleton tenant (same fact the pre-existing Ratify/Confirm-as-owner tests already branch on) — a second run of the new keyed test against an already-seeded Postgres hit `POST /genome/import`'s correct, existing `work_unit_id_already_exists` rejection instead of a fresh accept, exactly as `test_genome_import_conflicts.py` says it should. The test now tolerates either outcome (its own 7 uploads and the always-uncited orphan file are true either way) rather than assuming a clean database; re-run three times against a warm database and once more from a freshly recreated one, all green. Both pages were also screenshotted in a real headless-Chrome run, guest and keyed, not just asserted from a passing test.
  - **Did not do, per the brief's own refusal list**: no WorkOS, no Finance, no login, no Box 4, no pointer-resolver product, no delinquency register, no observed-pack theatre, no Render purchase, no new edge types, no Darwinbox/Zwayam API anywhere in the repair list's copy. Also did not: rewrite the Contradiction Resolver's detection logic, touch `OfferDeskGap.tsx`, invent a measured-vs-declared coverage percentage anywhere, or reopen CENSUS-v0/INTENT-PLAN.

- **Slice `CENSUS-PACK`: P1 — Census export (Plan + census shell) and P2 — Handoff completeness (Spec wall) — one PR.** Does not reopen CENSUS-v0/INTENT-PLAN/EVIDENCE-GAP: Chart, Plan's own units/Hours/Moderation, Evidence, and Gap are all untouched except for two mechanical extractions (below).
  - **P1 — "Download census" is a real, live-state markdown file, not a static template.** `lib/censusExport.ts` (new, frontend-only) assembles all 7 required sections from exactly what Evidence/Gap/Work Chart/Plan already fetch or already show — no new backend endpoint, no second scoring or gap detector. Guest gets the identical declared-schematic those screens already show a guest (`GUEST_JOURNEY`, `GUEST_REGISTER_COUNTS`, `GAP_ROWS`, every unit "not scored"); keyed gathers this tenant's real `GET /work-units/`, `GET /verdict/`, `GET /discovery/gaps`, `GET /files`, and the same get-or-create `POST /work-systems` Chart/Plan already call, on demand at click time (no eager fetch added to any census page that doesn't already need that data). The button lives both on the census shell (`CensusStepper.tsx`, all six steps) and, more fully, on Plan itself, per the brief's own "Plan (and census shell)."
    - **No invented numbers, including the one that would have been easiest to fake.** There is nowhere in this codebase that persists or reads back a live family-genome GQS score, so the export does not pretend to query one — it quotes the exact same qualitative, structural sentence Plan's own on-screen "Quality gate reminder" card states ("expected to fail GQS — around 30 of the 90-point gate... **~30/90 is not a pass**"), extracted once into `GQS_REMINDER_PRE/BOLD/POST` (`censusExport.ts`) and imported by `CensusPlan.tsx` so the on-screen sentence and the exported one are the same string, not two hand-kept copies.
    - **Banner, exactly as scoped, not printed unconditionally.** A guest export opens with a talk-only-empty banner. A keyed export adds a second banner line — "this pack is fabricated, not Rashmi's real data" — **only when** the tenant's own real `GET /files` response actually contains one of the seven known sample-pack filenames (`zwayam-candidate-export.csv` etc., from `offerDeskEvidencePack.json`'s own `files` array); a tenant that never ran "With evidence (sample)" gets no such line, because nothing fabricated is actually being exported for it.
    - **Item 7 ties directly to P2, not a separately invented "open questions" list.** The repair list reuses `gapRegisters.ts`'s own per-register repair lines (unchanged); the "not ready to hand off, and why" half calls the exact same `unitReadiness()` replay Plan's own Handoff column uses (see P2 below) over every matched unit on the tenant, so the export's own open-questions section can never silently disagree with what Plan shows on screen for the same tenant at the same moment.
  - **P2 — a Work Unit is Ready only if the backend itself says so, not just a UI badge.** New `GET /spec/handoff/{code}` (`routers/spec.py`, `services/handoff.py`, `schemas/handoff.py`) is the enforcement: **record exists** (a code with none reads `ready: false, bundle: null`, not a 404 — Plan needs to render "never imported" inline, the same honest-empty-state idiom this whole walk already uses) **and** a verification method is named (real, once a row exists — `models/workunit.py`'s own column default means this clause is a schema guarantee the moment (1) holds, stated as such in the service's own doc comment rather than presented as a second live check that could independently fail) **and** VERDICT has actually scored it, so its hard gates are *known* (an empty `gates: []` from a real score passes; the complete *absence* of a score does not — genome import alone never creates a `VerdictScore`, only a human scoring it via `/verdict` or a payload that ships one, same as Plan's pre-existing Moderation section already documents) **and**, only for the one code the sheet actually states a dual-employment stop on (`WU-OD-02`/`WU-OD-002` — Document check, sheet step 2), the live record's own persisted text (`failure_semantics`/`acceptance_criteria`/`evidence_required`/`context`) still says so; every other code is vacuously exempt from that fourth clause because no sheet requires it there. Refusal is the same 200-with-a-verdict-in-the-body idiom `POST /spec/check`'s own allow/deny already uses, not a 4xx — "Same deny as Spec without evidence," literally. Never executes the unit or sends it to an agent (P2's own scope line): a ready unit's `bundle` is its existing `WorkUnitOut` contract, read-only.
    - **Mirrored, not re-implemented, on Plan** (`lib/handoffReadiness.ts`), the same relationship `offerDeskScenarios.ts` already has with `services/verdict.py` — every unit Plan already loads gets a Ready/Not-ready badge with its real reason (`InfoTooltip`) with no per-unit round trip, plus one explicit "Check handoff bundle" control that calls the real endpoint and renders its raw JSON, so a visitor can see the server's own refusal, not only this replay. Guest: no button, no live check, just the rule stated in prose — "explain, never fake allow" — since a guest sitting has no real record for the rule to honestly evaluate.
    - **`ChartRow` (`lib/workSystemUnits.ts`) now also carries the raw `Verdict` row alongside the `ScenarioStrip` derived from it** — a one-line, additive change so Plan's Handoff column can read gates without a second lookup; nothing about `buildRows`'s existing matching or `scenarioStrip`'s existing math changed.
  - **Two purely mechanical extractions, verified behavior-identical, not a redesign of either page.** `CensusGap.tsx`'s `GATE_KINDS`/`KIND_COPY`/the two "cannot see" paragraphs moved into new `lib/gapGateKinds.ts`, and `CensusEvidence.tsx`'s `GUEST_REGISTER_COUNTS` moved into `gapRegisters.ts` — in both cases because `lib/censusExport.ts` needs the exact same constants, and every census page (including these two) renders the shell (`CensusStepper.tsx`) that now also renders the download button, which imports `censusExport.ts` — importing the constants back out of the page components would have created a real page→shell→button→export→page import cycle. Moving the constants down into `lib/` (pages already depend on `lib/`, never the reverse, everywhere else in this codebase) removes the cycle instead of relying on it happening to evaluate safely. Both pages' own JSX now reads from the moved constants unchanged; the CANNOT_SEE bullets keep their original bold-lead-then-body rendering via a lead/body split so no visible text changed.
  - **Verified live, not asserted from reading the code.** A native `postgresql-16` cluster stood up on port 5433 (same `wep`/`wep_app` roles, `wep` database name, no Docker daemon in this sandbox), migrations applied clean, `alembic check` reports no drift (no schema change this slice). Backend suite **232 passed** (225 baseline + 7 new in `test_handoff.py`: 401 with no key, not-ready with no record, not-ready with a record never scored, ready once scored with no dual-employment requirement, the dual-employment unit refused when scored but the stop text is missing, the same unit accepted once the stop text and a score both exist, and RLS isolation — tenant B reading tenant A's code sees "no record," not a leak). Frontend `tsc --noEmit && vite build` clean. Playwright, run once against a **freshly recreated** database (not the warm one this session had already run many times against — see the bug below) to match what CI's own ephemeral service container actually does: **12/12** (9 baseline unchanged + 3 new — a guest download containing 95, 61.8, and "not a pass"; a keyed download containing "confirmed by" once an intent is confirmed via the real API; and handoff refusing both in the UI, guest, "Not ready" everywhere, and directly from the server for a code no import path could ever produce).
    - **One real, pre-existing bug found while testing, not introduced by this slice and not fixed by it.** `POST /api/files/upload` (`routers/files.py`, shipped in Slice 1 PR 1a, untouched here) 500s with `sqlalchemy.exc.InvalidRequestError: Could not refresh instance` on its own `db.refresh(row)` call — but **only** when run as the ninth test in this file's existing sequence against a Postgres this same suite had already been run against several times in one long-lived local session; it passes every time (this test run included) against a database that has only gone through the sequence once, which is what CI's own per-run ephemeral service container always is. Root cause not chased further since it sits entirely inside EVIDENCE-GAP's own shipped upload path, out of scope for this slice's brief ("do not reopen EVIDENCE-GAP") — named here rather than silently worked around, so it isn't mistaken for something CENSUS-PACK's own changes caused, and so a real fresh-vs-warm-database distinction some future session hits isn't a surprise.
  - **Did not do, per the brief's own refusal list**: no WorkOS, no Finance, no Darwinbox, no login, no Box 4, no delinquency product, no Render purchase, no new Work Graph edge types. Also did not: execute a Work Unit or send one to an agent from the handoff bundle, add a PDF exporter (markdown only, per the brief), invent a live GQS number, or touch Evidence/Gap/Chart/Moderation/Hours beyond the two constant extractions and `ChartRow` gaining one extra field.

- **Slice `V10-1` BACKEND: `censuses` table + RLS + `GET`/`POST /api/censuses` (get-or-create + start), readiness booleans wired to EXISTING data.** Does not touch Chart/Plan/Spec, does not start V10-2 (no pointer columns, no resolver, no 5-status model), does not do the customer-copy/Scope-screen UI sweep — that is Cursor's V10-1 FRONTEND slice, unlocked only after this PR merges (`docs/V10_BUILD.md`).
  - **New table, not a repurposed one.** `censuses` (`models/censuses.py`, alembic `c1e81ae18515`) is deliberately a different module and route (`/api/censuses`, plural) from the pre-existing `routers/census.py` / `services/census.py` (`/api/census`, singular) — that older file is the "one-click function census" draft-VERDICT/cost feature and is untouched by this slice; the naming collision in English is real but the two are unrelated tables/routes/routers, not a rename of one into the other. Same RLS shape as the most recent tenant-scoped table (`work_systems`, `549fc2e9287a`): `client_id` direct column, `FORCE ROW LEVEL SECURITY`, `tenant_isolation` policy with `app.system_bypass` baked in from creation rather than bolted on later. One row per `(client_id, work_system_id)` — get-or-create by that pair, same idiom as `work_systems`' own `(client_id, code)` uniqueness, and never overwrites an existing row's `scope`/`document_requests` on a repeat `POST` (verified: a second `ensure` call with a different `scope` body returns the first call's row, unchanged).
  - **`scope` and `document_requests` are opaque JSON the API accepts and echoes back — this slice never interprets either.** They exist because `docs/V10_BUILD.md`'s own column list names them, for a future Cursor Scope screen to give shape to; a fresh census defaults to `scope: {}`, `document_requests: []` and nothing here reads either column to compute anything. This is the literal reading of "do not invent a document-request workflow" — the column exists, the workflow does not.
  - **Readiness is three honest booleans, each read live from a table this repo already had, on every response — never stored, never a fabricated "documents received" flag:**
    - `consent`: at least one `consent_receipts` row for this tenant with `status='active'` (the same table/status Slice 3 PR 3c's `POST /consent/receipts` and the purge sweep already use).
    - `people`: all **three** `scout_interview_sessions.type` values (`function_head`, `sub_function_lead`, `sme` — the three seats `README.md`/`CensusCapture.tsx` already describe) represented at least once for this tenant — deliberately not "any 3 sessions," which three SME sittings would satisfy without a single function-head or sub-function-lead seat ever sitting. Verified: two seats filled reads `people: false, people_seats_filled: 2`; filling the third flips it true without touching the other two signals.
    - `docs`: at least one `uploaded_files` row for this tenant (the same table `POST /files/upload`, server-hashed, already writes).
    - **Named limitation, not hidden:** none of these three tables carries a `work_system_id` column, so all three readiness signals are tenant-wide, not scoped to the one census's journey. A tenant running two censuses for two different Work Systems would see identical readiness on both. Honest given today's schema — this slice did not add a `work_system_id` column to `consent_receipts`/`scout_interview_sessions`/`uploaded_files` to narrow it, since only one Work System (`WS-OFFER-ONBOARD`) exists anywhere in this build and doing so would be schema change unrequested by the brief.
  - **`POST /{id}/start`** flips `status: draft → started` and stamps `started_at`, the same "keyed transition, real timestamp, survives refresh" shape as `work_systems`' own `ratify` — rejects a second call on an already-started row with 409, matching `ratify`'s own re-ratify rejection. No `started_by` name field: unlike `ratify` (a governance event, "V10 rule" per `models/work_system.py`), nothing in `V10_BUILD.md`'s text calls "Start census" a named signoff, so this slice did not invent one.
  - **Verified live, not asserted from reading the code.** This environment has no Docker daemon, so a native `postgresql-16` cluster was stood up on port 5433 with the same `wep`/`wep_app` roles and `wep` database name CI's own service container uses; `alembic upgrade head` applied the new migration clean on top of the existing chain, and `alembic check` reports no model/migration drift. Backend suite **240 passed** (232 baseline + 8 new in `test_censuses.py`: 401 with no key, get-or-create against a real Work System with every readiness boolean honestly false for a brand-new tenant, idempotent ensure that never overwrites, 404 on an unknown or cross-tenant `work_system_id`, start setting status+timestamp and surviving a fresh `GET`, start-twice 409, RLS isolation between two tenants' censuses via the real HTTP endpoints — cross-tenant start is 404, not a leak — and the readiness-flips-as-source-tables-gain-rows test above). Frontend untouched, so its own build/Playwright suite is unaffected by this PR — not re-run as part of this slice's own verification since nothing in `frontend/` changed.
  - **Did not do, per the brief's own refusal list**: no Chart/Plan/Spec rewrite, no V10-2 pointer/resolver/status work, no customer-copy or Scope-screen UI (Cursor's slice, locked until this merges), no fake "documents received" workflow, no WorkOS/Clerk login, no new edge types, no Darwinbox/Zwayam API.

- **Slice `V10-1` FRONTEND: Scope Start census + readiness strip + customer headings.** Does not add tables, does not start V10-2 (no pointer columns, no resolver, no 5-status badges), does not start V10-4 (no 18-leaf Chart). Home stays Census step 1.
  - **Start census (Scope only).** Guest: the button is visible, clicking it does not mint a key and does not call `GET`/`POST /api/censuses` (those 401 without a real tenant). Keyed: `POST /work-systems` get-or-create for `WS-OFFER-ONBOARD`, then `POST /api/censuses` get-or-create, then `POST /api/censuses/{id}/start`. Status `started` + `started_at` survive a refresh via a fresh `GET /api/censuses`. A second Start on an already-started row is not offered (the badge replaces the button); a 409 from a race re-reads GET rather than inventing a client-side started state.
  - **Readiness strip, three rows, honest red/green.** Consent / three people / docs. Green only when the matching boolean on the GET payload is true. Guest and keyed-with-no-row both render empty (all red, counts zero) — never a fabricated green. Docs copy says files uploaded, never "documents received"; the unused `document_requests` JSON is not interpreted. Named limitation restated in the i-button: the three signals are tenant-wide (backend's own named limit — none of the source tables carries `work_system_id`).
  - **Customer headings on every `/census/*` screen including Chart and Plan:** piece of work, how sure we are, how we know it, checked by, careful / as calculated / ambitious. Coined terms (Work Unit, VERDICT, provenance, verification, S1/S2/S3) live only in i-buttons. Chart's scenario tabs dropped the S1/S2/S3 parentheticals; Plan's existing unit columns were relabelled (piece of work / how sure we are / checked by) — no new table.
  - **Playwright (real backend, no mocked network):** guest Start → `1 of 6`, strip visible, walk 1→6, download still contains 95 and 61.8, `localStorage['we-spec-key']` stays null after clicking Start; keyed Start survives a reload. Spec deny and 95/61.8 logic untouched. Guest walk + download + Hours + Chart purpose-strip passed locally against `vite dev` with no key. The keyed Start-survives-refresh case is in the suite for CI (needs the wep cluster on 5433 + running API); this environment's Docker daemon was down and the app DB is not on the default 5432 service.
  - **Did not do:** no new Postgres table, no Alembic, no V10-2, no Chart 18-leaf, no login vendor, no Offer Desk as Home.

- **Slice `V10-2` BACKEND: field pointers + resolver-on-write + five statuses + binding-field enforcement.** Does not touch Census/Chart/Plan/Spec UI (Cursor's Evidence-UI slice, locked until this merges, per `docs/V10_BUILD.md`), does not start V10-3 (no `verification_design`, no 5th gate, no cert class), does not touch Darwinbox/Zwayam.
  - **New table, additive alongside the existing provenance table, not a replacement.** `field_pointers` (`models/pointers.py`, alembic `afca8777b3bd`) is one row per `(work_unit_id, field_name)` — deliberately separate from the pre-existing 1:1 `work_unit_provenance` (`WorkUnitProvenanceDetail`, written once at genome-import time by `services/genome_import.py`, which this slice does not touch or retrofit). The two coexist: a Work Unit imported via `POST /genome/import` still gets its existing unit-level `Provenance` (observed/declared/inferred/designed, `models/ontology.py`, unchanged); a caller can now *additionally* record a field-level claim (one of the five V10-2 statuses) for any of 12 substantive attributes via the new API, independent of how the unit itself was imported. "Reuse UploadedFile / existing provenance" is read literally: the new table FKs to the existing `uploaded_files` (no second file store) and follows the existing `work_unit_provenance`/`WORK_UNIT_CHILD_TABLES` RLS shape (`work_unit_id -> work_units.client_id`, `app.system_bypass` baked in from creation, same convention `censuses`/`work_systems` already use).
  - **Pointer = `file_id` + `page`/`line`/`cell` (`V10_BUILD.md`'s own three-part shape), resolved for real against the file's actual bytes, never trusted from the caller.** `services/pointers.py::resolve_pointer` dispatches on the cited file's extension:
    - **CSV/XLSX must store `cell`** (an Excel-style `"B7"` reference) — enforced, not just documented: a CSV/XLSX pointer with no `cell` fails to resolve. The referenced row/column is checked against the file's real parsed rows (reuses `services/classifier.py`'s existing CSV/XLSX row-reader, renamed `rows_from_bytes` — was already the parser `classify()`/`map_track_a()` use, so this is the same opener the rest of the pipeline already trusts, not a second one). If a `quote` was offered, it is checked as a literal substring of the resolved cell's real text (same "checked against the source, never trusted" rule `services/scout_story.py` already applies to LLM-extracted spans) — a cell that exists but doesn't contain the claimed quote does not resolve.
    - **PDF this PR is file-only, honestly.** No PDF parsing library exists in `requirements.txt` and none was added (real scope-creep risk, deliberately not taken) — so a PDF pointer can only claim "this file exists and has content," never a page or line: if a caller supplies `page`/`line`/`cell` against a PDF (or any other unparsed format), resolution fails outright rather than silently accepting an unverifiable page number. `routers/files.py`'s `ALLOWED_EXTENSIONS` gained `.pdf` (was CSV/XLSX-only) specifically so this path is real and end-to-end testable through the actual upload API, not a dead branch exercised only by a hand-built fixture — verified live: `classify()`/`map_track_a()` already degrade a non-spreadsheet-shaped file to `review_queue` rather than guessing, so an uploaded PDF queues honestly through those two pre-existing endpoints exactly as a garbage CSV would, unchanged.
  - **"If a pointer cannot be opened, that field is NOT a fact" is enforced as a downgrade, not a rejection, for the two evidence-claiming statuses.** `observed`/`reconstructed` are the only two of the five statuses V10-2 treats as a resolver-checkable evidence claim; a pointer written at either status that fails to resolve is **downgraded to `predicted`** in the same write (never silently kept at a status it can no longer support), a `ConformanceGap(kind=broken_pointer)` is raised citing the unit/field and the resolver's own plain-language reason, and the tenant's single `clients.fabrication_count` counter (`alembic 55df2bc65a11` — "not a 12-metric dashboard," one number) increments by exactly one. `declared`/`composed`/`predicted` on a non-binding field make no evidence claim a broken pointer could contradict, so a pointer attempt that doesn't pan out under one of those statuses is recorded honestly (`resolved: false`, `resolution_note` set) without flipping the status or raising a gap — a deliberate scope line, named here rather than left to be inferred: the rule is "a claimed fact that turns out not to be one gets corrected," not "every imperfect citation is fabrication."
  - **Binding fields (`authority`, `acceptance_criteria`, `actor_constraints`) are narrower still, and refuse rather than downgrade.** A write to one of these three is **rejected outright (422)** unless status is `declared`, a non-empty `quote` is given, *and* the resolver independently verified that quote as a literal substring of an opened CSV/XLSX cell — `predicted`/`composed`/`observed`/`reconstructed` are all rejected for these fields (`V10_BUILD.md`: "Reject predicted/composed as binding," read as the general case: only `declared`+verified-quote may bind). Nothing is ever saved in a downgraded form for a binding field, on purpose: a binding field silently persisted as `predicted` would still read as an ordinary field to anything that doesn't specifically check `requested_status`. A direct consequence, stated plainly rather than left implicit: **a PDF-sourced pointer cannot satisfy a binding field this PR**, since PDF resolution never verifies a quote — it can still back a *non-binding* field as honest file-level evidence.
  - **`field_name` is checked against a real, curated list, not accepted as an arbitrary string.** `POINTERABLE_FIELDS` (`services/pointers.py`) is the 18-attribute contract's substantive fields minus identity (`code`/`name`) and system-managed columns (`status`, `autonomy_level`, ...) — an unrecognized field name is a 422, not a silently-created row nobody will ever query by its real name.
  - **API is upsert-by-`(work_unit_id, field_name)`, backend-only this PR.** `POST /work-units/{id}/pointers` (new `routers/pointers.py`, mounted at the same `/work-units` prefix as the existing router in a separate file — same reasoning `routers/censuses.py` already gives for staying apart from `routers/census.py`: keeps this slice's diff independently reviewable) always re-resolves against the file's *current* content, whether or not a row already existed for that field; `GET .../pointers` and `GET .../pointers/{field_name}` back the click-a-claim screen Cursor's next slice builds. `ClientOut`/`GET /api/clients/` now also returns `fabrication_count` — the existing tenant-listing endpoint, not a new dashboard.
  - **Verified live, not asserted from reading the code.** This environment has no Docker daemon, so a native `postgresql-16` cluster was stood up on port 5433 with the same `wep`/`wep_app` roles and `wep` database name CI's own service container uses; `alembic upgrade head` applied all three new migrations (`field_pointers` table, `conformance_gaps.kind` gains `broken_pointer`, `clients.fabrication_count`) clean on top of the existing chain, and `alembic check` reports no model/migration drift. Backend suite **255 passed** (240 baseline + 15 new in `test_pointers.py`: 401 with no key, the broken-pointer-cannot-stay-observed case with its gap and fabrication-count assertions, the same downgrade for `reconstructed`, a CSV pointer missing `cell` failing to open, a valid cell staying `observed` with no gap raised, upsert-by-field idempotency, binding fields rejecting `predicted`/`composed`/`observed`/`reconstructed`, binding fields requiring a quote, binding fields refusing rather than downgrading a broken pointer, a verified-quote binding write succeeding, the PDF file-only case (resolves for a non-binding field, cannot satisfy a binding field, and a page-number request against a PDF is honestly refused), an unrecognized field name, a missing `file_id`, and RLS isolation via the real HTTP endpoints). Two of the 240 baseline files (`services/classifier.py`, `services/track_a.py`) needed a one-line reference update after `classifier.py`'s internal CSV/XLSX row-reader was renamed from `_rows_from_bytes` to `rows_from_bytes` so `services/pointers.py` could reuse it instead of writing a second parser — behavior-identical, covered by the unchanged classifier/track-A tests continuing to pass. **The downgrade logic itself was mutation-tested, not just exercised**: temporarily disabling the observed/reconstructed-to-predicted downgrade in `services/pointers.py` and re-running `test_pointers.py` fails exactly the four tests that depend on it (`test_broken_pointer_cannot_stay_observed`, `test_reconstructed_also_downgrades_on_broken_pointer`, `test_csv_pointer_without_cell_cannot_open`, `test_pdf_pointer_is_file_only_and_cannot_satisfy_binding`) and passes the other eleven, then the real code was restored and the full suite re-confirmed green — evidence the new tests actually catch the regression they're named for, not passing vacuously. Frontend untouched, so its own build/Playwright suite is unaffected by this PR — not re-run as part of this slice's own verification since nothing in `frontend/` changed.
  - **Did not do, per the brief's own refusal list**: no Census/Chart/Plan/Spec UI edit (Cursor's slice, locked until this merges), no Evidence click-a-claim screen, no Darwinbox/Zwayam API, no V10-3 (`verification_design`, cert class, 5th gate), no PDF text/line parser or new PDF dependency, no retrofit of `services/genome_import.py`'s existing unit-level provenance write path, no 12-metric fabrication dashboard (one counter).

- **Slice `V10-2` FRONTEND: Evidence click-a-claim.** Does not add tables, does not write a resolver, does not start V10-3, does not rebuild Chart 18-leaf. Home stays Census step 1.
  - **Click a claim → pointer or cannot open.** Census step 3 lists field claims from `GET /work-units/{id}/pointers` (keyed) or three walk-only document-check claims (guest — never Client A's files, never a live pointer). The detail pane shows file + cell/page/line only when `resolved` is true *and* the standing status is a fact (`observed` / `declared` / `reconstructed`, or binding `declared` + quote). Otherwise the copy is the required honest **cannot open**. The UI reads `status` (what the resolver let stand), not `requested_status`. A downgrade (claimed observed, stored predicted) is named as not a fact.
  - **Five statuses in customer words:** seen in records / said by a person / pieced together / proposed by us / predicted by a model. Canon names stay in i-buttons. **Composed** is a badge ("proposed by us — not adopted as a record"). Binding fields (`authority`, `acceptance_criteria`, `actor_constraints`) always carry "Must be said by a person" and never render "predicted by a model."
  - **Guest:** no `/work-units` or `/pointers` fetch (those 401 without a tenant). Clicking a walk-only claim shows cannot open and does not mint `we-spec-key`.
  - **Playwright (real backend, no mocked network):** guest walk 1→6 still, Evidence click leaves `we-spec-key` null; keyed walk is demo bootstrap → Start census → wait for `GET /work-units/{id}/pointers` → click. A real XLSX cell `B2` shows only when the pointer opened; a broken observed pointer (cell out of range, backend-downgraded to predicted) renders **cannot open**, never as a fact. The claims list waits for whoami before fetching, and loads pointers a few at a time so Chrome's per-origin socket limit does not starve the seeded unit behind the HR clone + Offer Desk units CI already wrote on Client A. `tsc --noEmit && vite build` clean. No Postgres table, no Alembic, no resolver code in this PR.
  - **Did not do:** no new table, no resolver, no V10-3 (`verification_design`, cert, 5th gate), no Chart 18-leaf, no login vendor, no Darwinbox/Zwayam, no Offer Desk as Home.

- **Slice `V10-3` BACKEND: verification design + certification + 5th gate, implementing `docs/contracts/v10-3-verify.md`.** That contract was published (983a875) after this slice's first push, which had been designed from `docs/V10_BUILD.md`/`docs/BUILD_PROGRAM.md`'s own terser wording alone (reusing `VerificationMethod`, a single merged table, a `predicted`-only/all-fields-scoped certification guard, a gate keyed off `VerdictScore.origin`). Reconciled against the contract before merge, in the same PR — this entry describes the reconciled, contract-matching version only. Does not touch Census/Chart/Plan/Spec UI (Cursor's Plan-columns slice, locked until this merges), does not start V10-4 (no Chart 18-leaf; `packs/hr/hire_leaves.yaml` is Cursor's declared-only seed, untouched here).
  - **Two new tables, one row per Work Unit each, not one merged table.** `verification_designs` (`models/verification_design.py`, alembic `f7d35a1c2c7b`) carries `method` — the contract's own seven-value vocabulary (`document_check` / `system_of_record` / `second_person` / `sample` / `reconcile` / `model_plus_human` / `none`), deliberately a *new* enum rather than reusing `models/workunit.py`'s `VerificationMethod` (a different, execution-time concept) — `independent` (`different_lineage` / `deterministic` / `no` / `not_stated`), `sampling` / `cost_of_check` (nullable free text, never imputed), and `dual_track` (always `true`, not a field on the write schema at all — "do may be delegated, check stays human. Never starts an agent" is structural, not a per-unit choice a caller can flip). `certifications` is genuinely separate — its own table, its own `PUT`/`GET /api/work-units/{id}/certification` — carrying just `class` (`sure` / `mostly_sure` / `reported_not_seen` / `cannot_define`; `class` is a Python keyword, mapped to the `cert_class` attribute but kept as the literal `class` column/wire name via a Pydantic alias, verified live to round-trip correctly through FastAPI's response serialization). Both RLS-shaped like `field_pointers` (V10-2): FK through `work_unit_id -> work_units.client_id`, `app.system_bypass` baked in from creation.
  - **Certification's one enforced rule, read literally rather than scoped to V10-2's `BINDING_FIELDS`.** The contract says "rejected if any **binding** field pointer status is `predicted` or `composed`" — but `services/pointers.py`'s existing binding-field enforcement already forces a binding field (`authority`/`acceptance_criteria`/`actor_constraints`) to be `declared` or absent; every other status, `predicted`/`composed` included, is **rejected outright at write time**. A guard scoped to `BINDING_FIELDS` could therefore never fire through this app's own API, and the contract's own required test ("`certification.class=sure` + predicted pointer → 422", no binding-field qualifier in the test itself) would be unsatisfiable. `services/verification_design.py::upsert_certification` instead checks **any** field pointer on the unit, any field: `predicted` (a guess) or `composed` (a person's judgement, not a quote) both block `class: "sure"` (422); a unit with zero pointers, or with `predicted`/`composed` never occurring at all, may still be certified `sure`. Verified live: broken-pointer-downgrades-to-predicted blocks it, a directly-posted `composed` status blocks it, and a clean unit with no pointers succeeds.
  - **5th gate, named `intent_guardrail` per the contract, computed at handoff time and never persisted onto `VerdictScore`.** The contract's literal handoff rule: `ready=false` if `method=none` (no `verification_designs` row at all reads identically to one whose row still says `method=none`) **or** ( `independent` is `no`/`not_stated` **and** this unit is one the contract names as always needing independence — offer-release and dual-employment). "Offer-release" is sheet step 5, "Rashmi triggers offer letter in Zwayam... TA Head signs first, then sent to candidate" (`offerDeskData.ts`) — `services/handoff.py::OFFER_RELEASE_CODES`, same two-code-shape reasoning (`WU-OD-05`/`WU-OD-005`) `DUAL_EMPLOYMENT_STOP_CODES` already uses for step 2. Deliberately **not** implemented as a change to `services/verdict.py::apply_hard_gates`/`recommended_level` (which backs `automation_index`/`census`/`projections`) and deliberately **not** keyed off `VerdictScore.origin` at all — the contract never mentions VERDICT's origin field; an earlier draft of this slice had assumed it did, before the contract existed to check against. Surfaced as `intent_guardrail` appended to `HandoffOut.gates` and an `independent_check_required` boolean (true exactly for offer-release/dual-employment codes) beside the existing `dual_employment_stop_required`. One real, load-bearing consequence verified live and **not** hidden: `gates` is no longer strictly "`null` until VERDICT-scored" — `intent_guardrail` can fire (and populate `gates`) before any VERDICT score exists, since it reads `verification_designs`, not `VerdictScore`; `test_not_ready_when_record_exists_but_never_scored` was updated to assert `gates == ["intent_guardrail"]` rather than `null`, and the "not yet scored" reason is still separately present so a caller isn't misled. Verified live: an offer-release unit (`WU-OD-05`) with a real method but no independence recorded is refused, the same unit is ready once `independent: "different_lineage"` is recorded, and a plain unit with no verification-design row at all is refused purely for `method=none` even though it needs no independence (`independent_check_required: false`).
  - **Dual-employment stop confirmed never liftable via moderation, not merely asserted from the existing code.** `models/moderation.py`'s own docstring already stated a logged S2→S3 opinion "cannot lift the dual-employment stop or any hard gate" — this slice adds the regression test that actually exercises it end to end: `WU-OD-02` scored and refused for missing the stop language, then `POST /api/moderation` requesting the most ambitious level (`to_level: 6`) with a real reason and name, then handoff re-checked and still `ready: false` for the same reason as before. No production code changed for this guarantee — it already held; now it is asserted, not just documented.
  - **Verified live, not asserted from reading the code.** This environment has no Docker daemon, so a native `postgresql-16` cluster was stood up on port 5433 with the same `wep`/`wep_app` roles and `wep` database name CI's own service container uses; `alembic upgrade head` applied the new migration clean both on top of the existing chain and from a genuinely empty throwaway database, and `alembic check` reports no model/migration drift either way. The generic `sa.Enum(..., create_type=False)` spelling the rest of this repo's migrations use for reusing an enum type only works when the type was created earlier in the *same* migration file; an earlier draft of this migration reused `verificationmethod` (the baseline migration's type, long since applied) that way and hit `DuplicateObject` — moot now that `method` is its own new enum, but the dialect-specific `postgresql.ENUM(..., create_type=False)` fix and finding are kept in mind for the next table that genuinely does reuse an old enum from a new file. Two pre-existing `test_handoff.py` cases needed their fixtures updated, not their assertions weakened, to keep testing what they always tested under the now-tightened rule (a real `verification_designs` row is required for *any* unit to read `ready=true`, dual-employment units additionally need `independent` recorded) — confirmed by first watching them fail for exactly that reason before adding the fix. Backend suite **268 passed** (255 baseline + 13 new: 9 in `test_verification_design.py` — missing-key 401 on both resources, no-design/no-certification-yet 404, defaults (`method: "none"`, `independent: "not_stated"`, `sampling`/`cost_of_check: null`, `dual_track: true`), idempotent upsert, `dual_track` not caller-settable, certification `sure` rejected on both predicted and composed, certification `sure` allowed with no blocking pointer, RLS isolation via the real HTTP endpoints — plus 4 in `test_handoff.py`: offer-release not-ready/ready-once-recorded, method=none blocking a unit that needs no independence, and the moderation regression above). Frontend untouched, so its own build/Playwright suite is unaffected by this PR — not re-run as part of this slice's own verification since nothing in `frontend/` changed.
  - **Did not do, per the contract's own refusal list**: no Census/Chart/Plan/Spec/CensusEvidence UI edit (Cursor's slice, locked until this merges), no Chart 18-leaf or V10-4, no Darwinbox/Zwayam API, no extra endpoint beyond the four the contract names, no invented check costs (`cost_of_check` stores exactly what a caller states, `null` otherwise), no change to `services/verdict.py`'s persisted `recommended_level`/`applied_gates` math or the dashboards built on it, no lifting of the dual-employment stop.

- **Slice `V10-4` FRONTEND: Work Chart 18-leaf + four bands including external + composite “the hire is complete”.** Does not edit backend, does not edit `CensusPlan.tsx`, does not add a 19th leaf, does not invent defended hours per leaf. Home stays Census step 1.
  - **Chart is the hire, not two desk lanes.** `CensusWorkChart.tsx` still carries purpose / Ratify / Confirm as owner. The Offer Desk + Onboarding step tables are replaced by `HireLeavesChart` (`components/census/HireLeavesChart.tsx`) reading `lib/hireLeaves.ts`, a faithful frontend seed of `packs/hr/hire_leaves.yaml`: exactly the 18 `WU-HIRE-01`…`18` names, bands automate / augment / human / **external**, composite name from that YAML. Parent stated hrs/mo (docs 38, fitment 14, trackers 19, offer 14, post-offer 10, handoff 5; request and exceptions 0) are a footnote of the sitting mix — 95 stated / 61.8 defended stay Offer Desk totals on Plan and on Chart's existing purpose strip, never printed on a leaf. Dual-employment stop on `WU-HIRE-05` still links to Document check; appetite does not lift it.
  - **Synthetic names.** Checked-by on the Chart (and the Chart snapshot in the census download) is stand-in people (Asha M., Kiran P., Meera V., Arjun S.) — not Rashmi, not Zwayam. A guest and a keyed tenant with no `WU-HIRE-*` Work Unit both read every leaf as not scored / declared seed. Matching a real `WU-HIRE-*` code, if a later slice persists one, still replays the same `scenarioStrip()` Document check already uses.
  - **Playwright (real backend, no mocked network):** guest 1→6; Chart shows 18 leaves + an external band whose three leaves are the YAML's own external rows (the test reads `packs/hr/hire_leaves.yaml` and asserts every name); Chart leaf region contains none of Rashmi/Zwayam; Plan still shows 95 and 61.8; guest download still contains 95, 61.8, “not a pass”, plus the composite and the external band label, and no `WU-HIRE-19`. `tsc --noEmit && vite build` clean. Against this machine's already-warm Postgres the full census suite was **12/14**: the four slice-required cases plus Ratify / Confirm / moderation / family GQS / Spec deny / Evidence file list / keyed download / handoff all passed. The two failures are the same pre-existing warm-tenant shapes this file has already named — Start-census click hanging on a reused Client A row, and `POST /work-units/{id}/pointers` 500ing (`Could not refresh instance`) after many prior uploads — not Chart, and CI's ephemeral Postgres is the 14/14 path.
  - **Did not do:** no Postgres table, no Alembic, no Plan columns (how sure / checked by / independent — next Cursor slice after V10-3 GET), no Outcome Record UI (Claude V10-5), no Darwinbox/Zwayam API, no lifting the stop.
- **Slice `V10-5` BACKEND: `outcome_records` table + `GET`/`PUT /api/work-systems/{id}/outcome`, the Claude half of `docs/V10_BUILD.md`'s "Gap 3 tiers + Outcome" slice.** Does not touch Cursor's Tier 1/2 Gap UI, Chart, Plan, Evidence, `packs/hr/hire_leaves.yaml`, or `docs/NEXT.md` — all untouched by this PR, per the brief's own refusal list.
  - **One row per Work System, same indirect-RLS shape as `verification_designs`/`certifications` (V10-3), not a direct-`client_id` table like `work_systems`/`censuses`.** `outcome_records` (`models/outcome.py`, alembic `0b996f6d42b7`) carries exactly the brief's own four columns — `work_system_id`, `promised` (text), `measured` (nullable text), `status` (`not_measured` / `measured`) — plus the usual timestamps; no `client_id` column of its own, tenant isolation enforced via `work_system_id IN (SELECT id FROM work_systems WHERE client_id = ...)` with `app.system_bypass` baked in from creation, the same convention every table since `68c3926e1143` follows.
  - **`promised` is copied once from `WorkSystem.outcome` and never rewritten by this table's own API.** `WorkSystem.outcome` is the pre-existing "sitting outcome sentence" (`"Candidate handed over, Day-1 ready"`), itself sourced client-side, verbatim, from Offer Desk/Onboarding meta at work-system-ensure time — untouched, unread-differently, and not re-derived here. Nothing in `schemas/outcome.py`'s write model even has a `promised` field for a caller to set.
  - **`GET` never 404s — it auto-vivifies the honest default.** `services/outcome.py::get_or_create` creates the row on first touch (`promised` = the Work System's own outcome sentence, `measured: null`, `status: not_measured`) rather than requiring a separate create call the way `verification_designs`/`certifications` do; a second `GET` returns the identical, unchanged row. This is the literal reading of the brief's own default line, not a new invented convention — "not yet measured" is real, persisted state from the first read, not a computed stand-in the caller has to special-case.
  - **"Never invent 62%" is an enforced write-time rule, not a comment.** `PUT` is the only path that can move `status` to `measured`, and it requires two separately-validated fields — `measured` (must contain a real digit; free text with no number, e.g. "went great, on schedule," is rejected) and `source` (must be non-blank after stripping whitespace) — both required by the schema itself (missing either 422s before the service even runs). A rejected `PUT` leaves the row exactly as it was (`not_measured`, `measured: null`) — verified live, not assumed. The brief's own column list has no separate `source` column, so a successful write composes both caller-supplied values into the one `measured` text column (`"58% Day-1 ready (source: Payroll export, Sep 2026)"`) rather than silently discarding the source or inventing an unlisted column.
  - **Verified live, not asserted from reading the code.** This environment has no Docker daemon, so a native `postgresql-16` cluster was stood up on port 5433 with the same `wep`/`wep_app` roles and `wep` database name CI's own service container uses. `alembic upgrade head` applied the new migration clean both on top of the existing chain and, separately, from a genuinely empty throwaway database created solely to check this; `alembic check` reports no model/migration drift either way; the migration's own `downgrade`/re-`upgrade` round-trip was also run and left the schema clean. The one substantive guard (measured must contain a digit) was mutation-tested: disabling the check in `services/outcome.py` and re-running `tests/test_outcome.py` failed exactly one test (`test_put_measured_rejects_text_with_no_number`) and left the other seven green, confirmed real before the code was restored and the full suite re-run clean. Backend suite **276 passed** (268 baseline + 8 new, all in `tests/test_outcome.py`: 401 on both `GET`/`PUT` with no key, keyed `GET` defaulting to `not_measured` with the real outcome sentence as `promised`, that default being idempotent across a repeat `GET`, `PUT` rejecting a non-numeric `measured` / a blank `source` / either field missing entirely (three cases), a successful `PUT` persisting and leaving `promised` untouched, and RLS isolation via the real HTTP endpoints — cross-tenant `GET`/`PUT` both 404, not a leak). No orphaned test data left in the dev database afterward (checked directly). Frontend untouched, so its own build/Playwright suite is unaffected by this PR — not re-run as part of this slice's own verification since nothing in `frontend/` changed.
  - **Did not do, per the brief's own refusal list**: no Cursor Tier 1/2 Gap UI, no Chart/Plan/Evidence edit, no `hire_leaves.yaml` or `NEXT.md` edit, no V10-4 leaf work, no fabricated or default-guessed `measured` value under any circumstance (the only text ever written there is a caller's own `PUT` payload, verbatim), no extra endpoint beyond the one `GET`/`PUT` pair, no `source` column beyond what the brief's own four-column list named.

- **Slice `V10-5b` BACKEND: three gap tiers (process / journey / outcome) on the existing `conformance_gaps` table.** Does not touch the frontend at all, does not rebuild Chart, Plan columns or 95/61.8 (Cursor's V10-6, already on `main`), does not edit `docs/NEXT.md`, and does not start V10-7. `docs/API.md` is the only non-backend file changed, for the one row describing the endpoint this slice altered.
  - **One additive column on the table the brief named, not a fourth gap table.** `conformance_gaps.tier` (`models/discovery.py::GapTier`, alembic `d4e5f6a7b8c9`), values `process` / `journey` / `outcome`, `NOT NULL DEFAULT 'process'`. Every gap that already existed is a Tier 1 process gap, so there is no honest "tier unknown" state to model and the column is not nullable — verified by inserting a gap row against the pre-`tier` revision on a throwaway database and then upgrading: the legacy row came back `('undeclared', 'process')`, not null. Unlike `severity` (deliberately a plain string, so manual triage isn't blocked on a migration), `tier` is a real Postgres enum, because three tiers is closed canon and a fourth would be a change to `docs/V10_BUILD.md`, not a triage call. Two new `GapKind` values (`missing_handoff`, `outcome_not_measured`) in their own additive revision `c3d4e5f6a7b8`, same `ADD VALUE IF NOT EXISTS` / no-drop-on-downgrade shape as Gate 6's and Gate 9's.
  - **A gap's tier is a pure function of its kind, so the column cannot lie.** `models/discovery.py::tier_for_kind` is the single mapping; a test asserts it is total over `GapKind` and that only the two new kinds are non-`process`. `POST /discovery/gaps` derives `tier` from `kind` and `GapCreate` has no `tier` field for a caller to set — a caller-labelled "journey" gap on an arbitrary kind would make the tier unreadable. `GET /discovery/gaps` returns `tier` and takes an optional `?tier=` filter; omitting it returns all three, so no existing caller changes.
  - **Tier 1 (process) is the three gates that already existed, unchanged.** `undeclared` (Gate 10), `split_recommended` (Gate 6), `missing_terminal_state` (Gate 9) now name `tier=process` explicitly at their write sites so the three tiers read side by side in one file; their logic, thresholds, counts and result keys are untouched, and the 276-test baseline still passes unmodified.
  - **Tier 2 (journey) reads a real cross-desk edge, it does not count units.** For each adjacent desk pair in the Work System's own lane order (`services/work_system.py::WORK_SYSTEM_DESK_LANES` — Offer Desk then Onboarding, the backend half of what `frontend/src/lib/workSystemUnits.ts` already declares), a handoff exists only if some `WorkEdge` actually runs from an upstream-desk unit into a downstream-desk unit. Desks are matched by the code prefixes this repo's own import paths really produce (`WU-OD-` / `WU-OFF-` for Offer Desk, `WU-ONB-` for Onboarding) — no desk column was invented on `WorkUnit`. The trailing hyphen is load-bearing and tested: `WU-OFFB-` is Offboarding, a desk explicitly outside this Work System, and a bare `WU-OFF` prefix test would swallow it. Three deliberate restraints, each with its own test: a pair is only evaluated if this import touched one of its desks (a Finance genome delivers no verdict on the HR journey — same restraint Gate 9 shows); an upstream desk with zero units is an empty inventory, not a broken journey; and the seam is reported once, not re-flagged per import, because its subject is the Work System rather than this batch. Nothing writes to `work_systems`, no edge is created to "fix" the seam, and no journey owner is invented for it.
  - **Tier 3 (outcome) is "promised vs not measured", and there is no code path that could invent the measurement.** A Work System whose `outcome_records` row says `not_measured` — or which has no row at all, since `services/outcome.py::get_or_create` vivifies exactly that default on first read, so absence and `not_measured` are the same claim — gets one gap quoting only the tenant's own declared promise (`WorkSystem.outcome`) and stating that nothing measured backs it. No number, no percentage, no estimate appears in it. `_flag_outcome_not_measured` has no write access to `OutcomeRecord.measured`, creates no `outcome_records` row while reading one (asserted: `OutcomeRecord` count stays 0), and a test re-reads the record after the flagging import to confirm it is still `not_measured` / `measured: null`. A tenant with no Work System row has promised nothing, so no gap is written — warning there would mean inventing the promise as well as the gap. A real measurement (the only route being `PUT /work-systems/{id}/outcome` with a number and a named source) closes the gap.
  - **GQS is still the only reject.** All three tiers warn. A genome that fails GQS is rejected before any tier runs and writes no gap of any tier — including the outcome gap, which does not depend on the batch's units at all — asserted directly. On the accepted path, the import that raises all three tiers still returns `accepted: true` with an empty `violations` list and its full work-unit count.
  - **Verified live, not asserted from reading the code.** A real `postgres:16-alpine` container on port 5433 with the same `wep`/`wep_app` roles CI's own service container uses. `alembic upgrade head` applied both new revisions clean on top of the existing chain and, separately, from a genuinely empty throwaway database; `alembic check` reports no model/migration drift in either case; the `downgrade`/re-`upgrade` round-trip was run and left the schema clean (column and `gaptier` type both gone at the bottom, both back and correct at the top). Every new check was mutation-tested, each mutant confirmed to fail real tests before the code was restored: pretending a handoff always exists (5 failures), treating a measured outcome as unmeasured (1), dropping the touched-desk restraint (1), dropping the once-only rule (2), mislabelling the journey tier as process (3), mislabelling the outcome tier (4), and widening the Offer Desk prefix to swallow `WU-OFFB-` (3). The first pass of the touched-desk mutant survived — the test as first written was actually exercising a different guard — so that test was rewritten to seed both lanes from a non-import path, leaving the seam genuinely open and unreported, and the mutant then failed as it should. Backend suite **296 passed** (276 baseline + 20 new in `tests/test_v10_5b_gap_tiers.py`), the baseline count unchanged from the V10-5 entry above. The real-import tests run the genome this repo actually ships (`samples/Private-Genome-MVP-HR-Ops-FIXED.json`, the same file CI's RLS test imports over HTTP): untouched it has a real Offer-Desk-to-Onboarding handoff (`WU-ONB-10` depends on `WU-OFF-04`), so the journey tier correctly stays silent while process and outcome both fire; with its Onboarding lane not yet imported — a real tenant state — one import produces all three tiers at once. RLS was checked through the real HTTP endpoints on real Postgres: tenant A's process, journey and outcome gaps are each invisible to tenant B through the same `?tier=` filter, `total: 0` and an empty list, not a redacted count. Frontend untouched, so its build and Playwright suite are unaffected and were not re-run.
  - **Did not do, per the brief's own refusal list**: no frontend change of any kind (no Gap/Chart/Plan/Evidence/Census UI), no `docs/NEXT.md` edit, no rebuild of Chart, Plan columns or 95/61.8, no invented measured KPI anywhere (no code path writes `OutcomeRecord.measured`), no new table, no Darwinbox/Zwayam API, no change to GQS or to any existing gate's logic or counts, no auto-created edge or auto-assigned journey owner to "close" a journey gap, no lifting of the dual-employment stop.

- **V10-8: field ratify + decision cards** (`docs/contracts/v10-8-field-ratify.md`, backend only). A new `field_ratifications` table (RLS, same `client_id` + `tenant_isolation` policy shape as `work_systems`) holds one draft-vs-confirmed row per `(work_unit, field)` for exactly three reused columns — `work_units.desired_condition`, `authority`, `acceptance_criteria` — never a fourth place those values live. `POST /api/work-units/{id}/field-ratifications` is get-or-create by `(unit, field)`: a repeat call returns the standing draft unchanged (never overwrites it with a later caller's quote/value), and a settled (`confirmed`/`corrected`) row 409s instead of drafting again. The quote rule is exactly the contract's: when a `declared` `field_pointers` row already exists for that unit+field, `sitting_quote` must be a literal Python `in` substring of the pointer's own quote (422 otherwise) — no LLM call, no invented pointer, reusing V10-2's existing evidence table rather than a parallel check. `POST .../{rid}/confirm` takes `action: confirm|correct`; `confirm` copies `drafted_value` verbatim onto `confirmed_value` and the Work Unit column; `correct` requires a `value` that differs from the draft (422 if equal) and writes that instead; either way it's a one-time transition (409 on a second call) and every write is `AuditLog`'d. `desired_condition` is clipped to 80 chars at write (reusing `genome_import._clip`, the same helper the genome-import path already uses for the same column) — `authority`/`acceptance_criteria` are `Text` and need no clipping. `GET /api/work-units/{id}/decision-cards` reads existing `conformance_gaps` rows only (no new `GapKind`, no scan, no invented pairing of a card to a field — the contract's own "Cursor will pair cards to fields later"); `named_human` is `work_units.owner` verbatim or the literal string `"not named"` when empty — never a fabricated name. A unit with zero gaps returns `[]`, asserted directly, not just left untested.
  - **Dual-employment stop, checked not just assumed unliftable.** `services/handoff.py`'s stop check reads `failure_semantics`/`acceptance_criteria`/`evidence_required`/`context` for the literal text "dual employ"; confirming `authority` (not one of those fields' checked set, though `acceptance_criteria` is one of the three ratifiable fields) writes only the single named column via `setattr(work_unit, field_name, ...)` — nothing else on the row moves. `test_confirm_of_authority_does_not_lift_dual_employment_stop` builds `WU-OD-02` with the stop language already absent, confirms `authority` through this new path, and re-reads `/api/spec/handoff/WU-OD-02` before and after: `dual_employment_stop_required` stays `true` and the missing-stop reason is still in the list both times. No code path in this slice touches the stop language at all, let alone lifts it.
  - **RLS verified over real HTTP, not asserted from the model.** Tenant B gets a 404 (not a redacted 200 or empty list) reading tenant A's `field-ratifications` or `decision-cards`, and a 404 attempting to confirm A's row by id — `tests/test_field_ratifications.py::test_rls_field_ratifications_isolation_via_http`. A raw same-connection `INSERT` bypassing the get-or-create service path still hits the DB-level `uq_field_ratifications_client_unit_field` unique constraint.
  - **Verified live, not asserted from reading the code.** `alembic upgrade head` applied the new revision clean on top of the existing chain and, separately, from a genuinely empty throwaway database (`wep_fresh_check`, same `wep`/`wep_app` roles CI's own service container uses); `alembic check` reports no drift in either case. Backend suite **309 passed** (296 baseline + 13 new in `tests/test_field_ratifications.py`), baseline unchanged.
  - **Did not do, per the contract's own refusal list**: no frontend of any kind (no Chart, Plan, Gap UI, `CensusPlan.tsx`), no new `GapKind`, no scan of `conformance_gaps` beyond a plain read, no touch to Darwinbox/Zwayam, no login work, no V10-9, no LLM call anywhere on this path, no invented measured KPI, no lifting of the dual-employment stop, no GQS change, no rewrite of the Offer Desk Rashmi rows. This is not Work System ratify and does not extend `confirm-function-intent` / `confirm-work-system-intent` — both untouched.

- **Slice `V10-8` FRONTEND: sit close — three rows (goal / authority / acceptance) beside the person's words, Confirm / Correct, decision cards.** Does not edit Chart, Plan 95/61.8, Gap buckets, Spec deny, or any backend table. Census stays six steps; sit close is Offer Desk depth after Playback (`/scout/offer-desk/sit-close`), not a seventh census pill.
  - **Three rows, customer words on glass.** Each row shows what they said (the sitting quote) and the draft, then Confirm and Correct. Quotes and drafts are verbatim from Rashmi's sitting / sheet (`offerDeskData.ts` step 2, `DOCUMENT_CHECK_RECORD`) — not paraphrased. Canon names (Work Unit, field ratify, `desired_condition`) live in i-buttons only. Confirm / Correct POST `/api/work-units/{id}/field-ratifications/{rid}/confirm` keyed only, against `WU-OD-02` when that piece of work exists on the tenant. A declared field pointer's own quote is reused when present so the substring rule cannot 422 on an invented line.
  - **Decision cards are GET `/decision-cards` or “none yet”.** Empty list is the literal words `none yet`. No stand-in Rashmi / Asha / CHRO card. A real card shows `named_human` from the API (the piece of work's owner, or `not named`) and is tappable to fill “Who is confirming”; no `field_name` is guessed onto the card (contract: pair later).
  - **Guest: look only, banner, no `we-spec-key` minted.** Guest never calls field-ratifications or decision-cards. Confirm / Correct stay disabled. Playwright: guest sit close three rows + `none yet` + null key; census still `1 of 6` … `6 of 6`; Plan still 95 and 61.8. Keyed Confirm of goal persists across reload when a real `WU-OD-02` exists.
  - **Did not do:** no Chart 18-leaf edit, no Plan columns or 95/61.8 rewrite, no Gap three-bucket edit, no Spec deny change, no Postgres / Alembic, no seventh census step, no V10-9, no lifting the dual-employment stop.

- **V10-9 BACKEND: strategy intent (third intent level) + intent debt.** Reuses the existing D — INTENT-LITE mechanism on `work_systems` (`function_intent_*` / `work_system_intent_*`, alembic `ac2a876395e4`) rather than a new table or a new identifier scheme — `docs/INTENT_CONTRACT.md`'s "no INT-007, or any other, new identifier or table for intent records" still holds, and this is not that: four additive columns on the row that already carries the other two intents, same shape.
  - **`strategy_intent_focus` / `strategy_intent_owner`** (alembic `e1a2b3c4d5f6`) — the one-line "what this period is about" sentence plus a named human or an explicit stand-in label, set once at `POST /api/work-systems` ensure time and never overwritten by a later call (same rule the other two intents, and the row's own `entry`/`exit`/`owner`/`outcome`, already follow). `POST /api/work-systems/{id}/confirm-strategy-intent` (new, `WorkSystemIntentConfirmIn` — name + server timestamp, nothing else) is the one-time draft→confirmed transition, identical in shape to `confirm-function-intent`/`confirm-work-system-intent`: 409 on a second call, `AuditLog`'d, the other two intents untouched by confirming this one (and vice versa) — asserted directly, not assumed.
  - **"Unowned goals are dashed and counted," both halves literal.** `IntentOut.owner` reads `"—"` rather than an empty string when an intent's own owner field is blank (`services/work_system.py::_owner_display`) — applies uniformly to all three intents, not just the new one, since a blank owner was already representable (just silently) before this slice. `WorkSystemOut.intent_debt` (new) is a single integer per row: how many of the three intents (function / work system / strategy) currently have no owner — 0 to 3, computed fresh on every read, nothing persisted. Not a dashboard: one field, on the existing list/ensure/confirm responses, no new endpoint. Mutation-tested: disabling the dash (`_owner_display` returning the raw value) and the count (`_intent_debt` returning `0`) and re-running `tests/test_work_systems.py -k dash` fails `test_unowned_goals_are_dashed_and_counted` for exactly that reason, confirmed before the code was restored and the full suite re-run clean.
  - **Guardrails were not touched, on purpose.** V10-3's 5th gate, `intent_guardrail` (`services/handoff.py`, `docs/contracts/v10-3-verify.md`), is unchanged — this slice adds no new gate, does not read or write `verification_designs`/`VerdictScore`, and does not alter `services/verdict.py`'s `apply_hard_gates`/`recommended_level` math or anything built on it (`automation_index`, `census`, `projections`). "Guardrails pass down to the existing 5th gate" is a refusal, not a feature: nothing in this PR needed to build one.
  - **Verified live, not asserted from reading the code.** No Docker daemon in this environment, so a native `postgresql-16` cluster was stood up on port 5433 with the same `wep`/`wep_app` roles and `wep` database name CI's own service container uses. `alembic upgrade head` applied the new migration clean both on top of the existing chain and, separately, from a genuinely empty throwaway database (`wep_fresh_check`); `alembic check` reports no model/migration drift either way. Backend suite **314 passed** (309 baseline + 5 new in `tests/test_work_systems.py`: confirm-strategy-intent setting owner/timestamp without touching the other two intents, confirm-twice 409, confirm requiring a non-blank name, cross-tenant confirm 404 (RLS), and the dash/debt test above), plus updated assertions in the existing `test_ensure_creates_candidate_row` for the new drafted `strategy_intent` and `intent_debt: 0` when every intent is owned. No orphaned test data left in the dev database afterward (checked directly).
  - **Did not do, per the brief's own refusal list**: no frontend of any kind, no Chart/Plan (95/61.8)/Gap UI edit, no login, no `INT-007` or any new intent table/identifier, no rewrite of `services/verdict.py` or the existing `intent_guardrail` gate, no COO-deck harvest or aggregate/reporting endpoint (`intent_debt` is a per-row integer already on an existing response, not a new dashboard), no `V10-10` work.

- **Slice `V10-9` FRONTEND: one Plan row for this period — focus, owner or dash, unowned-lines count.** Does not edit Chart 18-leaf, Plan 95/61.8 numbers, Gap three buckets, Sit close, Spec deny, or any backend table. Census stays six steps.
  - **One row on Plan, customer words on glass.** `This period:` plus the one-line focus, the owner (or `—` if unowned), and `unowned lines: N` from `intent_debt`. Canon names (strategy intent, intent debt) live in i-buttons only. Not a new page and not a dashboard.
  - **Keyed Confirm as owner POSTs `/api/work-systems/{id}/confirm-strategy-intent`.** Guest looks only, never mints `we-spec-key`, no confirm control. First keyed ensure sends the same period-focus draft the backend tests already persist.
  - **Playwright:** guest 1→6; Plan still 95 and 61.8; the new row is visible; guest download includes `This period:` and `unowned lines:`. Keyed confirm on that row persists across reload when the journey row exists.

Update this file in the same PR any time a row moves from "specified" to "enforced."
