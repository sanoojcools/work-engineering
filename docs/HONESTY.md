# Scout Import Path — Honesty Matrix

Enforced today vs specified-but-not-enforced, for `POST /api/genome/import`'s `WorkUnitImport` schema. Source: `Scout-Reference.md` (corrected 2026-08-26). Do not add a row claiming a validator that has no matching test.

| Attr | Enforced today | Specified, not enforced |
|---|---|---|
| id | `str`, min 1, max 40 | `^WU-[A-Z]+-[0-9]+[A-Z]?$` |
| name | min 1, max 200 | verb-noun format |
| business_object | min 1; auto-creates `EntityType` | pack ontology allow-list |
| current_condition | min 1; clipped to 80 + ellipsis at write | — |
| desired_condition | same clip | — |
| context | `{decision_branches, variants[]}`, extra=forbid | — |
| trigger | min 1 | verb-content check |
| input | `list[str]` min 1 at API; joined to text col | native array column |
| authority | min 1; PII-scanned | — |
| actor_constraints | min 1; PII-scanned | — |
| acceptance_criteria | `list[str]` min 1 at API; joined | native array column |
| evidence_required | same; PII-scanned | native array column |
| verification_method | free text → enum heuristic, default `human_spot_check` | strict input enum |
| sla_timing | structured, all fields optional, extra=forbid; `time_per_case_min` mapped to `sla_hours`, `volume_per_month` persisted as-is (Slice 2 PR 2c) | `gt=0` on time; `frequency` / `sla_deadline` still not read back |
| dependencies | `list[str]`, default []; unresolved = skip edge | schema/FK reject |
| failure_semantics | min 1 | — |
| regulatory_register_link | `list[str]`; upsert stub on `regulatory_entries.regulation` | `RR-` prefix; must-exist FK |
| provenance | extra=forbid; only `source_type` pattern-validated `^(observed\|declared\|inferred\|designed)$` | `hash_sha256` `^[a-f0-9]{64}$`; server-computed hash; `file_name`; consent existence |

Also true, not a per-attribute row:

- Container `GenomeImportRequest` is `extra="ignore"` — benign top-level metadata (`source_files`, `automation_index`, a stray `org`) does not 422; GQS scores it instead.
- GQS runs on the raw dict, before Pydantic validation.
- `dual_scoring_kappa` is caller-supplied. No mechanism produces two independent scores to compute it from.
- No file writer exists yet for `UploadedFile` — no endpoint uploads a file, so `hash_sha256` in provenance today is whatever the caller supplied, never server-computed.
- Slice 3 PR 3a: `work_units`, `spec`, and `census` are off the legacy global `spec_api_key` (default `dev-spec-key-change-me` is gone, not just changed) and onto per-org `X-Spec-Key` / RLS, same as genome and files. `POST /api/org/keys/rotate` rotates a key; the old one 401s after a 60-minute grace window (`dependencies.ROTATION_GRACE_MINUTES`). `ontology`, `discovery`, `projections`, `verdict`, `work-graph`, `economics`, `regulatory`, `verification`, `clients`, and `admin` are still unauthenticated — that's out of PR 3a's scope (playbook G.1 names work_units/spec/census only), not a gap this PR closed. **They are still open, but no longer non-deterministic.** They run as `wep_app`, which RLS binds, and nothing set `app.current_client_id` for them, so they answered from whatever the pooled connection happened to be carrying from a previous request — in practice nothing, which is why Projections rendered `0 inventory / 0 work graph edges` for a tenant holding 14 work units and 20 edges, and occasionally a *previous caller's* tenant. `discovery`, `projections`, `verdict`, `work-graph`, `economics` and `verification` now take `OptionalTenantDbDep`: an `X-Spec-Key`, when one is presented, pins the request to that tenant; a missing or unrecognised key is ignored rather than rejected, so these routes stay open exactly as before. This narrows what a request can see, it does not widen it, and it is not authentication — a caller with no key still gets a 200. `ontology` is untouched (`entity_types` is global, not per-tenant); `clients` and `admin` are cross-tenant by nature. Covered by `tests/test_projections_tenant_binding.py`, including the guard that these routes must never start returning 401.
- Slice 3 PR 3c: `consent_receipts` (P0 foundation table, RLS since alembic `9a07306c5434`) now has its first HTTP surface — `POST/GET /api/consent/receipts`, `POST /api/consent/receipts/{id}/revoke`, per-org `X-Spec-Key`, same RLS isolation as work_units. `purge_expired_consents()` (daily sweep, triggered manually today via `POST /api/admin/consent/purge`) tombstones `status=active` receipts past `expires_at`. **This reports 0 purged in any real environment today** — no caller writes a consent receipt from an actual interview/transcript flow yet (`genome_import.py` still hardcodes `consent_receipt_id=None`); the only way to see `purged: 1` is the test suite's direct-DB backdated fixture. The job and its RLS-bypassing cross-tenant scan are proven correct; the upstream wiring that would make it matter in production (genome import creating a real receipt at interview time) is not built.
- 15 No-Cracks Gates: #15 (RLS) is fully live. ~6 are partially live. ~8 are specified-only. GQS ≥ 90 means the quality gate passed — it does not mean 15/15 gates passed.
- **Scout Elevated V2 PR1** (`scout_interview_sessions` / `scout_captured_units`, `routers/scout.py`): a founder/SME interview session, a live-editable Work Capture Grid, and a Genome Strength completeness meter are real — tenant-scoped via `X-Spec-Key`/RLS, browser-tested end to end (session create → 401 → key banner → retry → add row → live meter update → inline edit → persisted). None of the "adaptive AI follow-up" or "words become structure live" behavior the design doc describes runs on an LLM — `LLM_PROVIDER=none` in this environment. `DiscoveryPartner`'s question bank is a static, hand-written list per track (founder/SME), not AI-generated or AI-adaptive; its voice input is the browser's own `SpeechRecognition` API (dictation only, works with no backend LLM, degrades to typing where unsupported) — it is not an AI interviewer. Completeness is computed from structured fields the interviewer/interviewee types or dictates into the grid directly; nothing extracts structure from free-text notes automatically. The design doc's `org_id` naming was corrected to `client_id` throughout (this repo's actual tenant column — see `SCOUT_OPERATING_PLAYBOOK.md` §A.3, "never introduce an `orgs` table").
- **Scout Elevated V2 PR2-4** (all 5 elevations, `services/scout_timeline.py` / `scout_contradictions.py` / `scout_pain.py` / `scout_story.py` / `scout_future.py` / `scout_genome.py`): all real, all browser-verified (see `docs/API.md`'s elevation-by-elevation table for the mechanism each one actually uses). None call an LLM. Two are deterministic arithmetic/scheduling (Time-Travel Replay, Pain Heatmap's automation-potential share), one is a plain text diff with a fixed confidence (Contradiction Resolver), one is a sentence-splitter with a guardrail, not extraction (Story to Structure), and one (Future Preview's "Generate V8 Work Units") is a best-effort mapping onto the real 18-attribute contract via the **existing** `genome_import.import_genome` — several attributes the capture grid never asks for (`trigger`, `actor_constraints`, `acceptance_criteria`, `evidence_required`, `failure_semantics`) get one honest literal placeholder rather than fabricated content, so a thin session's genome correctly scores low and gets blocked by the same GQS gate every other import goes through (verified: a 1-unit session scores GQS 30, `accepted: false` — not silently waved through for being Scout-sourced). Future Preview's UI intentionally has no confetti animation, unlike the design doc's spec — a deliberate call to match the app's existing minimal-motion style, stated in the UI copy itself rather than silently dropped. `scout_pain_signals` (the design doc's optional table) was not created — pain is derived on read from `scout_captured_units`, no new table needed.

- **Stitching Scout to V8 delivery** (`frontend/src/pages/Genome.tsx`, new route `/genome/:versionId`): the missing other half of the demo loop — a real UI over the existing, already-tested `genome.py` router (GQS header, a business-object list, each object's work-unit table, full 18-attribute detail per unit, ratify whole-version and ratify-one-business-object, Automation Index tab with bottleneck view and work-graph edge counts). Nothing new on the backend; this page calls endpoints that were already live and tested. `FuturePreview.tsx`'s "Generate V8 Work Units" result now links to `/genome/{version_id}` for both outcomes, gate-passed and gate-failed, closing the Scout → Genome loop visually. **Important, browser-verified finding, not a bug:** the GQS formula (`gqs.py`, frozen) weights `Observed%` at 40% of the score, and every Scout-sourced work unit is honestly labeled `provenance.source_type: "declared"` (interview-derived, not document-derived) — so `Observed%` is structurally 0 for any Scout-only genome, capping its GQS well under the 90 gate. Verified live: an 8-unit, 100%-complete Scout session generates genome v307 at GQS 30.0, "gates passed: none" — and because `import_genome` only persists `WorkUnit` rows on gate pass, that version's business-object list and Automation Index are both correctly empty; the new Genome page renders that empty state honestly rather than erroring or fabricating rows. A genome imported from real observed data (the existing `Private-Genome-MVP-HR-Ops-FIXED.json` fixture) passes the same page's full loop end to end — GQS 94.3 at the time, ratify whole version, ratify one business object, drill from the business-object list down to one unit's full 18-attribute detail, Automation Index with real bottleneck/hours data — verified live at version v306. Nobody worked around the gate to make the Scout path "look" done; the demo narrative is: Scout gets you a structured, honestly-labeled draft, and V8's own quality gate is what tells you it still needs document-backed verification before it ratifies.

  **The fixture's score is now 92.86, not 94.3.** The file used to declare four work units with mutually contradictory `dependencies[]` (A before B *and* B before A for the same pair — e.g. `WU-OFF-02B` and `WU-OFF-03` each named the other), which `services/genome_import.py` faithfully turned into two opposing `sequence` WorkEdge rows per pair and the Work Graph page rendered as an unreadable tangled cluster, not the process order those edges are supposed to encode. Fixed by removing the backwards half of each pair — the kept direction is the one each unit's own `current_condition`/`desired_condition`/`trigger` already states, not invented. One casualty: `WU-OFF-02B` is a genuine chain root (its precondition comes from an external trigger, not another work unit in this genome) and lost its only, backwards dependency, leaving `dependencies: []`. `gqs.py` (frozen — not touched by this fix) treats an empty `dependencies[]` as a missing attribute regardless of whether the unit is a legitimate root, which costs 1/14 of the completeness component: 94.29 → 92.86, still clear of the 90 gate. Sequence edges on this fixture: 13 → 9; total edges 20 → 16 (`shared_object` edges are unaffected).

- **Demo-readiness pass** (bugs found by running the demo end to end, not by the test suite — each was reachable in the documented demo path):
  - `POST /api/genome/import` rejected a re-import with a bare 500. `work_units` is UNIQUE on `(client_id, code)` and nothing checked it before the insert loop, so a colliding id blew up mid-loop on a raw `UniqueViolation` — and left the rows written before the collision behind, attached to a version row still claiming the full count, which then collided with every retry. Now a pre-pass violation (`work_unit_id_already_exists`, `duplicate_work_unit_id`) like file provenance already was, and the write phase is one rollback boundary. Covered by `tests/test_genome_import_conflicts.py`, verified to fail against the previous code.
  - `samples/Private-Genome-MVP-HR-Ops-FIXED.json` and the Client A HR seed both define `WU-OFF-03` and `WU-OFF-04`, so the sample could never be imported into Client A. They are two genomes for two employers; `POST /api/demo/bootstrap` now provisions a separate **Sample Genome Co** tenant for the sample rather than reconciling the ids.
  - `POST /api/demo/bootstrap` (new, gated by `settings.demo_bootstrap_enabled`) seeds the demo **and mints first API keys**, returning each plaintext once. `routers/org.py` could only ROTATE a key you already held, so there was no way to obtain a first key through the app — standing up a demo required a hand-written `org_api_keys` INSERT with a manually computed sha256. It hands out a credential over an unauthenticated request, so it must be disabled outside a throwaway local database.
  - `GET /api/genome/versions` (new): every other genome route needs a `version_id` the caller had to already know, so an imported genome was unreachable from the UI. Backs the new `/genome` index page; Genome also had no nav entry at all.
  - `GET /api/org/whoami` (new): nothing exposed which tenant a key belonged to. The company switcher defaulted to Catalog while the key was Client A's, so every tenant-scoped read was correctly filtered to nothing and the UI rendered a screen of zeros with no explanation. The switcher now defaults to the key's own company and says so when they diverge.
  - `GET /api/clients/` counted work units per client on the RLS-bound session, so every count came back 0 — the switcher read "Client A (0)" on a fully seeded tenant. Enumerating tenants is cross-tenant by definition and now uses the system session.
  - `useApi` never sent `X-Spec-Key`, so Overview, Discovery and Economics all 401'd on `/work-units/` (moved to per-org auth in Slice 3 PR 3a) and silently rendered 0.
  - `/demo/prepare` called `SystemSessionLocal()` directly, escaping the FastAPI dependency override — its test was asserting against the developer's real Postgres rather than its own fixture. The cross-tenant session is now an injectable `get_system_db` that `conftest` overrides.
  - `ApiKeyBanner`'s "Save & retry" stored the key and hid the banner **without retrying**, leaving the operator on an unchanged form with nothing to indicate progress. It now awaits a real retry.
  - `routers/org.py::rotate_key` refreshed a row after `commit()` without re-applying the tenant binding — the documented RLS/connection-pool bug this repo already fixes with `_rebind_tenant` in `scout.py`.
  - UI: the stylesheet was reworked around a real type scale, three line weights and a restrained elevation set; nav is grouped rather than eleven equal-weight links; empty and loading states are explicit everywhere they were blank or a bare "Loading…". No behaviour claims changed — a gate-blocked genome now *says* it was gate-blocked and that no work units were written, instead of rendering an unexplained empty page.

- **Story to Structure now really extracts — when a model is configured.** This supersedes the "None call an LLM" claim in the PR2-4 row above, which was true when written. With `LLM_PROVIDER=anthropic` and a key, `services/scout_story.py` calls Claude (`services/llm.py`, official `anthropic` SDK, default `claude-opus-5`) and returns candidate work units with the grid's structured fields filled in. Three things stay true regardless:
  - **The verbatim guarantee is enforced, not trusted.** Every span the model returns is checked against the transcript and *discarded* if it is not a literal substring. A model that paraphrases cannot put words in the interviewee's mouth. Covered by `tests/test_scout_story_llm.py`, including the paraphrase case.
  - **The structured fields are labelled as the model's reading, not quotes**, and a field the transcript does not cover comes back empty and renders as "not stated" — the UI never fills a gap with something plausible.
  - **With no model configured (still the default) nothing changed**: the deterministic sentence-splitter runs and says so, naming `LLM_PROVIDER=none`. A configured-but-failing model falls back to the same splitter and reports the failure rather than looking like the deterministic mode.
  `services/discovery.py`'s candidate suggestion moved onto the same shared client; its hand-rolled Anthropic POST is gone, so the codebase no longer has two ways of calling a model. The other four elevations are unchanged and still call no model. **Tests never call one** — `tests/conftest.py` forces `LLM_PROVIDER=none` for the whole suite, because a developer with a key in their `.env` was otherwise turning `pytest` into live, billed network calls.
- **Demo setup is a button, not a runbook.** `components/DemoSetup.tsx` on Overview calls `POST /api/demo/bootstrap`, stores the returned key itself, and signs the browser in — the terminal step and the copy-pasting of two opaque 32-character keys are gone. It holds both demo tenants' keys locally so switching between Client A and Sample Genome Co is one click. It requests fresh keys automatically when this browser is not already holding a usable one, because a key's plaintext exists only at the moment it is minted and a re-run would otherwise leave you signed out. Everything it does is the same endpoint documented in the README; nothing is faked to make the demo smoother.
- **Work Capture Grid sample rows** (`lib/scoutSamples.ts`): eight HR & People Ops rows — the same function as the Client A census, so a Scout session sits in the same domain as the rest of the screen. They are saved through the same `POST /scout/sessions/{id}/units` a typed row uses, so the completeness meter reflects them exactly as it would hand-typed input. Eight is not arbitrary: the meter expects 8 work units, so the full set takes a session to 100% and unlocks Future Preview.

- **Scout Elevated upgrade thread, Points 1 & 2** (three-layer interview model, function-agnostic selector, CHRO blast-radius grid). This supersedes the "founder/SME" naming in the PR1 row above, which was true when written.
  - `InterviewType` grows from two organizational altitudes (`founder`, `sme`) to three: `function_head` (renamed, same meaning — e.g. a CHRO), plus a new middle tier `sub_function_lead` (e.g. Head of TA, Head of People Ops). Existing rows carry the new label automatically via an Alembic `RENAME VALUE`, not a data migration. Contradiction Resolver stays a deliberate 2-way (`function_head` vs `sme`) comparison — `sub_function_lead` sessions are not compared against anything yet, stated in the UI's own copy, not silently dropped.
  - The CHRO blast-radius grid (`services/scout_blast_radius.py`, `/scout/blast-radius`) is a published 44-sub-function/6-cluster HR catalog — copied from a reviewed build instruction, with one correction: its People Operations cluster listed 10 items under a "(9)" label, the only cluster where the two disagreed and the only place the stated 44 total didn't reconcile with its own list; fixed by merging two clearly-overlapping items (Employee Self Service into HR Shared Services & Helpdesk). The instruction's per-item "typical owner" and "expected work units (8-12)" are **not** built — invented per-item numbers with no real source. Owner and priority are fields the CHRO fills in during the session; nothing is pre-suggested.
  - `ScoutBlastRadiusSelection` is a sparse, tenant-scoped table (one row per sub-function actually touched, RLS-isolated like every other scout table) — the 44-item catalog itself is a code constant, identical for every tenant, not per-tenant data.
  - The function-agnostic selector (`pages/BlastRadius.tsx`) shows HR as active/detailed and Finance/Legal/Sales/Operations as "coming soon." **There is no catalog, no data, and no backend route for the non-HR functions** — clicking one renders a static sentence and makes no API call. This is a genuinely open gap, not a placeholder standing in for hidden work.
  - "Start interview" on an in-scope sub-function card navigates to the existing session-create form with the track and owner name pre-filled via router state. It does **not** create a session automatically — the interviewer still presses Start. No cascade, no auto-generated interview queue.
  - Browser-verified end to end via Playwright against the local dev stack: the 44/6 grid renders with correct per-cluster counts, a checkbox/owner/priority edit persists and recomputes the live meter, the Start-interview hand-off lands the right track and name on the create form, and the Finance placeholder shows no data.

- **Offer Desk worked example** (`pages/OfferDesk.tsx`, `lib/offerDeskData.ts`, `/scout/offer-desk`, linked from the blast-radius grid's Offer Management and Onboarding cards). All 11 steps, the handoff map, the exception catalog, and the automation-readiness summary are transcribed verbatim from a real interview-sourced workflow document (Session 1, Rashmi KN, 12 May 2026) — no field, number, name, or policy is invented.
  - **"Run this on the platform"** (Track 1 of the enterprise-readiness roadmap, "Prove it"): creates a real Scout session and captures all 11 steps as real Work Capture Grid rows through the same `POST /scout/sessions/{id}/units` any interviewer uses — so the completeness breakdown that follows is the platform's own computation, not this page's. **Verified live: the session settles at 84% completeness, not 100%**, and stays there honestly — the transcript states a pain point for only 5 of the 11 steps and step 1 has no output field at all in the source, so `Pain & Exceptions` (5/11) and `Inputs / Outputs` (8/11, since that dimension requires both fields non-empty) both come in under 100% by construction, not by omission. "Generate V8 Work Units" stays correctly locked below 100% completeness, the same rule every Scout session faces — this run does not reach genome generation, and the page's own copy says so rather than promising a genome it doesn't produce. `time_minutes` for steps with a stated range (e.g. "5-10 min") uses that range's midpoint; step 1 has no stated minutes for Rashmi's own effort (it's the recruiter's trigger) and is left `null` rather than guessed. Per-step `pain` and `handoffs` values are condensed from that step's own quoted text in the source (its "heaviest," "no mail merge," "≤18 characters," etc.) — five of eleven have no such quote and are left empty, not filled with a plausible-sounding guess.

- **Track 2 of the enterprise-readiness roadmap ("make it safe to run")**: CI, and the hardcoded PII-encryption-key default.
  - `.github/workflows/ci.yml` (new): a Postgres 16 service container (`POSTGRES_DB: wep`, matching every test file's own hardcoded `localhost:5433/wep` DSN — 17 files use that literal string, not `settings`, so the CI database must be named exactly `wep`), `alembic upgrade head`, `alembic check`, then `pytest -q` for the backend; `npm ci && npm run build` (typecheck + build) for the frontend. Runs on push to `main` and on every PR. No CI existed anywhere in this project's history before this.
  - Verified for real, not just written: migrations applied cleanly to a genuinely empty database (a throwaway `wep_ci_check` db, same cluster), `alembic check` reported no drift, and the full 146-test suite passed against a `wep`-named database matching CI's config exactly. One real mistake surfaced by this verification, not by the CI change itself: pointing the app at a differently-named database while several test files' own hardcoded fixtures kept writing to the literal `wep` database left 7 orphaned `test-*` client rows behind (their import failed against the mismatched target before fixture teardown could run) — found and cleaned up directly, not left for the next person to trip over.
  - `pii_encryption_key` (`app/config.py`) has no default now — every environment, local dev included, must set `PII_ENCRYPTION_KEY` explicitly (`openssl rand -hex 32`, see `.env.example`) or the app refuses to start. A harder-to-guess hardcoded value would not have closed this: the actual risk was a default committed to source at all, discoverable by anyone who reads the repo. Making the field required surfaced a real, separate bug: `uvicorn --reload`'s watcher subprocess does not reliably inherit the parent's working directory, so the old relative `env_file=".env"` path resolved inconsistently and crashed only under `--reload` — fixed by anchoring `env_file` to an absolute path derived from `config.py`'s own location rather than the process's CWD. `tests/conftest.py` sets a fixed, clearly-labeled non-secret test value before importing `app.config`, since no test ever decrypts real data.

- **Track 3 of the enterprise-readiness roadmap ("make the spec trustworthy"), first piece: Work Graph cycle detection.** `services/genome_import.py::_detect_dependency_cycles` is a new pre-pass check — same shape as `_validate_file_provenance`/`_validate_work_unit_codes` in the same module — that rejects any genome whose `dependencies[]` form a cycle (self-reference, mutual pair, or a longer ring), before any row is written. GQS's own completeness check (frozen, untouched) only ever validated that a dependency reference *exists*; it never checked that the graph those references form is acyclic. This was a real, previously-reachable gap: `samples/Private-Genome-MVP-HR-Ops-FIXED.json` shipped with exactly this shape once (two work units naming each other as dependencies), fixed by hand before this check existed — see the `92.86, not 94.29` note earlier in this file. Runs on every import, Scout-originated or not, since the gap was never specific to Scout.
  - Fixing this surfaced a real, separate finding, not introduced by this change: three existing test fixtures (`test_genome_import_conflicts.py`, `test_genome_version_numbering.py`, `test_automation_index.py`) had been quietly relying on self-referencing or ring-shaped dependencies purely to satisfy GQS's non-empty-dependencies completeness rule — a hack the new check correctly now rejects. Fixed the same way the real sample genome's own cycle was fixed: keep one direction, let the other end become a legitimate root with a placeholder dependency (a token that isn't `WU-`-prefixed, so it satisfies completeness without tripping GQS's `orphan_dependency` check or being treated as a graph edge). `test_automation_index.py`'s synthetic fixture went from two 4-edge rings plus a 2-edge mutual pair (10 sequence edges) to three non-cyclic chains (7) — the edge-count assertion changed to match, not to make the test pass superficially.
  - New coverage: `test_genome_import_cycles.py` (6 tests) — a two-node mutual pair, a four-node ring (with the reported cycle path checked to actually name all four units), a one-node self-reference, a valid chain that must *not* false-positive, a dependency naming a code outside the current payload (out of scope for this check, same exclusion `_write_genome`'s own edge-creation loop already applies), and that a rejected-for-cycles import still leaves an auditable `GenomeVersion` row.

- **Track 3 of the enterprise-readiness roadmap ("make the spec trustworthy"), second piece: consent as an enforced precondition.** Closes the join Part K9 named as the open piece: "`genome_import.py` still hardcodes `consent_receipt_id=None`... nothing currently *requires* a session to cite a receipt at all." `services/genome_import.py::_validate_consent` (new pre-pass check) and a new `enforce_consent: bool = False` parameter on `import_genome`. When `True`, any unit whose `provenance.interview_id` is set must also cite a `consent_receipt_id` that resolves to a real, active `ConsentReceipt` for that tenant, or the batch is rejected (`missing_consent` / `invalid_consent`) before any row is written. `services/scout_genome.py::generate_genome` — the one real, live Scout-session-to-genome path — now sets `provenance.interview_id`/`consent_receipt_id` on every unit it builds and calls `import_genome(..., enforce_consent=True)`. `WorkUnitProvenanceDetail.consent_receipt_id` is now written from the validated value (resolved defensively even when `enforce_consent` was off, so a bad value can't crash the insert against a real foreign key) instead of hardcoded `None`.
  - **Deliberately not wired into the generic JSON-body import** (`POST /api/genome/import`, `enforce_consent` stays `False`). Turning it on there broke the shipped sample genome: `samples/Private-Genome-MVP-HR-Ops-FIXED.json` carries decorative UUID-shaped `interview_id`/`consent_receipt_id` values (e.g. `WU-US-01`'s `"consent_receipt_id": "ce8a8faf-cfde-5a6e-b707-9dd28a4b1fee"`) that predate the real consent-receipt feature (Slice 3 PR 3c) and were never meant to resolve against it — its `notes` field explains the shape: `"Observed - USHR CSV Row 3 + Interview <redacted interviewer>..."`, a document-backed unit whose evidence happens to also cite a corroborating interview. Enforcing consent there would have blocked a well-established, heavily-referenced (README, this file, multiple tests) fixture over data that was never real consent-tracking to begin with — found via `test_rls_genome_isolation_via_http` and two other existing tests failing when this was first wired in broadly, not by inspection.
  - **A real, separate gap this surfaced, stated precisely rather than silently left implied**: nothing in the Scout Interview UI prompts a user to create a consent receipt before starting a session. The API requires and checks one at genome-generation time; no screen guides anyone to `POST /api/consent/receipts` first. A session created exactly as the UI walks you through it today has no receipt and will be blocked the moment "Generate V8 Work Units" is pressed.
  - New coverage: `test_scout_consent_gate.py` (9 tests), exercised directly against `import_genome(enforce_consent=True)` with a constructed, GQS-passing payload rather than through the real Scout HTTP flow — a real Scout-generated genome always sets `dependencies: []` (never captured by the Work Capture Grid), which GQS's own completeness check treats as a missing attribute on every unit, so a real Scout session's genome is *always* blocked by GQS first (an 8-unit, 100%-complete session scores GQS 30, documented above) and the consent gate would never be reached to test in isolation that way. Covers: `enforce_consent=False` ignoring a missing receipt, a missing receipt, an unresolvable receipt id, a non-integer (UUID-shaped) receipt id failing cleanly rather than raising, a revoked receipt, another tenant's receipt (the gate's own lookup is client-scoped independent of RLS), a real active receipt letting the outcome fall back to GQS instead of short-circuiting it, a document-sourced unit with no `interview_id` being unaffected even with `enforce_consent=True`, and audit coverage for a consent-blocked import.

- **Track 3 of the enterprise-readiness roadmap ("make the spec trustworthy"), third piece: a re-audit of the 15 No-Cracks Gates against `Scout-Reference.md`'s original 26 August table.** This supersedes the "#15 fully live, ~6 partial, ~8 specified-only" line earlier in this file, which was true when written. Current count: **5 of 15 are live** (1 — founder/function_head why captured; 4 — bulk attachments; 8 — Work Graph orphan+cycle detection, closed this same track; 12 — partial ratification; 15 — RLS, regression-tested), **2 more are substantially built but don't match the original spec's exact letter** (3 — SME transcripts + consent are each real and now joined for the live Scout path, but nothing enforces a minimum *count* of transcripts; 14 — PII lifecycle is real and consent is now gate-enforced for Scout, but not for the generic JSON-body import, deliberately), and **8 remain not built**. Of those eight, three (5, 7, 13) are unchanged, genuinely partial gaps already named precisely elsewhere (schema permissiveness, caller-supplied kappa, FK-vs-auto-create). The other five (2, 6, 9, 10, 11) are not just unbuilt but not *attempted* here, because each needs a real product decision before it's buildable at all: a Function Pack SDK's question-bank format (2); what heuristic legitimately decides a Work Unit names more than one business object or authority, the F1 split rule (6); what a "terminal state" means per business-object type (9); what should count as a conformance gap during import specifically, distinct from the existing `/discovery/gaps/scan` path (10); and what "Playback 1 before Playback 2" maps to given the current system has one `GenomeVersionType`, not two (11). A plausible-looking heuristic invented to make one of these look closed would be worse than leaving it named as open — none were built speculatively to move this count. See `docs/Work-Engineering-V8.md` Part K10 for the full per-gate table.

- **Gate 11 revisited, then explicitly not built as a gate.** The line above (Track 3, third piece) said gate 11 wasn't attempted because "the current system has one `GenomeVersionType`, not two" — that turned out to be wrong: the enum already has `inferred`/`detailed`/`ratified`, and every write path (`services/genome_import.py::import_genome`) simply hardcoded `version_type=GenomeVersionType.detailed`, so nothing had ever created an `inferred` row. Revisiting it surfaced that the gate as originally worded — "a client's first `GenomeVersion` must be `inferred`; a `detailed` version cannot be created unless an `inferred` version already exists for that client" — is a capture-*sequence* rule (Scout's interview-then-docs order), not a V8 quality gate; V8's D1 requires both provenance arms without ordering them, and a customer importing from an SOP pack with no prior Scout sitting is a legitimate, intended path (Sample Genome Co is exactly that, on purpose). Enforcing the literal predecessor check would have blocked Sample Genome Co's and the Offer Desk import's very first import, since neither ever produced an `inferred` row. **Decision: label only, no order enforced.** `import_genome` now takes a `version_type` parameter (default `detailed`, unchanged for every existing caller); `services/scout_genome.py::generate_genome` — the one path whose output is genuinely interview-shaped and incomplete-by-contract (`NOT_CAPTURED` placeholders, declared-only provenance) — passes `version_type=GenomeVersionType.inferred`. No predecessor check exists or is planned under this name; nothing about Client A or Sample Genome Co changed. `version_type=GenomeVersionType.ratified` is still never set by anything — "ratified" as a state is `GenomeVersion.ratified`, a boolean flipped in `routers/genome.py`, a separate mechanism from this enum's third value. Do not read a passing GQS score, a `detailed` label, or an absence of an `inferred` predecessor as evidence of anything about import order — none of it is checked.

- **The hosted pitch instance's "Set up the demo" 500'd on Render — root-caused against the live database, not guessed.** `POST /api/demo/bootstrap?new_keys=true` returned `psycopg2.errors.InsufficientPrivilege: new row violates row-level security policy for table "work_units"` (confirmed via Render's own log API on commit `f219980`, 2026-09-05 17:19 UTC). Root cause, confirmed by querying `pg_roles` on the live Render Postgres directly: the managed database's owning role (`wep`) has `rolsuper=false` and `rolbypassrls=false`. Every managed Postgres provider withholds both from the app-facing owner role — this was never Render-specific, and was never going to survive first contact with any real host. `db.py`'s `get_system_db()` and `routers/admin.py`'s cross-tenant endpoints (`demo/bootstrap`, `demo/prepare`, `admin/consent/purge`) were always designed to bypass RLS by connecting as this "superuser" — a design that only ever worked locally because docker-compose's `wep` happens to *also* be a genuine Postgres superuser (bootstrapped by the stock `postgres:16-alpine` image), which bypasses `FORCE ROW LEVEL SECURITY` unconditionally. **Fix, verified against a real Postgres 16 instance with Render's exact role attributes reproduced locally (`ALTER ROLE wep NOSUPERUSER`), not just read as plausible:** an explicit, portable session flag, `app.system_bypass`, that `get_system_db()` sets on every system session and that alembic `68c3926e1143` ORs into every existing `tenant_isolation` RLS policy (found by introspecting `pg_policies` for every table literally named `tenant_isolation` — 25 tables, five of them added by *later* migrations that the original 9a07306c5434 migration's hand-maintained table list never mentioned, proof a hand-maintained list drifts). Reproduced the exact reported traceback locally by stripping `wep`'s superuser bit to match Render's `pg_roles` row exactly, confirmed `bootstrap_demo()` then succeeds once the fix is applied, and ran the full 165-test suite clean both under those Render-matching conditions and under CI's actual conditions (a real Postgres superuser, same as docker-compose) — RLS/tenant-isolation tests included, so this does not weaken isolation for ordinary per-request sessions, which never set the new GUC.
  - **A second, related bug the same investigation surfaced**: `main.py`'s startup `lifespan` hook calls `bootstrap_tenants()` (clones catalog Work Units across clients — itself a legitimate cross-tenant write) via a bare `SystemSessionLocal()`, not the `get_system_db()` dependency, so it never got the new GUC either. Reproduced and confirmed fixed the same way: under Render's exact non-superuser conditions, `/api/health`'s `db_ready` field flips from what would have been a silent `false` (the failure is caught by a broad `except Exception`, so it never surfaces as an error) to `true` once `main.py` also sets `app.system_bypass` before calling `bootstrap_tenants`.
  - **What is still a known, narrow gap, not silently left implied**: several test files' own fixture helpers (e.g. `tests/test_scout_sessions.py::_cleanup`) open a raw `SetupSession` directly against `SYSTEM_DATABASE_URL` for setup/teardown, bypassing `get_system_db()` and its GUC — this only works today because CI's own Postgres service container, like docker-compose, hands out a genuine superuser. It has no bearing on the deployed app (production code never uses these fixtures), so it wasn't in scope for this fix, but a test run against a database where the connecting role isn't superuser would need the same GUC added to those fixtures too.
  - **A separate, real issue on the *same* public-URL concern, found while reading `DemoSetup.tsx`**: "Set up the demo" auto-detected `new_keys=true` whenever *this browser* held no stored key — correct for one developer's laptop, but on a shared public pitch instance every new visitor's browser starts with empty `localStorage`, so every single "Set up the demo" click silently retired the *previous* visitor's Client A key, signing them out with no warning. Fixed: only the explicit "Issue fresh keys" button may pass `new_keys=true` now; a browser that finds no key for a tenant that already exists is told so in plain language instead of silently rotating the credential out from under whoever was using it. A fresh local dev database is unaffected, since `new_keys=false` still mints a real key on a tenant's true first bootstrap.
  - Also fixed alongside, lower-risk: the sidebar's internal `"V9 on V8"` label (meaningless outside this repo's own commit history) is now just `"Specification layer"`; `ALLOWED_ORIGINS` is now set on the Render API service defensively (requests from the static frontend arrive same-origin through its own `/api/*` rewrite already, so this was not the actual cause of the 500, but a direct browser call against `work-engineering-api.onrender.com` — e.g. `/docs` — is genuinely cross-origin and needs it).

- **Spec deny can now be cleared with real evidence, inline, on the same screen.** `pages/OfferDeskSpecDeny.tsx` previously only ever asked `POST /spec/check` with `evidence_ref: ""` — always denied, by design, with no way to see the other side. It now also uploads a real file (a small CSV, sample checklist content) through the actual `POST /files/upload` endpoint — server-computed sha256, a real `UploadedFile` row, the same endpoint `offer-desk-inputs/` used — and re-runs the identical check with that hash as `evidence_ref`, rendering both outcomes stacked rather than replacing the deny. `services/spec.py::enforce`'s evidence check only ever asked "is `evidence_ref` a non-empty string" — confirmed by reading it, not assumed — so the fix is a real upload feeding a real check, not a shortcut. Verified end to end via `TestClient` before shipping: bootstrap → denied (empty ref) → upload → allowed (real sha256), on the exact contracted unit (`WU-ONB-01`) the walk already used.
  - **Deliberately not built the same way everywhere** — the three deny/stop screens in this walk aren't the same shape, and a uniform "clear with evidence" button would misrepresent two of them: **Save talk-only / the GQS gate** is blocked because most of a genome is `declared` not `observed`, which one file can't fix (that's what `offer-desk-inputs/`'s whole seven-file pack was for) — that screen now names this explicitly instead of offering a button that couldn't deliver, with a real evidence-pack view named as the next build. **Dual employment** (`OfferDeskDocumentCheck.tsx`) is a policy stop, not an evidence gap — V8's own design intent is "appetite does not lift it" — so it gets an explanation of why it has no clear button, not one added anyway. Only the Spec-deny case is a genuine one-file, one-unit gap, which is why it's the only one that got a real "clear" action.

- **"What if the evidence existed?" (`pages/OfferDeskEvidencePack.tsx`, new) — the Save-talk-only gate's "what would clear this" now has a real, live answer, not just a citation.** Uploads all 7 `offer-desk-inputs/` files for real through `POST /files/upload` (server-computed sha256 each time — not the batch's originally recorded hashes, though they match byte-for-byte, confirmed), rewrites the embedded 11-unit genome's `provenance.file_id`/`hash_sha256` to point at *this* tenant's fresh uploads (the original batch's file ids meant nothing outside the throwaway tenant that first proved this), then imports it through the same `POST /genome/import` and the same GQS>=90 gate every genome faces. Verified end to end via `TestClient` before shipping — not assumed to still work because it worked once during `offer-desk-inputs/`'s own construction: reproduces the exact **92.73/90, accepted, 11 Work Units** result live.
  - **Deliberately a separate genome, on a separate screen — never a mutation of Rashmi's real talk-only result.** Save talk-only's own result (0 saved, GQS ~30, declared) is untouched by this page; the two are linked to, not merged. The disclaimer banner and the page copy both say `offer-desk-inputs/` is fabricated test evidence before anything else on the page.
  - The 2 units the original batch couldn't back with any log (`WU-OD-07` welcome mail, `WU-OD-11` candidate drop-out) stay `declared` here too — nothing upgrades a unit that wasn't genuinely re-uploaded.

- **Track 4 (per-user login) got its first, decision-independent brick: a `users` table, schema only.** `models/security.py::AppUser`/`AppUserRole`, alembic `efc855b7d06a`: `client_id` FK, `external_id` (deliberately provider-agnostic, not `workos_user_id` — doesn't lock in a choice `docs/ROADMAP-DECISIONS.md` Track 4 says is still pending), `role` (admin/editor/viewer), RLS from creation with the `app.system_bypass` clause already in its `tenant_isolation` policy (no follow-up migration needed the way the *original* RLS migration needed one, five months and five tables later). **Nothing reads or writes this table.** No router, no session, no WorkOS/Clerk/Auth0 call, no login. Real per-user auth still does not exist — only per-org keys. Built this far, and no further, because the column shapes don't depend on which of the three still-open decisions (provider, 3 roles vs. 4, whether org keys stay live) gets made; building past this point would mean guessing at one of those on the roadmap's behalf.
  - Caught and fixed before it shipped, not after: a copy/paste edit first landed with `Ratification`'s own trailing `client`/`version`/`work_unit` relationships accidentally re-parented onto `AppUser` (an incomplete `Read` before the edit missed them) — surfaced immediately as `NoForeignKeysError: ... relationship AppUser.version` across 87 failing tests, fixed by putting the three lines back on `Ratification` where they belonged, confirmed by rerunning the full suite clean (165/165) on a fresh database before considering this done.

- **The 12-minute Offer Desk walk now works for a stranger with no key, not just a signed-in colleague.** Every V9 route (Home → Enterprise → HR → HR operations → Offer Desk → three seats → Playback → Spreadsheet → Save talk-only → How we cut it → Gap → Document check → Hours → Spec deny → Evidence pack → Sitting record) renders fully in guest mode, carrying `"Looking only — nothing is saved."` The three action screens (Save talk-only, Spec deny, evidence-pack import) don't attempt their live write for a guest at all — no 401 noise, no crash — showing a preview/explanation and a sign-in prompt instead, which is a stronger claim than "denied": nothing is even attempted without a key. `useIsGuest()` (`lib/guestMode.ts`) is `keyClientId === null` from the same `POST /org/whoami` resolution `CompanyProvider` already does, so a stranger and a colleague whose key has quietly expired see the identical honest state, not a crash for one and a banner for the other.
  - Home's own front door flipped: "Look — no key needed" (linking straight to the walk) is now the primary action; "Set up the demo" is a collapsed disclosure underneath, for whoever will actually save something.
  - Two pieces of V8-walk chrome were leaking onto V9 routes and are now suppressed there: `GuidedTour`'s floating "Step X of Y" widget (its own steps target `/`, `/work-units`, `/discovery` — the pre-V9 walk) and `ProgressTracker`'s "1 of 7" pill bar (which used to treat Home, `/`, as its own step 1 "Overview," a doubly wrong reading now that Home is V9's front door). Both still render correctly on the pages they actually describe (`/overview`, `/work-units`, etc.) — this isn't a removal, it's making them route-aware.
  - **Verified against the real running app, not just typechecked**: a Playwright pass with a brand-new browser context (guaranteed empty `localStorage`) walked all 18 screens — every one returned 200, rendered the app shell, showed the guest banner on every Offer Desk route, and **never once had a real key appear in `localStorage`**. A second pass on the SAME running app confirmed the keyed colleague walk is unaffected: "Set up the demo" still mints keys and imports the sample genome, and Spec deny's upload → allow still flips for real.
  - Backend untouched: unauthenticated persist/Spec-check/genome-import already 401'd before this slice (`require_org_api_key`) and still do — the guest experience is entirely about the frontend not attempting those calls in the first place, not a new backend permission. `render.yaml`'s `ALLOWED_ORIGINS` already covered the CORS case this slice's brief asked about.

- **Track 1 slice 1.1 — Gate 10 (declared vs. observed at import) is now built, on the specific shape `docs/ROADMAP-DECISIONS.md` recommended: a Work Unit whose `provenance.source_type` is `declared` and has no corroborating `observed` Work Unit gets a `ConformanceGap` (`kind=undeclared`, new `severity` column, `P2` default) at import time — a warning, never a reject; the import's `accepted`/`work_unit_count`/GQS outcome is unchanged either way, with a new `conformance_gaps_flagged` count on the response.** `services/genome_import.py::_flag_undeclared_gaps`, reusing the pre-existing `conformance_gaps` table (`models/discovery.py`) — no second gap table.
  - **"Sharing its code," read literally, turned out to be impossible to build**: `work_units` is `UNIQUE(client_id, code)`, so a newly-imported Work Unit's code can, by construction, never already belong to another row for that client — that collision is a hard reject (`_validate_work_unit_codes`) long before Gate 10 would run. The corroboration this actually checks is **business object**: does this client have *any* observed-provenance Work Unit — from this import or an earlier one — for the same `business_object_type_id`. This is a real design decision made in this slice, not something the roadmap doc pinned down to the byte; stated here rather than left implicit.
  - **A genome that is 100% `declared` cannot pass GQS at all, so it never reaches Gate 10.** `services/gqs.py` weights Observed% at 40% of the score; a payload with zero observed units caps at 60 regardless of completeness, violations, or kappa — the same ceiling `test_scout_consent_gate.py` already documented for Scout's own capture. Every test proving Gate 10 fires or doesn't therefore carries real observed filler content in the same payload (an 80%-observed / 20%-other ratio clears the gate on its own) — Gate 10 is then read off the declared unit(s) in that batch. This is the correct interaction, not a workaround: it means Gate 10 only ever gets a chance to flag a *minority* of declared content riding in on an otherwise-evidenced import, which is what "declared ≠ observed" is supposed to catch — not a wholesale declared-only genome, which GQS already stops on its own.
  - **Scout's `generate-genome` is unaffected and still blocked by GQS first**, same as before this slice (`test_scout_generate_genome_still_blocked_by_gqs_and_never_persists_observed`) — Scout always sets `provenance.source_type: declared` and empty `dependencies`, so it never clears GQS's own gate to reach Gate 10 at all, and nothing in this slice gives it a way to claim `observed` to dodge it (`test_scout_payload_is_always_declared_never_observed`).
  - Covered by `tests/test_gate10_conformance_gap.py` (8 tests): declared-only-for-its-business-object creates a gap; an observed twin in the same import suppresses it; a twin for a *different* business object does not count; a twin already on record from an earlier `GenomeVersion` still counts; `inferred`/`designed` provenance is out of Gate 10's scope (the frozen contract names `declared` specifically); the check is client-scoped; and the two Scout non-regression tests above.
  - Not built, deliberately: Gate 11 predecessor ordering (out of program), and any matching against real trace/`DiscoveryCandidate` data (`/discovery/gaps/scan`'s job, Track 6, not started) — Gate 10 only ever has the declared side of a genome import to look at, per the roadmap doc's own framing of how it differs from `/discovery/gaps/scan`.

- **Track 1 slice 1.4 — HR Function Pack v0: a real question bank + a real, standalone CLI that fails on missing coverage.** `packs/hr/question_bank.yaml` and `./scout pack validate <pack_dir>` (dispatching to `backend/app/services/pack_validate.py`) exist and do exactly one thing: fail with a non-zero exit unless every one of the 18 Work Unit attributes (`gqs.py::REQUIRED_18`) has at least one interview question mapped to it. Proven both ways — `packs/hr` passes (18/18, exit 0) and a pack with one attribute's mapping stripped fails (exit 1, names the missing attribute) — as pytest cases (`test_pack_validate.py`) and by hand.
  - **Source = the existing hand-written bank, not invented content.** Every question's `id`, `text`, and `why` in `question_bank.yaml` is copied verbatim from `frontend/src/components/scout/DiscoveryPartner.tsx`'s `QUESTION_BANK` — `test_hr_pack_questions_are_not_invented` asserts each one is a literal substring of that file. Only the `attributes` mapping onto the 18 is new work product from this slice. That source bank has 13 questions across three tracks (function_head/sub_function_lead/sme) for 18 attributes, so several questions are honestly mapped to more than one attribute (e.g. "walk me through what happens today, step by step" covers `trigger`, `current_condition`, and `desired_condition` at once) rather than one question being split or invented per attribute.
  - **Deliberately not wired to anything else.** `scout pack validate` is not called from GQS, from Scout `generate-genome`, from genome import, or from any CI step — it is a standalone check run by hand against a pack directory. GQS's formula and weights are untouched; Gate 11 order is still not enforced; no login/WorkOS path is touched.
  - **Only the one file this slice asked for.** `Scout-Reference.md`'s "Function Pack SDK — 5 Files + CLI" target design also names `pack.json`, `business_objects.yaml`, `regulatory_stubs.yaml`, `parser_hints.yaml`, and `verdict_anchors.yaml` — none of those exist. This slice built the question bank and its coverage validator only; the rest of that design remains not built.
  - **No Finance or Legal pack.** `packs/` holds `hr/` only. A second function pack is Track 5, gated on a founder-reviewed catalog (`ROADMAP-DECISIONS.md`), not attempted here.

- **Track 2 slice 2.1 — a consent screen sits between Scout capture and `generate-genome`, and the API now refuses outright (4xx) when one is missing, instead of quietly returning 200 with `accepted: false`.** `ConsentGate.tsx` renders inside the existing Future Preview elevation (`FuturePreview.tsx`) whenever `session.consent_receipt_id` is null: pick a real, active receipt already on file (`GET /api/consent/receipts`) or create one (`POST /api/consent/receipts`) in plain words — "who agreed," "what we told them it's for," "keep this record for (days)," a checkbox instead of a legal-consent wall of text — then attach it via a new `PATCH /api/scout/sessions/{id}/consent-receipt` endpoint. `routers/scout.py::generate_genome` now checks `session.consent_receipt_id` before calling the import pipeline at all and raises `400` if it's missing — a real HTTP failure, not a body flag, proven by `test_generate_genome_without_consent_receipt_4xx` (asserts the 4xx *and* that no `GenomeVersion` row gets written, unlike a GQS-blocked attempt).
  - **The deeper gate is untouched.** `genome_import.py::_validate_consent` (missing/invalid/revoked/cross-tenant receipt, all pre-existing from Track 3 PR 3c) still runs exactly as before once a receipt is attached — this slice only adds a fast, honest door in front of the one case (no receipt at all) a UI screen can prevent outright. A revoked or wrong-tenant receipt id still falls through to that existing check and comes back as `accepted: false` at `200`, same as it always has (`test_scout_consent_gate.py`, unmodified).
  - **GQS still blocks Scout-only genomes correctly, and this doesn't paper over that.** `test_attach_consent_receipt_then_generate_runs_existing_path` attaches a real receipt to a thin session and confirms `generate-genome` reaches the real pipeline (`200`, a `gqs` score present) — whatever GQS decides for a Scout-mapped genome's honest placeholders is exactly what it decided before this slice.
  - **One pre-existing test's setup no longer matched the new contract, so it was fixed, not weakened**: `test_generate_genome_uses_real_import_pipeline_and_gate` created a session with no consent receipt and asserted `200` — true before this slice only because a thin Scout genome fails GQS *before* the (previously unreachable via HTTP) consent check ever ran. It now attaches a real receipt first, so it still tests what its name says (the GQS gate), rather than accidentally depending on a code path this slice intentionally closes off.
  - **No WorkOS, no Gate 11 ordering, no invented traces.** The consent screen calls only the existing `consent_receipts` table and the two Scout endpoints above; nothing here touches login, a predecessor check, or any Zwayam/HRIS-shaped data.
  - Verified against the real running app: `Prepare Client A HR demo` → new session → captured unit → Future Preview shows the consent screen (not the genome preview) → pick-existing and create-new both work and correctly disappear once a receipt is attached, confirmed with a second session against the first session's saved receipt. `curl`'d directly: no receipt → `400` with a plain-language `detail`; with a receipt, the normal locked/unlocked preview returns. Full backend suite (184 tests) and frontend typecheck/build both pass.

- **Track 1 slice 1.2 — Gate 6 (F1 split rule) is now built, on the specific shape `docs/ROADMAP-DECISIONS.md` recommended: advisory, not blocking.** A Work Unit whose `business_object` contains a multi-object delimiter (comma, slash, "and") OR whose `authority` names more than one distinct approver gets a `ConformanceGap` (`kind=split_recommended`, `severity=P2`) at import time — a warning, never a reject, and the unit is never auto-split. `services/genome_import.py::_flag_split_recommended`, reusing the same pre-existing `conformance_gaps` table Gate 10 already writes to (`models/discovery.py`) — no second warnings table; the response gained a `split_recommended_flagged` count alongside Gate 10's existing `conformance_gaps_flagged`.
  - **`GapKind` gained one new value, `split_recommended`.** Postgres enforces this as a native enum type, so a small additive migration (`a1b2c3d4e5f6`) runs `ALTER TYPE gapkind ADD VALUE IF NOT EXISTS` — same shape as the existing `interviewtype` precedent (`8c8ef364ec46`). Verified for real: applied clean against a live Postgres 16 instance, `alembic check` reports no drift, and the full suite passes against that same database afterward.
  - **The delimiter check is literal, not semantic.** `_split_distinct_parts` splits on `,`, `/`, and the word "and" (case-insensitive, word-boundary so it doesn't fire inside a word like "Andaman"), then dedupes case-insensitively. "Offer, Employee" and "Payroll and Benefits" both flag; "Offer" alone does not. The same function checks both `business_object` and `authority` independently, and either one matching is enough — a unit can carry both reasons in one gap's description if both fire.
  - **Scout's own genome payload is unaffected.** `services/scout_genome.py::build_genome_payload` sets `business_object` to the captured unit's name and `authority` to the single interviewee name — neither carries the three delimiters by construction, so nothing about this slice changes Scout's existing GQS-blocked outcome for a thin session (`test_scout_generate_genome_still_blocked_by_gqs_unchanged_by_gate6`).
  - Covered by `tests/test_gate6_split_recommended.py` (5 tests): a comma-delimited `business_object` warns and the import still succeeds; slash and "and" both trigger the same warning; a multi-approver `authority` warns independently of `business_object`; a single clean object/authority produces no gap; and the Scout GQS-blocked path is unchanged.
  - Not built, deliberately: no hard-block path (the roadmap doc's own recommendation — the heuristic is fuzzy enough that blocking risks false-positive rejections of real imports), no auto-split, no WorkOS, no Gate 11 predecessor ordering.

Update this file in the same PR any time a row moves from "specified" to "enforced."
