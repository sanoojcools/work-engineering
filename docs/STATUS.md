# Product state — 6 September 2026

*(This file previously described the state as of 1 September 2026 — before V9's guided pitch walk (Home → Enterprise → HR → Offer Desk) merged to `main`. That version is superseded; nothing below should be read as consistent with an older copy of this file elsewhere. See `V8-STATE-AND-REFERENCE.md` for a fuller standalone snapshot and `V8-PRODUCTION-ENGINEERING.md` for how the product got from there to here.)*

*(5 September's copy of this file had fallen behind `main` in one specific, contradiction-shaped way: it still listed the consent-receipt screen under "What is not built" — "no screen guides anyone to create one first" — after Slice 2.1 had already shipped exactly that screen (`ConsentGate.tsx`). It also never mentioned, anywhere in this file's own summary, that Gates 6/9/10 or the HR Function Pack existed at all, even though `HONESTY.md`'s ledger documented all three in depth. This revision fixes that: every claim below was checked against the code in this same PR, not carried forward from the previous copy.)*

Concept: [Work-Engineering-V8.md](Work-Engineering-V8.md) (now including Part K, Scout). Code mapping: [../ARCHITECTURE.md](../ARCHITECTURE.md). HTTP: [API.md](API.md). Honesty ledger: [HONESTY.md](HONESTY.md). How to run: [../README.md](../README.md). What's next, pending review: [ROADMAP-DECISIONS.md](ROADMAP-DECISIONS.md). Ops runbook: [OPS.md](OPS.md).

---

## In one sentence

Work Engineering is a specification layer (Work Units, VERDICT autonomy scoring, a GQS quality gate, five projections, a Spec API) fused with Scout, its capture layer (a CHRO blast-radius grid that scopes a census across a 44-sub-function HR catalog, structured three-layer interviews — Function Head, Sub-function Lead, SME — a live Work Capture Grid, five analytical elevations, LLM-optional extraction with an enforced verbatim guarantee) — one multi-tenant, Row-Level-Security-isolated product, demoable end to end with one click and no terminal step.

This repo remains the **specification layer** (V8 C4). It does not log an individual person in, connect to a real ERP, or execute work.

---

## Readiness

| Bar | Now | Meaning |
|---|---|---|
| **Colleague demo** | **Yes — and now zero-click** | The 12-minute guided walk (Home → Enterprise track → HR → Offer Desk SME → Playback → Spreadsheet → Save talk-only → How we cut it → Gap → Document check → Hours → Spec deny → Evidence pack → Sitting record) needs **no key and no "Set up the demo" click at all** — every screen renders read-only for a stranger, carrying "Looking only — nothing is saved." "Set up the demo" still exists (now a collapsed disclosure on Home, not the primary action) for a colleague who wants to see the same screens actually write: real GQS score, a real upload → allow flip on Spec deny, a real evidence-pack import. Bootstrap was deliberately left enabled, not disabled, to keep that path working. The older Overview → Discovery → Projections → VERDICT → Scout Interview walk still exists under Specification / Analysis in the nav. |
| **End-to-end (wedge)** | **Yes, and past J1** | One company × one function × inventory, graph, VERDICT, economics, gap, pack — plus the capture side (Scout) that produces that inventory from a structured interview, and the quality gate (GQS) that decides whether a genome is trustworthy enough to write. |
| **Multi-tenant, RLS-isolated** | **Yes, HTTP-proven** | Per-org `X-Spec-Key`, hashed at rest, rotatable with a grace window. Two-tenant cross-isolation proven by a real HTTP test matrix (`test_rls_http.py`), not a manual check. |
| **Customer-ready** | **Not yet** | The HR census is a sample employer's data. Scout captures real structured input from a real interview, but per-*user* login does not exist — only per-org keys. |
| **Ship-ready (hosted product)** | **Hosted and live on commit `f202b7c`; guest walk deployed, hosted browser click-through still not directly confirmed** | The Render free-tier blueprint (`render.yaml`) is deployed and starts clean; `POST /api/demo/bootstrap`'s earlier 500 (RLS bypass needing a GUC, not a superuser) is fixed and re-verified against Render-matching role conditions — see `docs/HONESTY.md`. CI now exists (`.github/workflows/ci.yml`). The PII-encryption key has no hardcoded default. **What's confirmed for this slice:** both Render services (`work-engineering-api`, `work-engineering`) report `status: live` on deploy `f202b7c`, no 5xx or error-level app logs on either service in the window since that deploy went live, and the full guest + keyed walk (18 screens) passes clean via Playwright against a *freshly migrated* local database (this environment's own DB still carried a stale bootstrapped tenant from an earlier run in this same session, which the fresh-context test correctly refused to reuse — not a code bug, reproduced and fixed by rebuilding the local DB from a clean `alembic upgrade head`). Added a "waking the demo" banner (`company.tsx`'s `firstLoadPending` + `AppShell.tsx`'s `WakingBanner`, debounced 2s so it never flashes on a normal fast load) so a Render free-tier cold start reads as "waking up," not broken. **What's not confirmed:** nobody has clicked through `https://work-engineering.onrender.com` in an actual browser this session — this session's own egress policy hard-blocks all outbound access to `onrender.com` (confirmed via both `curl`, which gets a 403 on CONNECT, and the `WebFetch` tool, which returns `EGRESS_BLOCKED` — not a retryable glitch, and per this session's own proxy documentation, not something to route around). Render's own request logs were checked as the best available substitute and show **zero requests of any kind** against either service since this deploy went live, so there is no indirect evidence either, honestly — the gap is real and named, not silently skipped. Still missing beyond that: monitoring, backup, a load test, and everything in tracks 4–6 of the enterprise-readiness roadmap. **Re-checked this slice, same result, via both methods again**: `WebFetch` against `work-engineering.onrender.com` and against `render.com` itself both returned `EGRESS_BLOCKED`; `curl` against both hosts returned exit 56 with the proxy naming it explicitly — `connect_rejected (organization policy)` on both. The block is not specific to one host, and nothing about it has changed since the last time this was checked. **This slice's own founder-visible non-negotiable — guest Hours (95 vs 61.8, both visible, no key) — is therefore still only Playwright-verified against a local dev stack, never against the hosted URL. That one click-through (Home → guest walk → Hours, on `https://work-engineering.onrender.com`, incognito) is a founder action this agent cannot substitute for, not a lower-priority gap than the rest of this row.** |
| **Operate (a 500 is seen, Track 6.1)** | **Partial — code shipped and log-tested; one native click not re-confirmed live** | Every unhandled 5xx now logs one structured, greppable line (`request_id`, `method`, `path`, `exc_type` — never the exception's own message/args) and echoes an `X-Request-Id` header/body field a founder can quote back into Render's log search; confirmed against a real local Postgres, not just SQLite (`backend/tests/test_error_logging.py`, `test_health.py`). `/api/health` no longer lies: it used to return HTTP 200 unconditionally regardless of `db_ready`, so Render's own `healthCheckPath`-driven monitoring (`render.yaml`) could never have detected a failed boot even in principle — it now returns 503 when the last boot's DB check failed. Render's per-service `notifyOnFail` field (confirmed via the Render API on both live services: currently `"default"`, i.e. inheriting whatever the workspace's own Notification Settings say) is the free, native, no-code path to an email on a failed deploy — **not re-verified by clicking through Render's own dashboard or docs this session**, same `onrender.com`/now also `render.com` egress block named in the Ship-ready row above; a human needs to open Workspace Settings → Notifications once and confirm "Deploy failed" email is actually on. See `docs/OPS.md` for the exact log-search filters that were checked and do/don't work (`statusCode` filtering does **not** work on this service's logs — confirmed empirically, not assumed) and for a worked example pulled from this slice's own log-forensics: a real `POST /api/demo/bootstrap` 500 (RLS `InsufficientPrivilege` on `work_units`) from 2026-09-05T17:19Z — cross-checked against Render's deploy history before writing anything down, this turned out to be the *same* incident `HONESTY.md` already documents fixed same-day (commit `30bbab2`, ~2 hours later), not a new find, and no recurrence has been logged since. The one real gap it exposes: no automated test would catch that exact bug reappearing (CI's Postgres role is a superuser, unlike Render's — the one condition that triggered it), named for whoever picks up test hardening next. **What's still not solved by any of this:** a single endpoint 500'ing while the process and `/api/health` both stay green — exactly what that incident is — pages nobody; closing that for real needs either a paid plan/third-party DSN (a STOP-GATE this slice didn't need to open, since nothing here required one) or a synthetic uptime prober (a monitoring product this slice was told not to invent). |
| **Backup (Postgres, Track 6.2)** | **STOP-GATE — checked for real, not enabled** | `work-engineering-db`'s real Render API record (`get_postgres`, this slice) reads `plan: free`, with no backup/point-in-time-recovery field anywhere on the resource — the free plan has nothing to toggle, not a setting left off. Sharper than that: the same record's `expiresAt` is `2026-10-05T09:42:10Z`, roughly 29 days from this entry's own date — Render's free Postgres is a 30-day instance, not a permanent free tier, so the whole primary copy is at risk on that date, not just its history. Enabling backup means upgrading off the free plan; this slice did not do that and did not substitute a home-grown `pg_dump` cron job for a real backup product. **Paid Render backup: no — founder decides**, and the decision is worth making before 2026-10-05 given what else is at stake on that date. See `docs/OPS.md` §4a for the full finding. |

V8 alignment: **C3** (five projections of one record), **C4** (spec, not execution), **D1/D3** (Scout's declared-arm capture vs. discovered work; conformance gap), **E7** (provenance: observed vs. declared, honestly labelled throughout), **G6** (Spec checks logged; runtime *stopping* on denial is not built), **H2** (VERDICT + four hard gates, deterministic), **H5** (honest case: attributed hours are the smaller, defensible number), **Part K** (Scout as the concrete instrument for D1's downward, declared arm).

---

## What a colleague should see (12 minutes)

The front door is now Home, not Overview. Click **Set up the demo**, then:

1. **Enterprise track**: a company with HR, Finance, Legal, Operations listed — only HR is live; the rest are honest "not built" placeholders. Enter through HR operations at the Offer Desk.
2. **Offer Desk SME → Playback**: the three-seat model (CHRO / HR Ops stand-in, SME, and a 3-column playback of what each seat said), transcribed from a real interview.
3. **Spreadsheet → Save talk-only**: saving a talk-only session stays honestly empty — no GQS score, no fabricated genome — until real observed evidence backs it.
4. **How we cut it → Gap**: the conformance gap between what was declared in the interview and what the sitting record shows. For a guest this is the same four illustrative rows it has always been. For a signed-in colleague who has actually imported a genome (e.g. via the **With evidence (sample)** screen, reachable from the same stepper this walk uses but not itself one of the 8 numbered stops below), it now reads real `ConformanceGap` rows from Gates 6/9/10 off this tenant's own imports instead — or a plainly-labelled empty state if none fired, never the illustrative rows dressed up as live data.
5. **Document check**: whether an evidence file actually backs a step, not just an assertion.
6. **Hours**: declared 95 hrs/month stays on screen next to the defended 61.8 — the smaller, evidence-backed number is not hidden behind the bigger declared one.
7. **Spec deny**: a live Spec API check against this Offer Desk work unit — evidence-free returns `denied, "evidence_ref required by contract"`; a real evidence_ref returns `allowed`.
8. **Sitting record**: the close of the 12-minute walk.

The pre-V9 walk (Overview → Discovery → Projections → VERDICT, then Scout Interview → the five elevations → Future Preview, then Sample Genome Co's ratify/drill/Automation-Index) still works and still lives in the nav under Specification / Analysis — it is just no longer the first thing a colleague is shown.

Do not treat any inferred VERDICT score, attributed FTE, or GQS score as more certain than the app itself states it to be. Do not demo `offer-desk-inputs/` as Rashmi's real production data — it is fabricated test evidence built to prove the observed-evidence path works end to end; see `offer-desk-inputs/README.md`.

---

## What is real right now (see `docs/HONESTY.md` for the full, continuously-updated ledger)

- Multi-tenant RLS isolation, per-org key rotation, server-computed file-hash upload, a deterministic (no-LLM) classifier and column mapper, partial genome ratification, the Automation Index's graph-edge detectors, consent receipts with a 90-day purge sweep.
- Consent is now a gate-enforced precondition for the live Scout path: generating a genome from a Scout session requires its `consent_receipt_id` to resolve to a real, active receipt for that tenant, or the whole batch is rejected (`missing_consent`/`invalid_consent`) — the generic JSON-body import stays permissive, since its `interview_id`/`consent_receipt_id` fields are free text a caller can set to anything, including the shipped sample genome's own decorative pre-consent-feature values. **The UI screen exists, not just the API check** (Track 2 slice 2.1, `ConsentGate.tsx` inside `FuturePreview.tsx`): pick a real, active receipt already on file or create one in plain language, then attach it before generation is offered at all. This corrects the previous copy of this file, which still listed "no screen guides anyone to create one first" under "What is not built" after that screen had already shipped.
- **Gates 6, 9, and 10 — three advisory conformance checks that run on every `POST /genome/import`, warn, and never block** (Track 1 slices 1.1–1.3): Gate 10 flags a `declared`-provenance Work Unit with no corroborating `observed` unit on the same business object (`GapKind.undeclared`); Gate 6 flags a `business_object` or `authority` that names more than one distinct thing (`GapKind.split_recommended`, comma/slash/"and" delimited, never auto-split); Gate 9 flags a business object with 3+ Work Units whose inferred current→desired state graph never reaches a terminal state (`GapKind.missing_terminal_state`, 2-unit objects exempt, no state machine is written). All three write into the same pre-existing `conformance_gaps` table and are surfaced on the Offer Desk **Gap** screen once a real genome has been imported for the signed-in tenant (this PR) — before this, that screen only ever showed four static, illustrative rows, identical for a guest and a colleague who had just imported real evidence.
- **HR Function Pack v0 exists and is now enforced in CI, not just runnable by hand** (Track 1 slice 1.4): `packs/hr/question_bank.yaml` plus `./scout pack validate packs/hr` fails non-zero unless all 18 Work Unit attributes have at least one mapped interview question. `.github/workflows/ci.yml` now runs this on every push and PR (this PR) — previously it existed only as a standalone command a person had to remember to run.
- Scout: three-layer interview sessions (Function Head, Sub-function Lead, SME), a live Work Capture Grid, a completeness ("Genome Strength") meter, all five elevations with real (if simple) named mechanisms, LLM-optional extraction with an enforced verbatim-substring guarantee.
- CHRO blast-radius grid: a published 44-sub-function/6-cluster HR catalog, tenant-scoped in-scope/owner/priority selections, a live blast-radius meter, and a function-agnostic selector — HR is active and detailed, Finance/Legal/Sales/Operations are honest "not built" placeholders with no catalog or data behind them.
- Offer Desk worked example: 11 real steps, a handoff map, an exception catalog, and an automation-readiness summary, transcribed verbatim from a real interview. Its "Run this on the platform" action seeds them as a real Scout session — the completeness numbers that come back are the platform's own (verified: 84%, not the fabricated 100% a demo might reach for), not a copy of the source document's claims.
- Work Graph dependency-cycle detection: `POST /api/genome/import` now rejects a genome whose `dependencies[]` form a cycle (self-reference, mutual pair, or a longer ring), reporting the actual cycle path as a `circular_dependency` violation — the same pre-pass shape as the file-provenance and duplicate-code checks. GQS's own completeness check only validated that a dependency reference exists, never that the graph it forms is acyclic; this was a real, reachable gap (the shipped sample genome fixture had exactly this shape once — see below).
- Generating Work Units from a Scout session reuses the *same* import pipeline and faces the *same* GQS gate as any other genome — no relaxed path for the product's own capture instrument.
- V9's guided pitch walk (Home → Enterprise → HR → Offer Desk) is merged to `main` and closes end to end: three-seat interview + playback, talk-only save honestly stays empty, the cut/gap view, a document-backed evidence check, declared-vs-defended hours (95 vs 61.8), and a live Spec API deny → allow. `offer-desk-inputs/` is a **fabricated** evidence set (invented candidates, invented transcripts, invented system-of-record exports) built to prove that path works when observed evidence exists — not Rashmi's real production data, and not proof any real Zwayam/Zoho/UAN integration exists (there is none; the only real ingestion path is the generic `.csv`/`.xlsx` upload endpoint).
- `render.yaml`'s free-tier Render blueprint is deployed and live at `work-engineering-api.onrender.com` / `work-engineering.onrender.com` — migrations ran clean against real Postgres, both services are serving on commit `f202b7c`; see "Ship-ready" above for what's confirmed vs. not yet walked in a browser.
- The guest walk's cold-start UX: a Render free service that's napped for 15+ idle minutes takes 30-60s to answer its first request. `CompanyProvider` now tracks `firstLoadPending` from mount until its first `/org/whoami`+`/clients/` round trip settles (success or failure), and `AppShell` shows a "waking the demo instance" banner once that's been pending for 2+ seconds — long enough that a normal fast load never sees it, short enough that a real cold start doesn't sit there looking broken. Verified with a Playwright run that artificially delays those two calls by 3s: the banner is absent at 1s, present at 2.5s, gone again once the calls resolve.

## What is not built, named precisely

The rest of the Function Pack SDK beyond the question bank (`pack.json`, `business_objects.yaml`, `regulatory_stubs.yaml`, `parser_hints.yaml`, `verdict_anchors.yaml` — none exist; only `question_bank.yaml` and its CLI validator do); adaptive, AI-generated interview follow-up questions (the question bank is static and hand-written); trace/log/ERP-based upward discovery (Scout captures declared intent only, and no Zwayam/Zoho/UAN connector exists); a dedup/split engine for malformed imports (Gate 6 flags a Work Unit that looks like it should be split — it never splits one); a hard block on any of Gates 6/9/10 (all three warn and let the import through, by design); per-user login or SSO; a real sub-function catalog for Finance, Legal, Sales, or Operations (the selector shows them as placeholders, not fabricated lists); automatic cascade from a CHRO's blast-radius selection into created interview sessions (Start interview pre-fills the create form — it does not create sessions on its own); a browser-confirmed hosted walk (the Render deploy is live on `f202b7c`, its migrations/health checks and the absence of any 5xx are confirmed via Render's own API, and the full guest+keyed walk is Playwright-verified against a freshly migrated local database — but nobody has clicked through the actual 12-minute walk against the hosted URL itself: this session's own outbound network policy hard-blocks `onrender.com`, and Render's request logs show zero traffic since the deploy, so there is no substitute evidence either). See `docs/Work-Engineering-V8.md` Part K11, `V8-PRODUCTION-ENGINEERING.md` §4, and `docs/ROADMAP-DECISIONS.md` for the full, evidenced list and what's proposed next.

## What not to promise

- That a passing GQS score means all fifteen of the original design's quality gates are individually enforced — most are partial or specified-only.
- That attributed FTE or an inferred VERDICT score is measured, confirmed labour.
- That a customer can log in and see only their own data — per-org keys exist; per-user login does not.
- That this product executes work, connects to a real ERP, or runs agents. It specifies work; execution systems consume the spec.
- That `offer-desk-inputs/` is Rashmi's real production month, or that any real Zwayam/Zoho/UAN system-of-record integration exists — both are fabricated test fixtures proving the observed-evidence path, not real data or a real connector.
