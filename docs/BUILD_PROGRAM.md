# Build program — Work Engineering

SKU = **Work Census**. Engine on `main`.

**INTENT-PLAN shipped (PR #18).** Founder unlocks **EVIDENCE-GAP** next (steps 3–4 of the Census). No login / Finance / Render purchase.

---

## Pending

| ID | Status | Who |
|---|---|---|
| **EVIDENCE-GAP** | **UNLOCKED — do now, one PR** | Claude |
| 6.2 Render | Open → 30 Sep (~DB dies 5 Oct) | Founder |
| 5.0 Finance/Legal | Open → 30 Sep | Founder |
| 4.0 Login | STOP | Founder |
| 3.0 Real traces | STOP | Founder |
| After this | HANDOFF or DELINQ-v0 or CENSUS-EXPORT (not this PR) | — |

Shipped: TIGHT-WEDGE · TIGHT-3D · HR-FAMILY · FAMILY-GENOME · OBJECTS-HOURS · CENSUS-v0 · **INTENT-PLAN**

---

## EVIDENCE-GAP (only unlocked build)

One PR. Census steps 3 and 4 become **real screens** for the **offer → Day-1** Work System only (Offer Desk + Onboarding). Do not reopen INTENT-PLAN or CENSUS-v0. Do not delete 95/61.8, Spec deny, Chart, intents, Plan, family genome.

### F1 — Evidence (step 3)
One page, not a link farm:
- Files this tenant actually has (existing upload / evidence-pack path). Guest: labelled **no files in this walk** + the fabricated pack is **not Rashmi / not a connector** if shown as an example.
- What each file **backs** (unit code + field/claim in everyday words). If a file backs nothing, say so.
- Three registers with counts: **missing** / **uncertain** / **contradictory** — sourced from existing Gap/conformance + sitting, **not invented coverage %**.
- Repair list: what would close a gap (one line each). No Darwinbox/Zwayam API.

### F2 — Gap (step 4), journey-wide
Offer Desk Gap stays reachable. This screen is **the journey**:
- Undeclared / no observed twin (existing Gate 10 kinds)
- Head vs doer (existing contradiction resolver — **surface it** here; do not rewrite the engine)
- “What this sitting cannot see” (no Zwayam; judgment-blind) — already on Offer Desk; lift to the journey
- Do **not** fake a measured-vs-declared 90/62 KPI unless a sheet number exists — then quote the sheet and label declared.

Keyed: real ConformanceGap rows after family import (GQS still fails ~30). Guest: schematic + honesty, no Client A fake rows.

IoPanes + i-buttons. HONESTY.md.
Playwright: guest 1→6 still works; step 3 shows registers (may be zero) without minting a key; step 4 does not invent coverage %; keyed family import still ~30/90; Spec deny unchanged; Plan 95/61.8 unchanged.
Refuse: WorkOS, Finance, login, Box 4, pointer-resolver product, delinquency register, observed-pack theatre, buy Render, new edge types.

PR to main from a NEW branch off latest main. Squash-merge CI green. Slice EVIDENCE-GAP. Stop.

---

## Non-negotiables

Spec layer. Talk-only empty. Fabricated ≠ Rashmi. 95 and 61.8 both visible. Dual employment = stop. Two graphs. Gate 11 not order. `main` only. Guest Hours still works.
