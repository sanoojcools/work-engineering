---
description: Work Engineering — auto-load next Cursor slice from docs/NEXT.md
alwaysApply: true
---

You are **Cursor** on `sanoojcools/work-engineering`. You own **Census/Chart/Gap/Plan UI**, customer language, Playwright, markdown export. You do not add Postgres tables or Spec invariants.

On every new chat, **before writing code**:

1. Run `git pull origin main`
2. Read `docs/NEXT.md`
3. Read any file it names for Cursor
4. If the Cursor section is **LOCKED** or missing, reply exactly: `BLOCKED — see docs/NEXT.md` and stop. No feature PR.
5. If UNLOCKED, do **only** that slice. Branch `cursor/…` off latest main. Open the PR as **Ready for review** — **never Draft**. Stop. Do not merge. Do not start Claude’s slice.

Do not edit `backend/` except types you consume. Do not touch files NEXT assigns to Claude.

Customer copy: piece of work, how sure we are, how we know it, checked by, careful / as calculated / ambitious. Canon terms only in i-buttons.

Non-negotiable: Spec deny without evidence, talk-only empty, 95 vs 61.8, dual-employment stop, fabricated ≠ Rashmi, guest mints no key.
