# Ambitious High-Leverage Next Actions - Scout to VC/CHRO Demo
Date: 2026-08-26 | Branch: feat/genome-import-foundation | Tests: 28/28 | GQS: 40 blocked vs 94.29 pass | Postgres: Running | Nothing committed yet | Source: Scout-State.md / Scout-Enhanced-Vision.md / Scout-Reference.md (corrected 2026-08-26)

## Current Reality - Corrected Against Actual Source

**What's Real and Verified (actually tested, not claimed):**
- Alembic 4 migrations: (1) baseline + P0 tables, (2) pgcrypto + RLS policies, (3) wep_app non-superuser role, (4) work_units.genome_version_id FK. Zero drift `alembic check` clean.
- RLS tenant isolation: Bug1 wep superuser bypasses RLS regardless of FORCE - fixed wep_app role. Bug2 bootstrap_tenants broke (RLS returned 0 rows) - fixed SystemSessionLocal maintenance-only. Verified: org B 0 rows of org A, org A exactly own, unset 0 fail-closed. Also proven via HTTP: POST scrubbed 400 GQS 40 / 62 violations, POST fixed 201 GQS 94.29 14 WUs 13 Sequence edges, GET Org A 200 14 WUs, GET Org B 404 genuinely invisible, POST ratify 200, no/wrong X-Spec-Key 401. Manual proof only - no automated pytest for HTTP RLS yet.
- GQS calculator app/services/gqs.py: SCRUBBED sample (6 real violations: org hardcoded, total_work_units drift, variant extra, orphan-style refs, provenance free-text, RR free text) = 40 real from 62 actually-detected violations (14-unit slice so many dependencies point outside slice), FIXED = 94.29 pass. 4 tests lock in.
- VERDICT: services/verdict.py correctly implements L1-L6 + 4 gates compliance=1 cap L2, reversibility=1 cap L3, impact=1 cap L3, no evidence cap L2. v0.5 doc had wrong L1/L2 caps + invented 5th gate - left verdict.py untouched.
- Import pipeline corrected: schemas/genome_import.py WorkUnitImport extra="forbid" (one WU), GenomeImportRequest extra="ignore" accepts benign metadata source_files/automation_index/org - hardcoded org not blocked by Pydantic, caught by GQS detector. Validations that EXIST: provenance.source_type pattern ^(observed|declared|inferred|designed)$, array fields non-empty. Validations that DO NOT EXIST: no regex id ^WU-[A-Z]+-[0-9]+$, no hash_sha256 format, no RR- prefix, no trigger verb check, no orphan model_validator. Orphan guarantee via GQS arithmetic zeroes Zero-Orphan 10pts + Zero-Violations 10pts caps max 80 <90 gate, not schema rejection. At DB write, unresolved dependency skipped no edge created. services/genome_import.py orchestrates GQS gate -> Pydantic -> WorkUnit + WorkUnitProvenanceDetail + WorkUnitRegulatoryLink + VerdictScore (if supplied) + PII scan -> WorkEdge type=Sequence from dependencies that resolve -> GenomeVersion -> AuditLog import.attempt + import.committed only (ratify/reads NOT audit-logged). services/pii.py regex email/phone/UAN + pgcrypto encrypt/decrypt + redaction first 3 chars + *** proven live. Regulatory links use pre-existing regulatory_entries (id, regulation, clause, control_objective, requires_licensed_human) not new regulatory_register - auto-create stub row if no match (permissive upsert not strict FK). Work Graph edges reuse pre-existing work_edges table EdgeType enum - only Sequence auto-derived today. work_units.genome_version_id FK added.
- Bugs fixed: Bug3 top-level extra="forbid" rejected benign metadata -> scoped to WorkUnitImport only. Bug4 SET LOCAL app.current_client_id reverts at every commit() -> switched to session-scoped SET re-applied fresh via tenant_db(). VARCHAR(80) truncation on current_condition/desired_condition clipped safely with ellipsis in mapping layer since V8 intends short labels.
- Test/demo tenant data cleaned up.

**What's Built But Not Wired / Unused:**
- OrgApiKey/TenantDbDep used by genome router only (POST /import, GET /genome/{id}, GET /gqs, POST /ratify) proven 401 for no/wrong key. Every other router work_units/spec/census still uses old global spec_api_key dev-spec-key-change-me default NOT removed.
- pgcrypto enabled PiiFieldValue exists encrypt/decrypt proven live.
- ConsentReceipt, UploadedFile, ReviewQueueItem tables exist in schema but nothing writes to them - no consent capture, no file upload endpoint so no sha256 fingerprinting anywhere in running system, no format-classifier populating review queue.

**What's Not Started:**
- Parser v2 full: fuzzy classifier synonym dict, confidence <0.7 -> review_queue, canonical micro-step schema, Two-Track deterministic Track A + guardrailed LLM Track B, F1 Split Engine, Dedup Engine. No CSV/Excel upload path - JSON-body only. 5 raw CSVs never parsed - 14-unit sample is pre-made fixture, 62 WUs/87-step parse unverified planning claim.
- Work Graph beyond Sequence: no Shared Object/Shared Resource/Reciprocal, no bottleneck/bus-factor-1 flagging.
- GET /diff semantic business-language, GET /business-objects L1/L2/L3 progressive disclosure, GET /automation-index (+ bottleneck + cost_per_verified_unit + rule_debt - needs CostProfile data not populated), POST /keys/rotate.
- Consent-receipt creation + 90-day purge cron.
- File upload + sha256 fingerprinting (genuinely not built).
- 50-file test corpus including File 6 breaking naive Step # detection.
- Automated HTTP RLS regression test (manual proof only).
- Migrating legacy routers off global spec key.
- Demo video.

**P0 Blockers Fixed vs Remaining (corrected):**
Fixed: org hardcoded detected not rejected if present, total_work_units removed no code derives/serves full genome export yet, variant extra enforced by WorkUnitImport extra="forbid" for new imports not retroactively, orphan guarantee by GQS score arithmetic not Pydantic/FK, provenance structured in FIXED fixture (file_id, row, col, hash_sha256, timestamp, interview_id, consent_receipt_id, source_type, notes) no file_name field and only source_type pattern-validated, RR link array matched/auto-created against regulatory_entries.regulation no format enforcement, PII hand-scrubbed Person A fake email UAN XXXX-XXXX-XXXX-XXXX, Alembic 4 migrations no orgs table tenant boundary clients, Pydantic WorkUnitImport 18 fields extra=forbid on this model only, GQS blocking POST if <90 proven.
Remaining: Parser v2 incl file upload+fingerprinting, Work Graph beyond Sequence, GET /diff /business-objects L1/2/3 /automation-index POST /keys/rotate, consent receipt + purge cron, 50-file corpus, automated HTTP RLS test, migrating legacy routers off global spec key, demo video.

**Tests:** 28/28 pass (24 original + 4 GQS). Nothing committed on feat/genome-import-foundation.

## Grok Validation Re-evaluated Against Corrected Files

Grok's 5 bullets were accurate against prior docs, and remain accurate against corrected docs with these refinements:

- PASS no 5th gate / no invented V8 numeric formula: Still PASS. Corrected files explicitly keep 1/3/5 only, 2/4 interpolation, 4 gates compliance=1 cap L2 reversibility=1 cap L3 impact=1 cap L3 no evidence cap L2, reject v0.5 table, GQS labeled Master Plan v0.4 not V8. No change.
- PASS schema cracks named and treated as fixed: PASS but mechanism corrected: org hardcode detected not rejected, orphan guarantee via GQS arithmetic caps 80 not schema rejection, provenance no file_name and only source_type validated, RR link auto-create stub not strict FK, PII hand-scrubbed not scanner in fixtures. Grok's summary of fixed path org_id UUID etc is aspirational - actual fixed path is clients tenant boundary not orgs table, derive len() not yet served via GET full genome export, context.variants[] enforced for new imports only.
- PASS bugs + VARCHAR clip as tested: PASS. Bug3 and Bug4 descriptions match corrected State.md exactly, plus Org A 200 / Org B 404 / 401 proven via HTTP manual proof, VARCHAR(80) clipped with ellipsis.
- FAIL Parser v2 / bulk corpus / demo APIs not built: FAIL and more precise now: JSON-body only, 14-unit pre-made fixture, 62 WUs/87-step unverified, not started list now includes file upload + sha256 genuinely not built, ConsentReceipt/UploadedFile/ReviewQueueItem tables exist but unwritten, automation-index needs CostProfile data not populated, consent UI/cron not built, 50-file corpus, automated HTTP RLS pytest, legacy routers still on global key.
- FAIL foundation verified but uncommitted + manual proof not full 15-gate demo: FAIL remains. Net 1 of 15 gates fully live (15 RLS), ~6 partially live, ~8 specified only. GQS 6 components cover much of same ground as several gates which is why >90 without all 15 wired. Nothing committed, GQS/RLS proof manual+unit not full product demo.

Grok's highest-leverage order is directionally right, but needs correction for corrected reality: file upload + sha256 fingerprinting must come before Parser v2 (since hash is caller-supplied today), and strict validations + audit log + global key removal are security P0 that block CHRO/VC trust.

## Most Ambitious High-Leverage Actions - 5 Actions in Order

### Action 1: Commit Corrected Baseline as Verified Truth (2 hours) - Unlocks everything else
**Why highest leverage:** Stops snowflake local demo, establishes corrected source of truth vs prior inflated claims, unblocks new engineer. Without commit, every demo is local and corrected State.md will be re-inflated.
**What:**
- git add Scout-State.md Scout-Enhanced-Vision.md Scout-Reference.md AMBITIOUS_HIGH_LEVERAGE_NEXT_ACTIONS_v0.7.md + alembic/ app/ schemas/ services/ routers/ samples/ tests/
- git commit -m "feat: foundation corrected - Alembic 4, RLS wep_app + SystemSessionLocal 2 bugs fixed, GQS 40 vs 94.29 28/28, import pipeline corrected extra=ignore top-level vs forbid WorkUnit, orphan via GQS arithmetic not schema, regulatory_entries auto-create stub, file upload + sha256 NOT built, legacy global key NOT removed, Parser v2 NOT built"
- git tag v0.7-foundation-corrected + push origin feat/genome-import-foundation
- Create BRANCH PROTECTION: No direct commit of real PII, no org hardcoding check via pre-commit hook scanning for "Trianz" + email regex.
**Acceptance:** Branch pushed, tag exists, 28/28 tests pass on fresh docker-compose up -d postgres alembic upgrade head pytest.
**Effort:** Low, Leverage: Highest (unblocks parallel work).

### Action 2: Close Security & Trust Gaps That Break CHRO/VC Demo (1 day) - Moat
**Why:** CHRO pitch promises consent + purge + encryption + RLS + no global key. Currently consent + purge not built, legacy routers still use dev-spec-key-change-me, audit logs only import attempt/committed not ratify/reads, regulatory auto-create stub is permissive not strict, Pydantic missing critical validations, file hash caller-supplied not computed. These are trust blockers - VC will ask "is data tamper-evident?" and answer today is "caller-supplied hash" which fails.
**What:**
- File upload endpoint POST /api/files/upload: multipart, calculate sha256 server-side via hashlib, store in UploadedFile table (id, client_id, file_name, sha256, size, uploaded_at, uploaded_by), return file_id + sha256. Update provenance to require file_id exists in UploadedFile and hash matches UploadedFile.sha256 - now hash is computed not caller-supplied. This unblocks provenance Structured% being real.
- Strict validations add: schemas/genome_import.py WorkUnitImport: id pattern ^WU-[A-Z0-9_-]+$ (keep permissive but at least pattern, not free), hash_sha256 pattern ^[a-f0-9]{64}$ if supplied, regulatory_register_link entries must match ^RR- or allow but flag auto-created stub as needs_review=True in regulatory_entries, trigger must contain verb list check warning not block (log to review_queue if no verb). Add model_validator for orphan: if dependency not in import, add to GQS violations list + create ReviewQueueItem type=orphan_dependency (do not silently skip edge - make visible).
- Audit logs: Extend AuditLog to log POST /ratify + GET /genome/{id} + GET /gqs with actor client_id, action, resource, timestamp, ip. Currently only import attempt/committed.
- Migrate legacy routers: Move work_units/spec/census off global spec_api_key dev-spec-key-change-me onto TenantDbDep per-org X-Spec-Key. Add deprecation warning for global key, then remove default. This is G6 Enforcement Gateway moat - currently only genome router enforces per-org key.
- Regulatory strict mode option: Add query param ?strict=true on POST /import: if strict, RR link must exist in regulatory_entries.regulation else 400 (no auto-create). If not strict (default), auto-create stub with needs_review=True + ReviewQueueItem type=regulatory_stub.
- Tests: tests/test_file_upload_sha256.py (upload file -> sha256 computed server-side matches hashlib), tests/test_pydantic_strict.py (id pattern, hash format, trigger verb warning -> review_queue), tests/test_audit_logs.py (ratify + reads logged), tests/test_legacy_key_removal.py (old global key 401 after migration).
**Acceptance:** File upload computes sha256 server-side, provenance hash must match UploadedFile, audit logs include ratify/reads, legacy routers require per-org key not global, strict mode blocks auto-create RR stub. Tests 32+ pass.
**Effort:** Medium, Leverage: Highest for trust (CHRO signs only if consent + RLS + no global key + tamper-evident hash real).

### Action 3: Ship CHRO Playback Surface - Business-Objects Progressive Disclosure + Ratify BO-Level (2 days) - Wedge
**Why:** This is the wedge pitch "ratify Business Objects not 1,116 fields" and only path that makes v1/v2/v3 ratification visible. Currently POST /ratify is whole-version only, not BO-level or partial. No GET /business-objects. Without this, CHRO sees JSON blob not ratifiable objects.
**What:**
- GET /api/genome/{version_id}/business-objects: Returns L1 list of BOs with state machines + WU count per BO + ratification status per BO + bottleneck flag placeholder. Source: group work_units by business_object, count, check if EntityType.state_machine is "[]" then return [] + flag needs_state_machine=True (since import doesn't populate state_machine yet). For each BO: {bo_name, wu_count, ratified: bool from GenomeVersion ratified flag or new ratification table, bottleneck: false placeholder until Action 4, state_machine: EntityType.state_machine or []}.
- GET /api/genome/{version_id}/business-objects/{bo_name}/work-units: L2 list WUs for BO with id, name, current->desired (clipped 80 chars with ellipsis already), SLA time_per_case_min, VERDICT autonomy, provenance file_id row hash, GQS contribution.
- GET /api/genome/{version_id}/work-units/{wu_id}: L3 full 18 attrs drill-down + evidence trail file_id row hash interview_id consent_receipt_id source_type + RR links + dependencies + failure semantics + variant array context.variants[].
- POST /api/genome/{version_id}/ratify: Extend existing whole-version ratify to support BO-level + partial WU: Body {business_object: string optional, work_unit_ids: [ids] optional, approved: bool, comment: string}. If business_object supplied, ratify all WUs for that BO. If work_unit_ids supplied, partial approval. If neither, whole-version (existing behavior). Store in new table ratifications (id, version_id, client_id, business_object, work_unit_id nullable, approved, comment, ratified_by, ratified_at) + update GenomeVersion ratified flag if whole-version approved. Audit log ratify.
- Update GET /api/genome/{version_id} response to include version_history placeholder + ratifications summary + business_objects summary (currently returns {version_id, gqs, ratified, work_unit_count, work_units: [...] } using WorkUnitOut schema DB-column shaped inputs joined string not array - keep for backward compat, add new fields business_objects_summary).
- Tests: tests/test_business_objects_progressive.py (L1 list BOs from 14 WUs -> expect 5-7 BOs, L2 WUs per BO, L3 full 18 attrs, ratify BO-level + partial + whole-version, audit logged).
**Acceptance:** CHRO can GET /business-objects -> see BOs + WU counts, GET /business-objects/{bo}/work-units -> see WUs for that BO, GET /work-units/{wu_id} -> full 18 attrs, POST /ratify with business_object or work_unit_ids partial. Demo: 14 WUs -> 6 BOs -> ratify 1 BO -> remaining BOs unratified. This is 90-min Playback 2 without waiting for Parser v2.
**Effort:** Medium, Leverage: Highest for wedge (CHRO sees ratifiable objects not JSON blob).

### Action 4: Automation-Index + Bottleneck Bus-Factor-1 + Minimum Parser v2 Visible Upload (2 days) - VC Number
**Why:** Turns 14 imported WUs into ROI slide (hours saveable, bottleneck, rule debt) before new parser work. That is VC number and CHRO burnout story. Sequence-only graph already exists, but Shared Resource/bottleneck not built. Minimum Parser v2 with synonym classifier + Track A deterministic map unlocks "bulk docs in, genome out" instead of fixture paste. Together they make demo visible.
**What:**
- GET /api/genome/{version_id}/automation-index: Returns {L5_count, L4_count, L3_count, L2_count, L1_count, total_hours_current: sum sla_hours * volume_per_month, total_hours_saveable: sum where autonomy L4/L5, highest_value_targets: [WU IDs sorted by hours saveable], cost_per_verified_unit: placeholder needs CostProfile data not populated -> return null + flag needs_cost_profile=True, rule_debt_count: count where verification_method mapped to human_spot_check fallback, bottleneck_view: [{authority: Person A, total_hours_per_day: sum time_per_case_min/60 * frequency factor, bus_factor: 1 if authority appears in >3 WUs and total_hours_per_day >6, risk: P0/P1, wu_ids: [...] }], work_graph_summary: {sequence_edges: 13, shared_object_edges: 0 placeholder, shared_resource_edges: bottleneck_view length, reciprocal_edges: 0 placeholder}}. Calculate total_hours_per_day: For each authority, sum (time_per_case_min/60) * (volume_per_month/22) approx hours per day. If >6 hrs/day and appears in >3 WUs -> bus_factor 1 P0 bottleneck. This is Shared Resource detection minimum viable - not full Thompson but enough for CHRO burnout story.
- Work Graph extension: Add Shared Object detection: Group work_units by business_object - if same BO appears in >1 WU -> create Shared Object edge between those WUs reason "Same Business Object {bo_name} operated by multiple WUs" detection_method="business_object_overlap". Add to work_edges table with type=shared_object. This is low effort high visibility.
- Minimum Parser v2 + visible upload:
  - Synonym classifier: parser_hints = {Step #: [Step #, Step No, Sl No, Activity, Task, S.No, No], What happens: [What happens, Description, Activity Description, Task Description], etc.} Use fuzzy Levenshtein distance <=2 to match column headers from CSV/Excel to canonical micro-step fields. Confidence 0-1: exact match 1.0, synonym match 0.8, fuzzy <=2 distance 0.6, else 0.3. If confidence <0.7 -> create ReviewQueueItem (file_id, row, col, confidence, reason, raw_text).
  - Track A deterministic map (NO LLM): For each canonical micro-step row, map deterministic fields: ID from step_id_raw substring must exist in raw_text, Business Object from business_object_raw substring, Current/Desired from current_condition/desired_condition raw, Trigger from trigger_raw must contain verb, Provenance from file_id row hash timestamp (hash computed from file upload sha256 not caller), RR IDs from regulatory_register_link raw if contains RR- pattern else leave empty + flag review. If any deterministic field not substring of raw_text -> reject to review_queue not silently accept. This is guardrail.
  - File upload integration: POST /api/files/upload already built in Action 2 now used by Parser v2: CSV/Excel file -> UploadedFile with sha256 -> parse via synonym classifier -> canonical micro-step schema {step_id_raw, what_happens_raw, decision_branches_raw, system_raw, step_type_raw, data_fields_raw, docs_policies_raw, automation_tag_raw, time_per_case_raw, frequency_raw, volume_raw, agent_notes_raw, file_id, row, hash_sha256 from file sha256, raw_text} -> Track A map -> produce WorkUnitImport list -> POST /api/genome/import JSON-body path (reuse existing import orchestration GQS gate etc). So Parser v2 outputs JSON that goes through existing import path proven 40 vs 94.29.
  - Defer: Track B LLM assist Context.variants[] Acceptance[] Evidence[] Verification Failure with guardrail substring, full F1 Split Engine logged reason, Dedup Engine hash BO+Desired+Context variant key -> variants[] array, 50-file corpus - defer until one real CSV/Excel round-trip clears GQS >90.
- Tests: tests/test_automation_index.py (14 WUs -> L counts + total_hours_current + saveable + highest_value_targets + bottleneck_view Person A 9hrs/day bus_factor 1), tests/test_work_graph_shared_object.py (same BO -> Shared Object edge), tests/test_parser_v2_minimum.py (upload CSV with Step # synonym Sl No -> confidence 0.8 -> canonical -> Track A map -> 3 WUs -> import -> GQS 94.29).
**Acceptance:** GET /automation-index returns L counts + hours saveable + bottleneck_view with bus_factor 1, work_edges includes Shared Object edges beyond Sequence, POST /files/upload + Parser v2 minimum produces 3 WUs from CSV that pass GQS >90 via existing import path.
**Effort:** High, Leverage: Highest for VC number + CHRO burnout story + unlocking bulk docs in genome out.

### Action 5: Close G6/DPDP + Demo Polish in Parallel (1 day) - Enforcement Gateway Moat
**Why:** These convert working import into something CHRO can sign and VC sees as moat. Do not block first visible BO/index demo on 50-file corpus or semantic GET /diff - those are week-two polish once upload + BO playback exist.
**What:**
- Automated HTTP RLS regression test: tests/test_rls_http.py - create client A + B via API with per-org keys (not global key), POST /api/genome/import with key A -> 201, GET /api/genome/{id} with key A -> 200 14 WUs, GET with key B -> 404 genuinely invisible not leak, no key -> 401, wrong key -> 401, POST /ratify with key B -> 404 (cannot ratify other org's genome). This turns manual proof into permanent regression. Currently only manual script cleaned up.
- POST /api/org/keys/rotate: Generate new key, hash with bcrypt, store in org_api_keys or clients table api_key_hash, keep old key as inactive with expires_at, return new key once (show only once). Endpoint requires existing valid X-Spec-Key. Audit log key rotation. This is moat - key rotation.
- Consent-receipt creation flow: POST /api/consent/receipts: Body {interview_id, interviewee_initials, consent_type: transcript|notes_only, granted_at, expires_at, purpose}. Store in ConsentReceipt table (id, client_id, interview_id, interviewee_initials scrubbed, consent_type, granted_at, expires_at, purpose, created_at). Update provenance to require consent_receipt_id exists in ConsentReceipt if source_type=Observed from interview. This makes consent promise real not just table exists.
- 90-day purge cron: Daily job DELETE FROM transcripts WHERE auto_purge_at < NOW() or soft delete metadata + log audit. Implement as Celery beat or simple cron endpoint POST /api/admin/purge (requires system role SystemSessionLocal) that deletes transcripts where consent expires + auto-purge. Log audit.
- GET /diff semantic business-language: GET /api/genome/diff?from=v1&to=v2 returns business language sentences not hash diff: Template engine: Added WU {id} {name} for BO {bo}, Removed WU {id}, Added variant {key} {condition} split 4th+8th vs <=1L 4th only, Fixed schema violation org hardcoded, Added BO {bo} with state machine, Changed Desired Condition {old} -> {new}. Compare two versions WU lists, generate sentences via templates. This is week-two polish but low effort.
- Demo video + commit: Record 7-day flow video: Founder why (Vision doc) -> Function Leader Inferred v1 (question bank placeholder) -> SME Detailed v2 (transcripts placeholder) -> Bulk Ingest (upload CSV via POST /files/upload -> Parser v2 minimum -> 3 WUs) -> Genome (POST /import -> GQS 40 blocked vs 94.29 pass -> 14 WUs 13 edges + Shared Object edges) -> Playback (GET /business-objects L1 -> L2 -> L3 -> POST /ratify BO-level) -> WEP import -> Spec API check with X-Spec-Key G6 Enforcement Gateway (GET /spec/check with authority+evidence+object state denied without token, allowed with token) -> automation-index bottleneck view Person A 9hrs/day bus_factor 1.
- Update Scout-State.md with new reality: Alembic 5? migrations if needed, tests 35+ pass, file upload sha256 real, audit logs include ratify/reads, legacy global key removed, consent receipt creation real, 90-day purge cron real, business-objects L1-L3 real, automation-index bottleneck real, Parser v2 minimum real, RLS automated regression real, nothing committed -> committed with tag v0.8-demo-ready.
**Acceptance:** Automated HTTP RLS pytest passes, POST /keys/rotate returns new key once old inactive, POST /consent/receipts creates receipt provenance requires it, purge cron deletes expired transcripts, GET /diff returns business sentences, demo video 7-day flow recorded, branch pushed tag v0.8-demo-ready, tests 35+ pass.
**Effort:** Medium, Leverage: High for moat + CHRO sign.

## Implementation Order - 7-Day Sprint

Day 1 Morning: Action 1 Commit corrected baseline + pre-commit hook no org hardcoding no real PII.
Day 1 Afternoon - Day 2 Morning: Action 2 File upload + sha256 + strict validations + audit logs + migrate legacy routers off global key + regulatory strict mode.
Day 2 Afternoon - Day 3: Action 3 Business-objects L1/L2/L3 + ratify BO-level + partial + business_objects_summary in GET genome response.
Day 4 - Day 5 Morning: Action 4 Automation-index + bottleneck bus-factor-1 + Shared Object detection + Minimum Parser v2 synonym classifier + Track A deterministic + file upload integration.
Day 5 Afternoon - Day 6: Action 5 Automated HTTP RLS pytest + keys rotate + consent receipt + purge cron + GET /diff semantic + demo video + update State.md.
Day 7: Final verification docker-compose down up alembic upgrade head pytest 35+ pass + commit push tag v0.8-demo-ready + handoff to VC/CHRO.

## Risks and Mitigations

- Risk: File upload + sha256 breaks existing JSON-body import path that uses caller-supplied hash. Mitigation: Keep JSON-body path backward compat - if file_id supplied, verify hash matches UploadedFile.sha256, if not supplied, accept caller hash but log warning + flag provenance hash not computed + GQS ProvenanceStructured% penalizes caller-supplied hash.
- Risk: Migrating legacy routers off global key breaks existing clients using dev-spec-key-change-me. Mitigation: Add deprecation period 1 week with warning header X-Deprecated-Global-Key, then remove. Document in State.md legacy key NOT removed -> removed in v0.8.
- Risk: Parser v2 minimum fuzzy Levenshtein may misclassify columns <0.7 -> review_queue grows. Mitigation: Confidence <0.7 -> ReviewQueueItem not block import, human reviews queue, low effort.
- Risk: Automation-index total_hours_current calculation needs volume_per_month and time_per_case_min - some WUs have raw fallback not parsed. Mitigation: If raw present, parse via regex for hours, if not parsable return null + flag needs_sla_parsing=True, don't block.
- Risk: CostProfile data needed for cost_per_verified_unit not populated by import path. Mitigation: Return null + flag needs_cost_profile=True in automation-index response, document as Vision not built, don't invent.

## Commit Plan

- Commit 1 (Day 1): Foundation corrected - 4 migrations, RLS 2 bugs, GQS 40 vs 94.29, import corrected mechanisms, PII proven, 28/28 tests, nothing committed -> committed tag v0.7-foundation-corrected.
- Commit 2 (Day 2): Security + trust gaps closed - file upload sha256 server-side, strict validations, audit logs ratify/reads, legacy global key removed, regulatory strict mode, 32+ tests tag v0.7-security-closed.
- Commit 3 (Day 3): CHRO playback surface - business-objects L1/L2/L3, ratify BO-level partial, business_objects_summary, 34+ tests tag v0.7-playback-surface.
- Commit 4 (Day 5): Automation-index + bottleneck + Shared Object + Parser v2 minimum, 35+ tests tag v0.8-demo-ready-minimum.
- Commit 5 (Day 6): G6/DPDP + demo polish - automated HTTP RLS pytest, keys rotate, consent receipt + purge cron, GET /diff semantic, demo video, update State.md, 35+ tests tag v0.8-demo-ready.

## Demo Script for VC/CHRO - What They See in Browser

1. Upload: POST /api/files/upload HR time-motion CSV -> returns file_id + sha256 computed server-side tamper-evident.
2. Import: Parser v2 minimum synonym classifier Sl No -> Step # confidence 0.8 -> Track A deterministic map -> 3 WUs -> POST /api/genome/import -> 201 GQS 94.29 14 WUs 13 Sequence + Shared Object edges.
3. Quality gate: POST scrubbed genome 6 violations -> 400 GQS 40 62 violations listed - blocking works.
4. Tenant isolation: GET /api/genome/{id} Org A 200 14 WUs, Org B 404 genuinely invisible, no key 401 - proven via automated pytest now.
5. Playback: GET /business-objects L1 -> 6 BOs Employee Record BGV Case etc + WU counts + ratified false + needs_state_machine flag, GET /business-objects/Employee Record/work-units L2 -> 5 WUs, GET /work-units/WU-OFF-02A L3 full 18 attrs + evidence file_id row hash + RR links + dependencies + failure semantics + variants array.
6. Ratify: POST /ratify BO-level Employee Record approved true comment + partial WU rejection - CHRO ratifies objects not 1,116 fields.
7. Automation-index: GET /automation-index -> L5 0 L4 2 L3 3 etc, total_hours_current 356 hrs/mo, saveable 142 hrs/mo, highest_value_targets [WU-OFF-03, WU-US-07], bottleneck_view Person A 9hrs/day bus_factor 1 P0 risk burnout, rule_debt_count 3 human_spot_check fallback.
8. Enforcement Gateway G6: GET /spec/check with authority+evidence+object state denied without X-Spec-Key, allowed with token - per-org key hash + rotation + audit log ratify/reads.
9. Consent + purge: POST /consent/receipts interview consent granted, provenance requires consent_receipt_id for Observed, purge cron deletes expired transcripts auto-purge 90d - DPDP compliant promise real not just table.
10. Diff: GET /diff?from=v1&to=v2 -> "Added variant joining_bonus >1L split 4th+8th vs <=1L 4th only, Fixed schema violation org hardcoded, Added BO Retention Case with state machine" business language not hash diff.

VC sees: Foundation verified multi-tenant + quality gate + PII encryption + tamper-evident hash real + Enforcement Gateway per-org key + Pack flywheel vision + GQS + automation-index ROI + bottleneck risk quantified pre-engineering. CHRO sees: Tenant isolation + PII encryption + consent receipt + purge + ratify BO by BO not field by field + burnout risk flagged + repeat queries reduced via L4 chatbot + tacit knowledge stays when people leave.

Nothing invented: All numbers from running code, all gates from actual Pydantic/GQS/RLS, all provenance file_id row hash from UploadedFile sha256 server-side, no org hardcoding, no real PII Person A fake email UAN XXXX-XXXX-XXXX-XXXX.

Date: 2026-08-26 | Branch: feat/genome-import-foundation -> v0.8-demo-ready | Tests: 28/28 -> 35+ | GQS: 40 vs 94.29 | Postgres: Running | Commit plan 5 commits
